---
phase: 01-account-shell
plan: "01"
subsystem: infra
tags: [next.js, vitest, playwright, zod, typescript, tailwind, drizzle, next-auth]

requires: []

provides:
  - Next.js 15.5.18 project scaffold with exact-pinned deps (no caret for locked-stack)
  - Vitest 4.x unit/integration test runner (Wave 0 infrastructure)
  - Playwright 1.59 E2E test runner with chromium project
  - Zod-validated env loader (lib/env.ts) for DATABASE_URL, AUTH_SECRET, etc.
  - Test fixtures (testUser, testLesson, allowedEmail) in tests/fixtures.ts
  - .env.example documenting all required vars with Supabase URL format

affects:
  - 01-02 (DB schema — imports lib/env.ts for DATABASE_URL)
  - 01-03 (auth — imports lib/env.ts for AUTH_SECRET, AUTH_RESEND_KEY)
  - 01-04 (seed script — uses tests/fixtures.ts shapes)
  - 01-05 (UI pages — uses vitest + playwright infra)
  - 01-06 (E2E + deploy — uses playwright + env validation)

tech-stack:
  added:
    - next@15.5.18 (exact pin)
    - react@18.3.1, react-dom@18.3.1 (exact pin)
    - next-auth@5.0.0-beta.31 (exact pin)
    - "@auth/drizzle-adapter@^1.11.2"
    - drizzle-orm@^0.45.2, drizzle-kit@^0.31.10
    - postgres@^3.4.9
    - resend@^6.12.3
    - zod@^4.4.3
    - vitest@^4.1.5, "@vitejs/plugin-react@^5"
    - "@testing-library/react@^16, @testing-library/jest-dom@^6"
    - happy-dom@^15
    - "@playwright/test@^1.59.1"
    - tailwindcss@^4, "@tailwindcss/postcss@^4"
    - tsx@^4.21.0
  patterns:
    - Exact version pinning for security-critical deps (next, react, next-auth)
    - Dual DATABASE_URL pattern (pooler 6543 + direct 5432) per RESEARCH Pitfall 3
    - vi.resetModules() for module cache busting in env unit tests
    - vitest run --reporter=verbose (not --reporter=basic, removed in vitest 4.x)

key-files:
  created:
    - package.json (exact-pinned manifest, all npm scripts)
    - package-lock.json (generated by npm install)
    - tsconfig.json (strict, ES2022, @/* alias)
    - next.config.ts (outputFileTracingRoot for CON-nextjs-tracing-root)
    - postcss.config.mjs (@tailwindcss/postcss for Tailwind v4)
    - .nvmrc (20)
    - .gitignore (updated with node_modules, .next/, .env*.local, test artifacts)
    - vitest.config.ts (happy-dom, @/* alias, setupFiles)
    - playwright.config.ts (chromium, serial workers, baseURL localhost:3000)
    - tests/setup.ts (@testing-library/jest-dom/vitest import)
    - tests/fixtures.ts (testUser, testLesson, allowedEmail, fixedNow)
    - tests/sanity.test.ts (1 passing sanity test)
    - e2e/sanity.spec.ts (1 playwright spec)
    - lib/env.ts (zod envSchema, exports env + Env type)
    - lib/__tests__/env.test.ts (4 tests: missing URL, short secret, missing key, valid)
    - .env.example (documented template for all required vars)
  modified:
    - .gitignore (appended Next.js, Node, env, test artifact patterns)

key-decisions:
  - "D-09: NextAuth.js v5 (next-auth@5.0.0-beta.31 exact pin) — beta but stable for App Router"
  - "D-10: Drizzle ORM with postgres-js driver — edge-compatible, recommended by Drizzle for Supabase"
  - "D-11: Tailwind v4 with @tailwindcss/postcss plugin — CSS-only @import syntax, no tailwind.config.js"
  - "vitest 4.x --reporter=verbose used instead of --reporter=basic (basic removed in vitest 4.x)"
  - "dual DATABASE_URL vars established: pooler (port 6543) for app, direct (port 5432) for migrations"
  - "AUTH_SECRET min(32) enforced via zod — preempts Pitfall 6 (JWT signature failure in production)"

patterns-established:
  - "Exact version pinning: next@15.5.18, react@18.3.1, next-auth@5.0.0-beta.31 — no caret"
  - "Env validation at module load via zod.parse(process.env) — crashes early with structured error"
  - "Test fixtures are deterministic (no Date.now() at module load), use fixedNow() for time injection"
  - "vi.resetModules() for env test isolation — avoids stale module cache across test cases"

requirements-completed: [ACC-01, ACC-02, INV-01]

duration: 30min
completed: "2026-05-09"
---

# Phase 01 Plan 01: Bootstrap Next.js 15.5.18 Project — Summary

**Next.js 15.5.18 greenfield project bootstrapped with exact-pinned deps, vitest 4.x + Playwright 1.59 test infrastructure, and zod-validated env loader covering DATABASE_URL, AUTH_SECRET, and AUTH_RESEND_KEY**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-05-09T18:50:00Z
- **Completed:** 2026-05-09T19:02:41Z
- **Tasks:** 3 of 3
- **Files modified:** 15 created, 1 modified

## Accomplishments

- Next.js 15.5.18 project scaffolded manually (root was non-empty — create-next-app refused), all deps installed via npm, package-lock.json generated
- Test runners configured: vitest 4.1.5 (5 tests passing) + Playwright 1.59 (1 spec listed + passing)
- Zod env validation in place: `lib/env.ts` crashes on startup with structured error if any of DATABASE_URL, DATABASE_URL_DIRECT, AUTH_SECRET, AUTH_RESEND_KEY is missing or malformed
- `.env.example` documents the public contract for Plan 02 account provisioning

## Task Commits

1. **Task 1: Bootstrap Next.js 15.5.18 project with exact-pinned deps** - `061abe0` (chore)
2. **Task 2: Set up vitest + Playwright test infrastructure (Wave 0)** - `9c09bd4` (feat)
3. **Task 3: Env scaffolding with zod validation + .env.example** - `3c9b0af` (feat)

## Files Created/Modified

- `package.json` — exact-pinned manifest with all scripts (dev, build, test, test:e2e, db:*, typecheck)
- `package-lock.json` — generated by npm install, lockfile for reproducible installs
- `tsconfig.json` — strict mode, ES2022 target, @/* path alias, App Router compatible
- `next.config.ts` — outputFileTracingRoot (CON-nextjs-tracing-root, NON-NEGOTIABLE)
- `postcss.config.mjs` — @tailwindcss/postcss plugin (Tailwind v4 syntax)
- `.nvmrc` — Node 20 pin
- `.gitignore` — updated with node_modules/, .next/, .env*.local, test-results/, playwright-report/
- `vitest.config.ts` — happy-dom environment, React plugin, @/* alias, setupFiles
- `playwright.config.ts` — chromium project, serial workers, baseURL localhost:3000
- `tests/setup.ts` — @testing-library/jest-dom/vitest import
- `tests/fixtures.ts` — testUser, testLesson, allowedEmail, fixedNow() exported
- `tests/sanity.test.ts` — sanity check (1 passing)
- `e2e/sanity.spec.ts` — playwright sanity (passes without dev server)
- `lib/env.ts` — zod envSchema with DATABASE_URL, DATABASE_URL_DIRECT, AUTH_SECRET (min 32), AUTH_RESEND_KEY (min 10), AUTH_URL (optional), SEED_ADMIN_EMAIL (optional), NODE_ENV (enum)
- `lib/__tests__/env.test.ts` — 4 tests: missing DATABASE_URL, short AUTH_SECRET, missing AUTH_RESEND_KEY, successful parse
- `.env.example` — documented template with Supabase URL format, all 4 required vars

## Decisions Made

- Used `vitest run --reporter=verbose` (see Deviations — `basic` removed in vitest 4.x)
- Used `vi.resetModules()` for env test cache busting instead of dynamic import with query string (more reliable across vitest versions)
- Dual DATABASE_URL pattern established now even though DB migration code isn't until Plan 03 — env validation must be in place first

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed --reporter=basic → --reporter=verbose (vitest 4.x incompatibility)**
- **Found during:** Task 2 (test infrastructure setup)
- **Issue:** `npm run test` failed with "Failed to load custom Reporter from basic" — the `basic` reporter was removed in vitest 4.x (exists as `minimal` or `verbose` now)
- **Fix:** Changed `"test": "vitest run --reporter=basic"` to `"test": "vitest run --reporter=verbose"` in package.json. The `verbose` reporter provides full test output, suitable for CI and developer feedback.
- **Files modified:** `package.json`
- **Verification:** `npm run test` exits 0 with all tests listed
- **Committed in:** `9c09bd4` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug)
**Impact on plan:** Essential fix — without it `npm run test` would never run. No scope creep.

## Issues Encountered

- `npm install` reported 7 vulnerabilities (6 moderate, 1 critical) in transitive deps. These are pre-existing in the beta ecosystem and not fixable without breaking changes (`npm audit fix --force` would update locked versions). Accepted per threat T-01-08 (dep pinning accepted risk). Will track in COSTS.md security notes.

## Threats Addressed

| Threat | Status | How |
|--------|--------|-----|
| T-01-07 (env misconfiguration) | mitigated | `lib/env.ts` zod.parse at module load — crashes with structured error before any auth/DB code runs |
| T-01-08 (dep chain spoofing) | accepted | Exact pins for next, react, next-auth; caret for non-critical; lockfile committed |
| T-01-09 (secrets in git) | mitigated | `.gitignore` includes `.env*.local`; `.env.example` ships placeholder values only; `.env.local` confirmed gitignored |

## Pitfalls Preempted

- **Pitfall 2** (prepare:false for pooler): DATABASE_URL env var established; `prepare: false` will be used in Plan 03 db client
- **Pitfall 3** (dual DATABASE_URL): Both DATABASE_URL and DATABASE_URL_DIRECT required in envSchema
- **Pitfall 6** (AUTH_SECRET min length): `AUTH_SECRET: z.string().min(32)` enforced
- **Pitfall 7** (NextAuth beta pin): `next-auth@5.0.0-beta.31` exact (no caret)
- **Pitfall 8** (Tailwind v4 syntax): `@tailwindcss/postcss` in postcss.config.mjs, no tailwind.config.js created

## User Setup Required

Plan 02 requires creating real accounts and filling `.env.local`:
- Supabase project → get DATABASE_URL (pooler 6543) and DATABASE_URL_DIRECT (direct 5432)
- Resend account → get AUTH_RESEND_KEY
- Generate AUTH_SECRET via `npx auth secret`

See `.env.example` for the full template.

## Next Phase Readiness

- Plan 02 can immediately: create Supabase project, fill `.env.local`, run `npm run dev`, verify `lib/env.ts` validation works
- Plan 03 can immediately: add Drizzle schema in `lib/db/schema.ts`, add `lib/db/index.ts` with `prepare: false`
- Plan 04 can immediately: add NextAuth split config (auth.config.ts + auth.ts)
- All plans can write vitest tests using `tests/fixtures.ts` shapes
- All plans can write Playwright E2E specs in `e2e/`

---
## Self-Check: PASSED

All 14 key files exist and all 3 task commits verified in git history.

*Phase: 01-account-shell*
*Completed: 2026-05-09*
