---
phase: 05-scenes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/board/scenes/types.ts
  - lib/board/scenes/explain-column-addition.ts
  - lib/board/scenes/explain-column-subtraction.ts
  - lib/board/scenes/explain-fraction-addition.ts
  - lib/board/scenes/explain-fraction-subtraction.ts
  - lib/board/scenes/explain-fraction-comparison.ts
  - lib/board/scenes/explain-fraction-simplification.ts
  - lib/board/scenes/explain-decimal-addition.ts
  - lib/board/scenes/explain-percent-calculation.ts
  - lib/board/scenes/index.ts
  - lib/board/scenes/__tests__/explain-column-addition.test.ts
  - lib/board/scenes/__tests__/explain-column-subtraction.test.ts
  - lib/board/scenes/__tests__/explain-fraction-addition.test.ts
  - lib/board/scenes/__tests__/explain-fraction-subtraction.test.ts
  - lib/board/scenes/__tests__/explain-fraction-comparison.test.ts
  - lib/board/scenes/__tests__/explain-fraction-simplification.test.ts
  - lib/board/scenes/__tests__/explain-decimal-addition.test.ts
  - lib/board/scenes/__tests__/explain-percent-calculation.test.ts
  - lib/board/tools.ts
  - lib/board/index.ts
  - app/api/draw/route.ts
autonomous: true
requirements:
  - BRD-02

must_haves:
  truths:
    - "LLM receives 24+ tools in its schema (9 primitives + 15 explain_* + finish)"
    - "When LLM calls an explain_* scene, route.ts intercepts it server-side and emits expanded primitive tool_use SSE events — client receives no raw explain_* event"
    - "System prompt encourages scenes over primitives for 5th-grade curriculum topics (soft guidance)"
    - "8 scenes implemented: explain_column_addition, explain_column_subtraction, explain_fraction_addition, explain_fraction_subtraction, explain_fraction_comparison, explain_fraction_simplification, explain_decimal_addition, explain_percent_calculation"
    - "Each scene yields only existing Phase 4 primitives (draw_text, draw_line, draw_rectangle, draw_circle, draw_arrow, highlight_region, wait, say)"
    - "Unit tests for all 8 scenes pass: primitive sequence, coordinate bounds (≥0, ≤800 x, ≤600 y), wait between major steps"
  artifacts:
    - path: "lib/board/scenes/types.ts"
      provides: "PrimitiveCall type, SceneGenerator type, SceneName union"
      exports: ["PrimitiveCall", "SceneGenerator", "SceneName"]
    - path: "lib/board/scenes/index.ts"
      provides: "sceneRegistry mapping scene name → generator function"
      exports: ["sceneRegistry", "isSceneName"]
    - path: "lib/board/scenes/explain-column-addition.ts"
      provides: "explainColumnAddition generator"
      exports: ["explainColumnAddition"]
    - path: "lib/board/tools.ts"
      provides: "Extended with 15 SceneToolSchema entries alongside existing 9 primitives"
      contains: "explain_column_addition"
    - path: "app/api/draw/route.ts"
      provides: "Scene interception in agent loop, scene_used tracking, updated system prompt"
      contains: "explain_"
  key_links:
    - from: "app/api/draw/route.ts"
      to: "lib/board/scenes/index.ts"
      via: "isSceneName(e.name) check in tool_calls.function.arguments.done handler"
      pattern: "isSceneName"
    - from: "lib/board/scenes/explain-column-addition.ts"
      to: "lib/board/scenes/types.ts"
      via: "PrimitiveCall type import"
      pattern: "PrimitiveCall"
    - from: "lib/board/tools.ts"
      to: "app/api/draw/route.ts"
      via: "allBoardTools (primitives + scenes) mapped to openaiTools"
      pattern: "allBoardTools"
---

<objective>
Establish the scene architecture (types, registry) and implement 8 simpler scenes for the Phase 5 explain_* feature.

Purpose: The LLM currently uses 9 low-level primitives to compose explanations manually — expensive in output tokens and inconsistent in layout. Scene tools give the LLM high-level, pedagogically correct operations: `explain_column_addition(245, 874)` instead of 15–20 individual draw_text/draw_line calls.

Output:
- `lib/board/scenes/types.ts` — PrimitiveCall, SceneGenerator, SceneName types
- `lib/board/scenes/index.ts` — sceneRegistry + isSceneName guard
- 8 scene files in `lib/board/scenes/`
- 8 unit test files in `lib/board/scenes/__tests__/`
- `lib/board/tools.ts` extended with 15 scene schemas (per D-05; Wave 2 adds remaining 7 scene files, all 15 schemas land here in Wave 1 so route.ts and tests compile cleanly)
- `lib/board/index.ts` extended with scene exports
- `app/api/draw/route.ts` — scene interception + system prompt update
</objective>

<execution_context>
@C:/Users/krato/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/krato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scenes/05-CONTEXT.md

<!-- Phase 4 deliverables referenced for patterns -->
@.planning/phases/04-board-deploy/04-01-SUMMARY.md
@.planning/phases/04-board-deploy/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Existing contracts the executor must build against. Do not re-read source files. -->

From lib/board/tools.ts (current):
```typescript
export type DrawToolName =
  | 'draw_text' | 'draw_rectangle' | 'draw_line' | 'draw_circle' | 'draw_arrow'
  | 'highlight_region' | 'wait' | 'say' | 'finish'

export interface JSONSchemaProperty {
  type: 'string' | 'number' | 'boolean' | 'object' | 'array'
  description?: string
}

export interface DrawToolSchema {
  name: DrawToolName
  description: string
  parameters: {
    type: 'object'
    properties: Record<string, JSONSchemaProperty>
    required: string[]
    additionalProperties?: boolean
  }
}

export const drawTools: DrawToolSchema[]  // 9 entries: draw_text through finish
```

From lib/board/index.ts (current):
```typescript
export type { DrawToolName, DrawToolSchema, JSONSchemaProperty } from './tools'
export { drawTools } from './tools'
export type { ExecuteResult } from './executor'
export { executeToolCall } from './executor'
```

From app/api/draw/route.ts (current agent-loop pattern):
```typescript
// openaiTools built from drawTools:
const openaiTools: ChatCompletionTool[] = drawTools.map((t) => ({ ... }))

// In agent loop:
completion.on('tool_calls.function.arguments.done', (e) => {
  if (e.name === 'finish') { finishedExplicitly = true; return }
  totalToolCalls++
  let input: unknown = e.parsed_arguments
  if (input == null) { try { input = JSON.parse(e.arguments) } catch { input = {} } }
  send({ type: 'tool_use', id: `t${turn}_${e.index}`, name: e.name, input })
})

// Done event:
send({ type: 'done', finish_reason: ..., usage: { prompt_tokens, completion_tokens, total_tokens } })
```

Canvas: 800×600 virtual pixels, (0,0) top-left.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define types, add all 15 scene schemas to tools.ts, set up scene registry</name>
  <files>
    lib/board/scenes/types.ts
    lib/board/scenes/index.ts
    lib/board/tools.ts
    lib/board/index.ts
    lib/board/__tests__/tools.test.ts
  </files>
  <behavior>
    - PrimitiveCall = { name: DrawToolName (excluding 'finish'); input: Record&lt;string, unknown&gt; }
    - SceneGenerator = (args: unknown) =&gt; Generator&lt;PrimitiveCall&gt;
    - SceneName = union of all 15 explain_* strings
    - sceneRegistry is a Record&lt;SceneName, SceneGenerator&gt; — populated lazily as scenes are imported (Wave 1 adds 8, Wave 2 adds 7; registry must accept partial population without TS errors)
    - isSceneName(name: string): name is SceneName — checks against the set of registered keys
    - tools.test.ts: existing 13 tests stay green; add test asserting allBoardTools has 25 entries (9 + 15 + finish = 25); add test asserting every SceneName appears in allBoardTools
  </behavior>
  <action>
**lib/board/scenes/types.ts** — create:
```typescript
import type { DrawToolName } from '@/lib/board/tools'

// A single primitive call emitted by a scene generator.
// 'finish' is excluded — scenes do not call finish; the agent loop does.
export type PrimitiveName = Exclude<DrawToolName, 'finish'>

export interface PrimitiveCall {
  name: PrimitiveName
  input: Record<string, unknown>
}

// All 15 scene names (D-01)
export type SceneName =
  | 'explain_column_addition'
  | 'explain_column_subtraction'
  | 'explain_multiplication_grid'
  | 'explain_long_division'
  | 'explain_fraction_addition'
  | 'explain_fraction_subtraction'
  | 'explain_fraction_comparison'
  | 'explain_fraction_simplification'
  | 'explain_decimal_addition'
  | 'explain_decimal_multiplication'
  | 'explain_percent_calculation'
  | 'explain_rectangle_area'
  | 'explain_rectangle_perimeter'
  | 'explain_simple_equation'
  | 'explain_arithmetic_mean'

// Each scene is a generator function that yields PrimitiveCalls.
// args is typed as unknown — each scene file does its own runtime validation.
export type SceneGenerator = (args: unknown) => Generator<PrimitiveCall>
```

**lib/board/scenes/index.ts** — create:
```typescript
import type { SceneName, SceneGenerator } from './types'

// Registry is populated by each scene file calling registerScene().
// This avoids circular imports: types.ts has no scene imports; index.ts has no scene imports at module level.
const _registry: Partial<Record<SceneName, SceneGenerator>> = {}

export function registerScene(name: SceneName, fn: SceneGenerator): void {
  _registry[name] = fn
}

export function isSceneName(name: string): name is SceneName {
  return name in _registry
}

export function getScene(name: SceneName): SceneGenerator | undefined {
  return _registry[name]
}

// Exported for testing — returns array of registered scene names
export function registeredScenes(): SceneName[] {
  return Object.keys(_registry) as SceneName[]
}
```

**lib/board/tools.ts** — extend:
- Change `DrawToolName` to include all 15 scene names as a union (or keep DrawToolName for primitives and add `SceneToolName` union; use the union in allBoardTools). Recommended: keep `DrawToolName` for the 9 primitives intact (executor.ts depends on it), add `SceneToolName = SceneName` from scenes/types, add `BoardToolName = DrawToolName | SceneToolName`.
- Add `SceneToolSchema` interface (same shape as DrawToolSchema but name: SceneToolName).
- Add `sceneTools: SceneToolSchema[]` array with all 15 schemas. Descriptions in Russian. Parameters validated with JSON Schema (no zod — we keep the existing pattern of manual JSONSchemaProperty objects).
- Add `allBoardTools: (DrawToolSchema | SceneToolSchema)[]` = [...drawTools, ...sceneTools].

Scene schemas to add (all 15 — per D-05 so route.ts compiles with them from Wave 1):

```typescript
// explain_column_addition
{
  name: 'explain_column_addition',
  description: 'Объясни сложение двух натуральных чисел в столбик. Используй для тем «сложение многозначных чисел», «сложение в столбик». Подходит для чисел 2–5 знаков. Показывает запись чисел, черту, суммирование по разрядам с переносом, итоговый ответ.',
  parameters: {
    type: 'object',
    properties: {
      a: { type: 'number', description: 'Первое слагаемое (натуральное число)' },
      b: { type: 'number', description: 'Второе слагаемое (натуральное число)' },
    },
    required: ['a', 'b'],
    additionalProperties: false,
  }
}

// explain_column_subtraction
{
  name: 'explain_column_subtraction',
  description: 'Объясни вычитание в столбик (a − b). Используй для тем «вычитание многозначных чисел», «вычитание в столбик». Показывает запись чисел, черту, вычитание по разрядам с заимствованием, итог.',
  parameters: {
    type: 'object',
    properties: {
      a: { type: 'number', description: 'Уменьшаемое' },
      b: { type: 'number', description: 'Вычитаемое (b ≤ a)' },
    },
    required: ['a', 'b'],
    additionalProperties: false,
  }
}

// explain_multiplication_grid
{
  name: 'explain_multiplication_grid',
  description: 'Объясни умножение в столбик (a × b). Используй для тем «умножение многозначных чисел», «умножение в столбик». Показывает запись, частичные произведения по каждой цифре множителя, их сдвиг и суммирование.',
  parameters: {
    type: 'object',
    properties: {
      a: { type: 'number', description: 'Множитель 1 (до 4 знаков)' },
      b: { type: 'number', description: 'Множитель 2 (до 3 знаков)' },
    },
    required: ['a', 'b'],
    additionalProperties: false,
  }
}

// explain_long_division
{
  name: 'explain_long_division',
  description: 'Объясни деление в столбик (dividend ÷ divisor). Используй для тем «деление в столбик», «деление с остатком». Показывает уголок, пошаговое деление, частное, остаток.',
  parameters: {
    type: 'object',
    properties: {
      dividend: { type: 'number', description: 'Делимое' },
      divisor: { type: 'number', description: 'Делитель (≠ 0)' },
    },
    required: ['dividend', 'divisor'],
    additionalProperties: false,
  }
}

// explain_fraction_addition
{
  name: 'explain_fraction_addition',
  description: 'Объясни сложение дробей a/b + c/d с приведением к общему знаменателю. Используй для тем «сложение дробей с разными знаменателями».',
  parameters: {
    type: 'object',
    properties: {
      a: { type: 'number', description: 'Числитель первой дроби' },
      b: { type: 'number', description: 'Знаменатель первой дроби (≠ 0)' },
      c: { type: 'number', description: 'Числитель второй дроби' },
      d: { type: 'number', description: 'Знаменатель второй дроби (≠ 0)' },
    },
    required: ['a', 'b', 'c', 'd'],
    additionalProperties: false,
  }
}

// explain_fraction_subtraction
{
  name: 'explain_fraction_subtraction',
  description: 'Объясни вычитание дробей a/b − c/d с приведением к общему знаменателю. Используй для тем «вычитание дробей».',
  parameters: {
    type: 'object',
    properties: {
      a: { type: 'number', description: 'Числитель уменьшаемой дроби' },
      b: { type: 'number', description: 'Знаменатель уменьшаемой дроби (≠ 0)' },
      c: { type: 'number', description: 'Числитель вычитаемой дроби' },
      d: { type: 'number', description: 'Знаменатель вычитаемой дроби (≠ 0)' },
    },
    required: ['a', 'b', 'c', 'd'],
    additionalProperties: false,
  }
}

// explain_fraction_comparison
{
  name: 'explain_fraction_comparison',
  description: 'Объясни сравнение дробей a/b и c/d (через приведение к общему знаменателю). Используй для тем «сравнение дробей».',
  parameters: {
    type: 'object',
    properties: {
      a: { type: 'number', description: 'Числитель первой дроби' },
      b: { type: 'number', description: 'Знаменатель первой дроби (≠ 0)' },
      c: { type: 'number', description: 'Числитель второй дроби' },
      d: { type: 'number', description: 'Знаменатель второй дроби (≠ 0)' },
    },
    required: ['a', 'b', 'c', 'd'],
    additionalProperties: false,
  }
}

// explain_fraction_simplification
{
  name: 'explain_fraction_simplification',
  description: 'Объясни сокращение дроби numerator/denominator через нахождение НОД. Используй для тем «сокращение дробей», «несократимые дроби».',
  parameters: {
    type: 'object',
    properties: {
      numerator: { type: 'number', description: 'Числитель дроби' },
      denominator: { type: 'number', description: 'Знаменатель дроби (≠ 0)' },
    },
    required: ['numerator', 'denominator'],
    additionalProperties: false,
  }
}

// explain_decimal_addition
{
  name: 'explain_decimal_addition',
  description: 'Объясни сложение десятичных дробей (a + b) с выравниванием запятых. Используй для тем «сложение десятичных дробей».',
  parameters: {
    type: 'object',
    properties: {
      a: { type: 'number', description: 'Первое слагаемое (десятичное)' },
      b: { type: 'number', description: 'Второе слагаемое (десятичное)' },
    },
    required: ['a', 'b'],
    additionalProperties: false,
  }
}

// explain_decimal_multiplication
{
  name: 'explain_decimal_multiplication',
  description: 'Объясни умножение десятичных дробей (a × b): умножение как целые числа, затем постановка запятой по сумме десятичных разрядов.',
  parameters: {
    type: 'object',
    properties: {
      a: { type: 'number', description: 'Первый множитель (десятичный)' },
      b: { type: 'number', description: 'Второй множитель (десятичный)' },
    },
    required: ['a', 'b'],
    additionalProperties: false,
  }
}

// explain_percent_calculation
{
  name: 'explain_percent_calculation',
  description: 'Объясни нахождение percent% от числа value. Например: «Найди 15% от 200». Показывает формулу: value × percent / 100.',
  parameters: {
    type: 'object',
    properties: {
      value: { type: 'number', description: 'Число, от которого ищем процент' },
      percent: { type: 'number', description: 'Процент (например, 15 для 15%)' },
    },
    required: ['value', 'percent'],
    additionalProperties: false,
  }
}

// explain_rectangle_area
{
  name: 'explain_rectangle_area',
  description: 'Объясни нахождение площади прямоугольника S = width × height. Рисует прямоугольник, подписывает стороны, формулу и результат.',
  parameters: {
    type: 'object',
    properties: {
      width: { type: 'number', description: 'Ширина прямоугольника' },
      height: { type: 'number', description: 'Высота прямоугольника' },
    },
    required: ['width', 'height'],
    additionalProperties: false,
  }
}

// explain_rectangle_perimeter
{
  name: 'explain_rectangle_perimeter',
  description: 'Объясни нахождение периметра прямоугольника P = 2 × (width + height). Рисует прямоугольник, подписывает стороны, формулу, результат.',
  parameters: {
    type: 'object',
    properties: {
      width: { type: 'number', description: 'Ширина прямоугольника' },
      height: { type: 'number', description: 'Высота прямоугольника' },
    },
    required: ['width', 'height'],
    additionalProperties: false,
  }
}

// explain_simple_equation
{
  name: 'explain_simple_equation',
  description: 'Объясни решение простого уравнения вида «coefficient × x = value» или «x + coefficient = value». Показывает уравнение, шаги нахождения x, проверку.',
  parameters: {
    type: 'object',
    properties: {
      coefficient: { type: 'number', description: 'Коэффициент (≠ 0)' },
      value: { type: 'number', description: 'Правая часть уравнения' },
      type: { type: 'string', description: '«multiply» для c·x=v, «add» для x+c=v' },
    },
    required: ['coefficient', 'value', 'type'],
    additionalProperties: false,
  }
}

// explain_arithmetic_mean
{
  name: 'explain_arithmetic_mean',
  description: 'Объясни нахождение среднего арифметического набора чисел: сумма всех / количество. Записывает числа, подсчёт суммы, деление, результат.',
  parameters: {
    type: 'object',
    properties: {
      numbers: { type: 'array', description: 'Массив чисел (от 2 до 8 элементов)' },
    },
    required: ['numbers'],
    additionalProperties: false,
  }
}
```

**lib/board/index.ts** — add scene exports:
```typescript
export type { SceneName, PrimitiveCall, PrimitiveName, SceneGenerator } from './scenes/types'
export { isSceneName, getScene, registerScene, registeredScenes } from './scenes/index'
export type { SceneToolSchema, BoardToolName } from './tools'
export { sceneTools, allBoardTools } from './tools'
```

**lib/board/__tests__/tools.test.ts** — add to existing 13 tests:
- `allBoardTools` has 25 entries (9 primitives + 15 scenes + finish = 25)
- Every SceneName from the union appears in `allBoardTools.map(t => t.name)`
- Each scene schema has non-empty description (Russian text)
- Each scene schema has at least 1 required parameter
  </action>
  <verify>
    <automated>npx vitest run lib/board/__tests__/tools.test.ts --reporter=verbose 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - lib/board/scenes/types.ts exports PrimitiveCall, SceneName, SceneGenerator, PrimitiveName
    - lib/board/scenes/index.ts exports registerScene, isSceneName, getScene, registeredScenes
    - lib/board/tools.ts exports allBoardTools with 25 entries; sceneTools with 15 entries
    - lib/board/index.ts re-exports all scene types and allBoardTools
    - All 15 scene schemas are present in allBoardTools; all have Russian descriptions
    - tools.test.ts assertions on allBoardTools count pass; TypeScript strict mode clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 8 Wave-1 scenes with unit tests</name>
  <files>
    lib/board/scenes/explain-column-addition.ts
    lib/board/scenes/explain-column-subtraction.ts
    lib/board/scenes/explain-fraction-addition.ts
    lib/board/scenes/explain-fraction-subtraction.ts
    lib/board/scenes/explain-fraction-comparison.ts
    lib/board/scenes/explain-fraction-simplification.ts
    lib/board/scenes/explain-decimal-addition.ts
    lib/board/scenes/explain-percent-calculation.ts
    lib/board/scenes/__tests__/explain-column-addition.test.ts
    lib/board/scenes/__tests__/explain-column-subtraction.test.ts
    lib/board/scenes/__tests__/explain-fraction-addition.test.ts
    lib/board/scenes/__tests__/explain-fraction-subtraction.test.ts
    lib/board/scenes/__tests__/explain-fraction-comparison.test.ts
    lib/board/scenes/__tests__/explain-fraction-simplification.test.ts
    lib/board/scenes/__tests__/explain-decimal-addition.test.ts
    lib/board/scenes/__tests__/explain-percent-calculation.test.ts
  </files>
  <behavior>
    For each scene, tests assert:
    1. Generator yields ≥ 10 PrimitiveCalls for normal input
    2. First primitive is 'say' (scene opens with a spoken introduction)
    3. At least one 'wait' primitive exists
    4. 'wait' with ms > 0 separates at least one pair of non-wait primitives (not all draws are bunched at end)
    5. All coordinate values: x ≥ 0, x ≤ 800, y ≥ 0, y ≤ 600
    6. Key values appear in text primitives (e.g., "245" and "874" and "1119" in column_addition(245,874))
    7. Invalid args (zero denominator, negative, NaN) throw TypeError with Russian message

    Specific example for explain_column_addition:
    - explainColumnAddition({a:245,b:874}) → collect all yields
    - primitives.length ≥ 15
    - primitives[0].name === 'say'
    - primitives.some(p => p.name === 'wait')
    - primitives.filter(p=>p.name==='draw_text').some(p=>(p.input.text as string).includes('1119'))
    - no x > 800, no y > 600, no negative coordinates

    Specific example for explain_fraction_simplification:
    - explainFractionSimplification({numerator:6,denominator:8}) → yields include text 'НОД' or 'GCD', text '3', text '4' (simplified form), text '2' (gcd)
    - explainFractionSimplification({numerator:1,denominator:0}) throws TypeError

    Specific example for explain_percent_calculation:
    - explainPercentCalculation({value:200,percent:15}) → yields include text '30' (result), text '15%' or '15', text '200'

    Specific example for explain_decimal_addition:
    - explainDecimalAddition({a:3.5,b:2.75}) → yields include text '6.25' or '6,25', text '3,5' or '3.5'
  </behavior>
  <action>
Each scene file follows the same pattern. Generator function is the default export AND calls registerScene() as a side effect on import. All text strings in Russian (labels, say phrases). Coordinates on virtual 800×600 canvas.

**Pattern for each scene file:**
```typescript
// lib/board/scenes/explain-<name>.ts
import { registerScene } from './index'
import type { PrimitiveCall } from './types'

export function* explainColumnAddition(args: unknown): Generator<PrimitiveCall> {
  // 1. Validate args — throw TypeError with Russian message on bad input
  const { a, b } = args as { a: number; b: number }
  if (!Number.isInteger(a) || !Number.isInteger(b) || a <= 0 || b <= 0) {
    throw new TypeError('explain_column_addition: a и b должны быть натуральными числами')
  }

  const sum = a + b
  const strA = String(a)
  const strB = String(b)
  const strSum = String(sum)

  // Canvas layout: column centered around x=400, numbers top at y=120
  const COL_X = 320  // right-edge of rightmost digit column
  const DIGIT_W = 32 // width per digit slot (fontSize 32)
  const ROW_Y1 = 120 // y of first number (a)
  const ROW_Y2 = 170 // y of second number (b)
  const LINE_Y = 215 // y of horizontal line
  const RES_Y = 240  // y of result row

  // Step 1: introduction
  yield { name: 'say', input: { text: `Складываем ${a} и ${b} в столбик.` } }

  // Step 2: write first number right-aligned
  yield { name: 'draw_text', input: { x: COL_X - strA.length * DIGIT_W, y: ROW_Y1, text: strA, fontSize: 32 } }
  yield { name: 'wait', input: { ms: 800 } }

  // Step 3: write + sign and second number
  yield { name: 'draw_text', input: { x: COL_X - strB.length * DIGIT_W - 40, y: ROW_Y2, text: '+', fontSize: 32, color: 'blue' } }
  yield { name: 'draw_text', input: { x: COL_X - strB.length * DIGIT_W, y: ROW_Y2, text: strB, fontSize: 32 } }
  yield { name: 'wait', input: { ms: 600 } }

  // Step 4: horizontal line
  yield { name: 'say', input: { text: 'Подводим черту.' } }
  yield { name: 'draw_line', input: { x1: COL_X - (Math.max(strA.length, strB.length) + 1) * DIGIT_W, y1: LINE_Y, x2: COL_X + 10, y2: LINE_Y, stroke: 'black', strokeWidth: 2 } }
  yield { name: 'wait', input: { ms: 600 } }

  // Step 5: add digit by digit from right, tracking carry
  // Pad strings to same length
  const maxLen = Math.max(strA.length, strB.length)
  const paddedA = strA.padStart(maxLen, '0')
  const paddedB = strB.padStart(maxLen, '0')
  let carry = 0
  const resultDigits: string[] = []

  for (let i = maxLen - 1; i >= 0; i--) {
    const digitA = Number(paddedA[i])
    const digitB = Number(paddedB[i])
    const colSum = digitA + digitB + carry
    const digit = colSum % 10
    carry = Math.floor(colSum / 10)
    resultDigits.unshift(String(digit))

    const colOffset = (maxLen - 1 - i) * DIGIT_W
    const highlightX = COL_X - (maxLen - 1 - i) * DIGIT_W - DIGIT_W

    yield { name: 'say', input: { text: `В разряде ${i === maxLen - 1 ? 'единиц' : i === maxLen - 2 ? 'десятков' : 'сотен'}: ${digitA} + ${digitB}${carry > 0 && i < maxLen - 1 ? ' + 1 (перенос)' : ''} = ${colSum}.` } }
    yield { name: 'highlight_region', input: { x: highlightX, y: ROW_Y1, w: DIGIT_W, h: RES_Y - ROW_Y1, color: '#fff59d', duration_ms: 2000 } }
    yield { name: 'wait', input: { ms: 500 } }

    if (carry > 0 && i > 0) {
      // draw carry digit above the next column
      yield { name: 'draw_text', input: { x: highlightX - DIGIT_W, y: ROW_Y1 - 30, text: '1', fontSize: 20, color: 'red' } }
    }

    yield { name: 'draw_text', input: { x: COL_X - (maxLen - 1 - i) * DIGIT_W - 16, y: RES_Y, text: String(digit), fontSize: 32 } }
    yield { name: 'wait', input: { ms: 700 } }
  }

  // If final carry, emit extra result digit
  if (carry > 0) {
    yield { name: 'say', input: { text: 'Переносим единицу в следующий разряд.' } }
    yield { name: 'draw_text', input: { x: COL_X - maxLen * DIGIT_W - 16, y: RES_Y, text: String(carry), fontSize: 32 } }
    yield { name: 'wait', input: { ms: 700 } }
  }

  // Step 6: highlight final answer
  yield { name: 'say', input: { text: `Получили ${strSum}. Готово.` } }
  yield { name: 'highlight_region', input: { x: COL_X - strSum.length * DIGIT_W - 10, y: RES_Y - 5, w: strSum.length * DIGIT_W + 20, h: 45, color: '#a5f3fc', duration_ms: 4000 } }
}

registerScene('explain_column_addition', explainColumnAddition as (args: unknown) => Generator<PrimitiveCall>)
```

Implement all 8 scene files with the same structure. Key layout decisions:

**explain_column_subtraction(a, b):**
- Layout: same column layout as addition. Show borrowing with small '-1' carry in red above borrowed column. Result at RES_Y. Note: if b > a, throw TypeError 'explain_column_subtraction: вычитаемое не может быть больше уменьшаемого'.

**explain_fraction_addition(a, b, c, d):**
- Validate: b ≠ 0, d ≠ 0 — throw TypeError if zero denominator.
- Compute LCM(b, d). Show: 'a/b + c/d'. Show transformation to common denominator. Show result fraction. Simplify if possible (show НОД step if gcd > 1). All on canvas left-to-right at y=150–400. Use draw_text for fraction numerator, draw_line for fraction bar, draw_text for denominator. Coordinates: numerator at y=180, bar at y=220 (draw_line horizontal), denominator at y=240.

**explain_fraction_subtraction(a, b, c, d):**
- Same as fraction_addition but subtraction. Show 'a/b − c/d'.

**explain_fraction_comparison(a, b, c, d):**
- Validate: b ≠ 0, d ≠ 0.
- Show both fractions. Bring to common denominator. Compare numerators. Yield say with conclusion '>' or '<' or '='. Highlight larger fraction.

**explain_fraction_simplification(numerator, denominator):**
- Validate: denominator ≠ 0.
- Compute GCD. Show: original fraction, 'НОД(num, den) = gcd', 'num÷gcd=n2', 'den÷gcd=d2', simplified fraction. If gcd === 1, say 'Дробь уже несократима'.

**explain_decimal_addition(a, b):**
- Show numbers right-aligned with decimal points vertically aligned (key visual). Draw vertical line at comma column. Add as integers then place comma. Result shown below line.

**explain_percent_calculation(value, percent):**
- Show formula: 'value × percent / 100'. Compute result. Show step: 'value × percent = product', 'product / 100 = result'. Highlight result.

**DO NOT add stub implementations that yield fewer than 10 primitives.** Every scene must have complete pedagogical flow with say + draw + wait structure. Use helper functions (gcd, lcm) defined at top of each file.

Write tests FIRST (TDD), make them fail, then implement. Test file structure:
```typescript
// lib/board/scenes/__tests__/explain-column-addition.test.ts
import { describe, it, expect } from 'vitest'
// Import side-effect registers the scene; import the function for direct testing
import '../explain-column-addition'
import { explainColumnAddition } from '../explain-column-addition'

describe('explainColumnAddition', () => {
  it('yields ≥ 15 primitives for 245 + 874', () => {
    const primitives = [...explainColumnAddition({ a: 245, b: 874 })]
    expect(primitives.length).toBeGreaterThanOrEqual(15)
  })
  it('first primitive is say', () => {
    const primitives = [...explainColumnAddition({ a: 245, b: 874 })]
    expect(primitives[0].name).toBe('say')
  })
  it('includes at least one wait', () => {
    const primitives = [...explainColumnAddition({ a: 245, b: 874 })]
    expect(primitives.some(p => p.name === 'wait')).toBe(true)
  })
  it('result text "1119" appears in draw_text primitives', () => {
    const primitives = [...explainColumnAddition({ a: 245, b: 874 })]
    const texts = primitives.filter(p => p.name === 'draw_text').map(p => String(p.input.text))
    expect(texts.join(' ')).toContain('1119')
  })
  it('all coordinates within 800×600 canvas', () => {
    const primitives = [...explainColumnAddition({ a: 245, b: 874 })]
    for (const p of primitives) {
      if ('x' in p.input) expect(p.input.x as number).toBeGreaterThanOrEqual(0)
      if ('x' in p.input) expect(p.input.x as number).toBeLessThanOrEqual(800)
      if ('y' in p.input) expect(p.input.y as number).toBeGreaterThanOrEqual(0)
      if ('y' in p.input) expect(p.input.y as number).toBeLessThanOrEqual(600)
      if ('x1' in p.input) expect(p.input.x1 as number).toBeLessThanOrEqual(800)
      if ('x2' in p.input) expect(p.input.x2 as number).toBeLessThanOrEqual(800)
    }
  })
  it('throws TypeError for invalid args', () => {
    expect(() => [...explainColumnAddition({ a: 0, b: 5 })]).toThrow(TypeError)
    expect(() => [...explainColumnAddition({ a: -1, b: 5 })]).toThrow(TypeError)
  })
})
```

Write similarly structured tests for each of the 7 other scenes, adapting the specific assertions to that scene's expected output.
  </action>
  <verify>
    <automated>npx vitest run lib/board/scenes/__tests__/ --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - 8 scene files exist in lib/board/scenes/
    - 8 test files exist in lib/board/scenes/__tests__/
    - All tests pass: ≥ 4 tests per scene (≥ 32 total new tests)
    - Each scene exports a named generator function AND calls registerScene()
    - No scene yields coordinates outside 800×600
    - Each scene starts with 'say' primitive
    - Invalid args throw TypeError with Russian error message
    - TypeScript strict mode: tsc --noEmit exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route.ts scene interception + system prompt update + integration test</name>
  <files>
    app/api/draw/route.ts
    app/api/draw/__tests__/route.test.ts
  </files>
  <behavior>
    Integration test (mock OpenAI returning explain_column_addition call):
    - Mock OpenAI stream emits tool_calls.function.arguments.done with name='explain_column_addition' and arguments='{"a":245,"b":874}'
    - Import all 8 scene files (side-effect) before route runs so registry is populated
    - Assert SSE stream contains ≥ 1 event with type='tool_use' and name NOT 'explain_column_addition' (scene expanded to primitives)
    - Assert final done event has scene_used = 'explain_column_addition'
    - Assert no SSE event with name='explain_column_addition' (scene name never forwarded to client)

    Scene interception logic:
    - When e.name starts with 'explain_' (isSceneName check), do NOT call send() with that tool_use
    - Instead: parse args, call getScene(name)(args), iterate generator, call send() for each yielded primitive
    - Record scene_used = e.name
    - Carry on agent loop normally (fake 'ok' tool result still returned to OpenAI)

    System prompt update:
    - Append new section to SYSTEM_PROMPT (after existing sections): scene encouragement per D-06
    - Text approximately: «═══ СЦЕНЫ (методические шаблоны) ═══\nДля тем программы 5 класса предпочитай вызывать сцены explain_* — они автоматически разворачиваются в правильный пошаговый разбор с нужными координатами и паузами. Используй примитивы только если: (а) запрошенная тема не покрыта сценой, (б) ребёнок просит специфическую модификацию что не покрывается сценой.\nДоступные сцены: explain_column_addition, explain_column_subtraction, explain_multiplication_grid, explain_long_division, explain_fraction_addition, explain_fraction_subtraction, explain_fraction_comparison, explain_fraction_simplification, explain_decimal_addition, explain_decimal_multiplication, explain_percent_calculation, explain_rectangle_area, explain_rectangle_perimeter, explain_simple_equation, explain_arithmetic_mean.»
  </behavior>
  <action>
**app/api/draw/route.ts** — modify in two places:

1. **Imports** — add at top (after existing imports):
```typescript
// Import scenes to populate registry — side-effect imports
// Wave 1 scenes (Plans 05-01); Wave 2 scenes imported in 05-02
import '@/lib/board/scenes/explain-column-addition'
import '@/lib/board/scenes/explain-column-subtraction'
import '@/lib/board/scenes/explain-fraction-addition'
import '@/lib/board/scenes/explain-fraction-subtraction'
import '@/lib/board/scenes/explain-fraction-comparison'
import '@/lib/board/scenes/explain-fraction-simplification'
import '@/lib/board/scenes/explain-decimal-addition'
import '@/lib/board/scenes/explain-percent-calculation'
import { isSceneName, getScene } from '@/lib/board'
```

2. **openaiTools** — change source from `drawTools` to `allBoardTools`:
```typescript
import { allBoardTools } from '@/lib/board'
// ...
const openaiTools: ChatCompletionTool[] = allBoardTools.map((t) => ({
  type: 'function',
  function: {
    name: t.name,
    description: t.description,
    parameters: t.parameters as unknown as Record<string, unknown>,
  },
}))
```

3. **SYSTEM_PROMPT** — append the scene encouragement section (see behavior above).

4. **Agent loop** — change the tool_calls handler to intercept scenes:
```typescript
// Add before the loop:
let sceneUsed: string | null = null

// In tool_calls.function.arguments.done handler, replace the body with:
completion.on('tool_calls.function.arguments.done', (e) => {
  if (e.name === 'finish') {
    finishedExplicitly = true
    return
  }

  let input: unknown = e.parsed_arguments
  if (input == null) {
    try { input = JSON.parse(e.arguments) } catch { input = {} }
  }

  // Scene interception (D-02): expand scene server-side, emit primitives individually
  if (isSceneName(e.name)) {
    sceneUsed = e.name
    const sceneFn = getScene(e.name)
    if (sceneFn) {
      try {
        for (const primitive of sceneFn(input)) {
          totalToolCalls++
          send({ type: 'tool_use', id: `t${turn}_${e.index}_s${totalToolCalls}`, name: primitive.name, input: primitive.input })
        }
      } catch (sceneErr) {
        const msg = sceneErr instanceof Error ? sceneErr.message : String(sceneErr)
        console.error(`[draw] Scene ${e.name} error:`, msg)
        // Fall through — OpenAI still gets 'ok' back, can retry with different args
      }
    }
    return  // scene call consumed; do NOT forward raw explain_* to client
  }

  // Primitive pass-through (unchanged)
  totalToolCalls++
  send({ type: 'tool_use', id: `t${turn}_${e.index}`, name: e.name, input })
})
```

5. **Done event** — add scene_used field (D-17):
```typescript
send({
  type: 'done',
  finish_reason: finishedExplicitly ? 'finished' : lastFinishReason === 'tool_calls' ? 'max_turns' : lastFinishReason,
  scene_used: sceneUsed,   // ADD THIS
  usage: {
    prompt_tokens: totalPromptTokens,
    completion_tokens: totalCompletionTokens,
    total_tokens: totalPromptTokens + totalCompletionTokens,
  },
})
```

**app/api/draw/__tests__/route.test.ts** — add integration test for scene expansion (keep all 8 existing tests passing).

The existing OpenAI mock uses `function MockOpenAI()` constructor pattern. Add a new `describe('scene expansion', ...)` block:
- Mock OpenAI to emit a single `explain_column_addition` tool call (name + arguments JSON)
- Since side-effect imports in route.ts populate the registry, the test can rely on the registry being populated when route.ts is imported
- BUT: vitest isolates modules. Use `vi.importActual` or ensure the real scene module is imported in the test setup. Simplest: add a `beforeAll` that does `await import('@/lib/board/scenes/explain-column-addition')`.
- Assert that the SSE response body contains at least one `tool_use` event with name `draw_text` or `say` (scene primitives)
- Assert that no SSE event has `name: 'explain_column_addition'`
- Assert that the final `done` event has `scene_used: 'explain_column_addition'`

Use the existing SSE parsing helper pattern from route.test.ts (ReadableStream reader, split by `data: `, JSON.parse each line).
  </action>
  <verify>
    <automated>npx vitest run app/api/draw/__tests__/route.test.ts --reporter=verbose 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - route.ts imports 8 scene files as side effects at module top
    - route.ts uses allBoardTools (25 tools) for openaiTools, not just drawTools (9 tools)
    - isSceneName(e.name) check in agent loop routes explain_* calls to scene expansion
    - Scene-expanded primitives are sent as individual tool_use SSE events
    - Raw explain_* name is never sent to client
    - sceneUsed is set when a scene is called; null when only primitives used
    - Done SSE event has scene_used field (string | null)
    - SYSTEM_PROMPT has scene encouragement section in Russian
    - All existing 8 route tests still pass
    - New integration test for scene expansion passes
    - npm run build passes (tsc strict clean)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → route.ts | Scene args come from LLM output — untrusted, may be out-of-range or wrong type |
| scene generator → SSE client | Scene primitives go directly to client canvas — coordinates must be canvas-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Scene args from LLM | mitigate | Each scene validates args at runtime, throws TypeError on invalid input; route catches error and logs, continues loop with 'ok' reply |
| T-05-01-02 | DoS | Scene generator infinite loop | mitigate | Generator pattern is finite (digit-by-digit loops bounded by string length ≤ 10); route has MAX_AGENT_TURNS=30 outer guard |
| T-05-01-03 | Spoofing | Scene name not in registry | accept | isSceneName guards against unknown names; unregistered name falls through to primitive handler; worst case: unknown primitive name sent to client — client executor ignores unknown names gracefully |
| T-05-01-04 | Information Disclosure | scene_used field in done event | accept | scene_used is the name of the explain_* function (not sensitive); reveals which topic the LLM chose — acceptable for cost monitoring |
</threat_model>

<verification>
After completing all 3 tasks:

```bash
# All tests green
npx vitest run --reporter=verbose 2>&1 | tail -5

# TypeScript clean
npx tsc --noEmit 2>&1 | head -20

# Build succeeds
npm run build 2>&1 | tail -10
```

Expected: all existing tests (≥117) + new scene tests (≥40) + new route integration tests pass. Build exits 0.
</verification>

<success_criteria>
- allBoardTools in lib/board/tools.ts has 25 entries (9 primitives + 15 scenes + finish)
- 8 scene files exist and each registers itself with sceneRegistry via registerScene()
- Each scene: ≥ 10 primitive yields for normal input, starts with 'say', has wait primitives, no off-canvas coords
- route.ts sends 25 tools to OpenAI (LLM sees all 15 explain_* + 9 primitives + finish)
- route.ts intercepts explain_* calls and emits expanded primitives only — client never sees raw scene names
- done SSE event has scene_used: string | null field
- System prompt has Russian scene encouragement section
- All vitest tests pass; npm run build passes
- Scenes covered in this plan: explain_column_addition, explain_column_subtraction, explain_fraction_addition, explain_fraction_subtraction, explain_fraction_comparison, explain_fraction_simplification, explain_decimal_addition, explain_percent_calculation
</success_criteria>

<output>
After completion, create `.planning/phases/05-scenes/05-01-SUMMARY.md` following the template at `@C:/Users/krato/.claude/get-shit-done/templates/summary.md`.
</output>
