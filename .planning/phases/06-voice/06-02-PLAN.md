---
phase: 06-voice
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - components/panels/voice-panel.tsx
  - components/lesson-shell.tsx
  - components/panels/__tests__/voice-panel.test.tsx
  - e2e/voice-flow.spec.ts
autonomous: true
requirements: [VOI-01]

must_haves:
  truths:
    - "User opens lesson page, sees a 'Запустить голос' button in the VoicePanel (bottom half, below Avatar)."
    - "Clicking the button calls navigator.mediaDevices.getUserMedia({audio:true}) BEFORE any fetch to /api/voice/signed-url (mic permission first per RESEARCH Pitfall 1)."
    - "After mic granted, the panel POSTs to /api/voice/signed-url with body {lessonId} and receives {signedUrl, topic}."
    - "On fetch success, the panel calls useConversation().startSession with {signedUrl, connectionType:'websocket', overrides:{agent:{firstMessage: Russian greeting containing the topic}}}."
    - "SDK callback onModeChange({mode:'listening'}) emits bus event ('voice:state', {state:'listening'}); Phase 9 Avatar transitions to 👂 (data-avatar-state=listening)."
    - "SDK callback onModeChange({mode:'speaking'}) emits bus event ('voice:state', {state:'speaking'}); Avatar transitions to 🗣️."
    - "SDK callback onDisconnect emits bus event ('voice:state', {state:'idle'}); Avatar returns to 🙂."
    - "SDK callback onError displays a Russian error message in a destructive-styled block AND emits ('voice:state', {state:'idle'}) as a fail-safe."
    - "Mic permission denial (NotAllowedError / NotFoundError / NotReadableError / generic) shows one of 4 distinct Russian messages — never a generic blob."
    - "Component unmount calls conversation.endSession() so the mic indicator does not stay red after navigating away (Pitfall 7)."
    - "Clicking 'Stop' (visible only while status === 'connected') calls endSession and the UI returns to the initial Start state."
    - "On reload of the lesson page, the user can click 'Запустить голос' again — no stale state from prior session."
    - "VoicePanel test file mocks @elevenlabs/react such that capturedOptions exposes onConnect / onModeChange / onDisconnect / onError, AND firing each one synchronously results in the corresponding bus.emit call."
    - "E2E voice-flow.spec.ts stubs /api/voice/signed-url at network layer (page.route) and drives avatar state via window.__lessonBus.emit — NEVER hits real 11labs."
    - "Production bundle does NOT contain literal substrings 'ELEVENLABS_API_KEY', 'ELEVENLABS_AGENT_ID', or any 'sk_' prefix (VOI-01-T / D-09 step 10 / T-06-02-01)."
    - "Allowlist + signed URL conflict resolved per manual smoke procedure (see verification section): either both confirmed compatible OR allowlist removed per MANUAL-ACTIONS.md note."
  artifacts:
    - path: "components/panels/voice-panel.tsx"
      provides: "Rewritten VoicePanel: top half Avatar (Phase 9 unchanged) + bottom half real Start/Stop button + status text + error block. Uses useConversation hook + getUserMedia + fetch + bus emits."
      min_lines: 120
      contains: "useConversation"
    - path: "components/lesson-shell.tsx"
      provides: "Thread the existing `topic` prop down into VoicePanel (LessonShell already has it on line 37)."
      contains: "topic={topic}"
    - path: "components/panels/__tests__/voice-panel.test.tsx"
      provides: "Vitest component coverage for VOI-01-I through VOI-01-R (12 tests minimum)."
      min_lines: 200
    - path: "e2e/voice-flow.spec.ts"
      provides: "Playwright spec covering VOI-01-S + VOI-01-T (mic button, bus-driven avatar transitions, bundle leak check)."
      min_lines: 150
  key_links:
    - from: "components/panels/voice-panel.tsx"
      to: "@elevenlabs/react → useConversation hook"
      via: "Named import; called once inside component with stable useCallback handlers"
      pattern: "useConversation\\("
    - from: "components/panels/voice-panel.tsx"
      to: "/api/voice/signed-url"
      via: "fetch('/api/voice/signed-url', { method: 'POST', body: JSON.stringify({ lessonId }) })"
      pattern: "fetch\\(['\"]/api/voice/signed-url"
    - from: "components/panels/voice-panel.tsx"
      to: "@/lib/lesson-bus → bus.emit('voice:state', ...)"
      via: "Stable useCallback handlers wired into useConversation options."
      pattern: "bus\\.emit\\(['\"]voice:state"
    - from: "components/panels/voice-panel.tsx"
      to: "conversation.startSession({ signedUrl, connectionType: 'websocket', overrides: { agent: { firstMessage } } })"
      via: "Click handler — after getUserMedia succeeds and fetch returns {signedUrl, topic}."
      pattern: "connectionType: 'websocket'"
    - from: "components/lesson-shell.tsx"
      to: "components/panels/voice-panel.tsx"
      via: "JSX prop <VoicePanel lessonId={lessonId} topic={topic} />"
      pattern: "topic=\\{topic\\}"
    - from: "e2e/voice-flow.spec.ts"
      to: "window.__lessonBus (Phase 9 dev-only exposure)"
      via: "page.evaluate emits voice:state to drive avatar transitions without real SDK."
      pattern: "__lessonBus"
---

<objective>
Phase 6 UI integration + verification — replace the VoicePanel Phase-6 placeholder with the full mic-button + `useConversation` hook + bus-emit flow, thread `topic` from LessonShell, and ship complete test coverage (component + E2E) including a manual smoke note for the Open Q1 allowlist+signedURL question.

Purpose: Deliver D-09 UAT criteria end-to-end. After this plan, a developer on Chrome+VPN can click «Запустить голос» on a real lesson page and have a Russian-speaking Nataly tutor respond in voice. The Avatar (Phase 9) transitions through 🙂 → 👂 → 🗣️ in sync. No client-side leak of ELEVENLABS_* secrets. E2E suite proves bus wiring without burning 11labs daily call budget.

Output:
- `components/panels/voice-panel.tsx` — rewritten panel (top half Avatar untouched, bottom half rewritten from placeholder to real Start/Stop button + mic permission UX + error block + status indicator).
- `components/lesson-shell.tsx` — one-line edit passing existing `topic` prop into `<VoicePanel>`.
- `components/panels/__tests__/voice-panel.test.tsx` — NEW test file (12+ tests covering VOI-01-I through VOI-01-R).
- `e2e/voice-flow.spec.ts` — NEW Playwright spec covering VOI-01-S (bus-driven avatar) + VOI-01-T (no `ELEVENLABS_*` literal in bundle).
- Manual smoke note recorded for Open Q1 (allowlist+signedURL conflict) — captured in SUMMARY.md as part of plan output.
- `npm test && npm run test:e2e -- voice-flow.spec.ts && npm run build` all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PHASE-6-SETUP-2026-05-10.md
@.planning/phases/06-voice/06-CONTEXT.md
@.planning/phases/06-voice/06-RESEARCH.md
@.planning/phases/06-voice/06-PATTERNS.md
@.planning/phases/06-voice/06-01-PLAN.md
@.planning/phases/06-voice/06-01-SUMMARY.md

# Analogs to mirror (read before writing)
@components/panels/voice-panel.tsx
@components/panels/board-panel.tsx
@components/lesson-shell.tsx
@components/avatar/avatar.tsx
@components/avatar/use-avatar-state.ts
@components/avatar/__tests__/avatar.test.tsx
@lib/lesson-bus/events.ts
@lib/lesson-bus/hooks.ts
@lib/trainer/__tests__/use-trainer-idle.test.ts
@e2e/avatar.spec.ts
@e2e/fixtures/db-setup.ts

# From dependency plan 06-01
@app/api/voice/signed-url/route.ts
@lib/elevenlabs/get-signed-url.ts
@lib/elevenlabs/types.ts

<interfaces>
<!-- Contracts the executor needs. Some come from existing code; some from 06-01 (just shipped). -->

From `lib/lesson-bus/events.ts` line 28 (already exists — DO NOT MODIFY per RESEARCH Open Q3):
```typescript
export type VoiceStatePayload = { state: 'idle' | 'listening' | 'speaking' | 'thinking' }
```
The union has FOUR states. `'connected'` is NOT in it. Per RESEARCH Open Q3 + PATTERNS § events.ts (lines 343-362), we DO NOT add `'connected'` — we map `onConnect → 'idle'` (avatar at 🙂 between WS-connect and first onModeChange). CONTEXT.md `<specifics>` lines 168-172 claim we should add `'connected'` — this is OVERRIDDEN by the RESEARCH recommendation. Reason: less surface area, no avatar state-machine cascade, semantically identical UX.

From `lib/lesson-bus/hooks.ts` (existing pattern):
```typescript
export function useLessonBus(): LessonBus  // gives emit/on/off
```

From `components/lesson-shell.tsx` lines 37 + 117 (current state — needs ONE edit):
```typescript
export function LessonShell({ lessonId, topic, trainerConfig }: LessonShellProps) {
  // ...
  <VoicePanel lessonId={lessonId} />  // line 117 — must add `topic={topic}`
```

From `06-01` plan output — `app/api/voice/signed-url/route.ts` returns:
```typescript
{ signedUrl: string, topic: string }  // 200 OK
{ error: 'Войдите в систему' }         // 401
{ error: 'Неверный формат запроса' }   // 400 (bad JSON)
{ error: 'lessonId обязателен' }       // 400 (missing field)
{ error: 'Этот урок не ваш' }          // 403
{ error: 'Voice service не настроен' } // 500
{ error: 'Не удалось подключиться к голосовому сервису' }  // 502
```

From `lib/elevenlabs/types.ts` (just shipped by 06-01):
```typescript
export type ConversationMode = 'speaking' | 'listening'
export type ConversationStatus = 'connected' | 'connecting' | 'disconnected' | 'disconnecting'
export type VoiceErrorKind = 'mic_denied' | 'mic_not_found' | 'mic_busy' | 'signed_url_fetch' | 'sdk'
```

From `@elevenlabs/react@^1.6.0` (RESEARCH § Pattern 1 / Standard Stack):
```typescript
// useConversation options (relevant subset)
useConversation(options: {
  onConnect?: () => void
  onDisconnect?: () => void
  onModeChange?: (e: { mode: 'speaking' | 'listening' }) => void
  onError?: (err: unknown) => void
  onMessage?: (msg: unknown) => void
})
// → returns:
{
  status: 'connected' | 'connecting' | 'disconnected' | 'disconnecting'
  mode: 'speaking' | 'listening'
  isSpeaking: boolean
  isListening: boolean
  isMuted: boolean
  startSession: (opts: {
    signedUrl: string
    connectionType: 'websocket'  // MUST be 'websocket' — RESEARCH Anti-pattern + Pitfall 3
    overrides?: { agent?: { firstMessage?: string } }
  }) => Promise<string>
  endSession: () => Promise<void>
  setVolume: (o: { volume: number }) => Promise<void>
  setMuted: (m: boolean) => void
}
```

From `components/avatar/avatar.tsx` (the consumer side — already wired in Phase 9):
```typescript
// Avatar renders <div data-avatar-state={state}>EMOJI</div>
// where state is computed by useAvatarState() which subscribes to voice:state events.
// No changes needed here.
```

From `e2e/avatar.spec.ts` (analog for the E2E we are writing):
```typescript
// Login + cookie save pattern — copy verbatim
test.beforeAll(async () => { /* seedUser, login via magic link, saveCookies */ })
// Bus injection pattern — copy verbatim
await page.evaluate(() => {
  if (!window.__lessonBus) throw new Error('window.__lessonBus not available')
  window.__lessonBus.emit('voice:state', { state: 'speaking' })
})
await expect(avatar).toHaveAttribute('data-avatar-state', 'speaking', { timeout: 2_000 })
```
</interfaces>

<russian_copy_table>
<!-- Project convention: all user-facing strings in Russian, error prefix "Ошибка:". -->
<!-- These exact strings are part of the test acceptance criteria — match byte-for-byte. -->

| Where | Russian text |
|-------|--------------|
| Start button label | `Запустить голос` |
| Stop button label | `Стоп` |
| firstMessage override template | ``Привет! Сегодня у нас тема: ${topic}. Тебя как зовут?`` (template literal with `${topic}` from response) |
| Mic denied (NotAllowedError) | `Доступ к микрофону запрещён. Разрешите его в настройках браузера и попробуйте снова.` |
| Mic not found (NotFoundError) | `Микрофон не найден. Подключите микрофон и попробуйте снова.` |
| Mic busy (NotReadableError) | `Микрофон занят другим приложением (Zoom, Discord). Закройте его и попробуйте снова.` |
| Mic generic | `Не удалось получить доступ к микрофону. Попробуйте перезагрузить страницу.` |
| Signed-URL fetch failure | `Не удалось получить ссылку. Попробуйте снова.` |
| SDK error | `Ошибка голосового сервиса. Попробуйте снова.` |
| Error block prefix | `Ошибка: ` (followed by the message above) |
| Status indicator (idle) | `Готов к запуску` |
| Status indicator (connecting) | `Подключаемся…` |
| Status indicator (connected/listening) | `Слушаю…` |
| Status indicator (speaking) | `Говорю…` |
</russian_copy_table>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite VoicePanel + thread `topic` from LessonShell + unit tests</name>
  <files>components/panels/voice-panel.tsx, components/lesson-shell.tsx, components/panels/__tests__/voice-panel.test.tsx</files>
  <read_first>
    - `components/panels/voice-panel.tsx` (current Phase 9 placeholder — read in full so you preserve top-half Avatar/useAvatarState wiring untouched)
    - `components/panels/board-panel.tsx` (analog — fetch + error block + button states — lines 1-50 imports, lines 105-120 click flow, lines 319-324 error block)
    - `components/lesson-shell.tsx` (the ONLY change here is line 117 — `<VoicePanel lessonId={lessonId} topic={topic} />`)
    - `components/avatar/avatar.tsx` + `components/avatar/use-avatar-state.ts` (so you understand why we do NOT touch the top half — they consume voice:state via bus, not props)
    - `lib/lesson-bus/events.ts` (current VoiceStatePayload union — confirm 4 states, NO `'connected'`)
    - `lib/lesson-bus/hooks.ts` (useLessonBus signature)
    - `lib/elevenlabs/types.ts` (just shipped by 06-01)
    - `components/avatar/__tests__/avatar.test.tsx` (vitest + testing-library render pattern; `React.createElement(...)` style — project convention)
    - `lib/trainer/__tests__/use-trainer-idle.test.ts` (bus mock pattern lines 7-30 — `subscriptions` map + `mockEmit`)
    - `.planning/phases/06-voice/06-RESEARCH.md` § Pattern 1 (lines 168-232 — full useConversation example) + § Pattern 3 (lines 323-353 — callback wiring) + § Common Pitfalls (especially #1, #3, #4, #7) + § Code Examples (lines 458-500 — vitest mock pattern)
    - `.planning/phases/06-voice/06-PATTERNS.md` § `components/panels/voice-panel.tsx` (lines 221-339) + § `components/panels/__tests__/voice-panel.test.tsx` (lines 393-496)
    - `.planning/PHASE-6-SETUP-2026-05-10.md` § 10 (First Message template) + § 12 (file inventory)
    - `vitest.config.ts` (confirms happy-dom env — getUserMedia mock pattern depends on this)
  </read_first>
  <behavior>
    Test cases for `components/panels/__tests__/voice-panel.test.tsx` (one per ID in VOI-01-I..R; some merged):

    1. (VOI-01 base) Initial render — `useConversation` returns `{ status: 'disconnected', mode: 'listening', ... }`; assert Start button visible with label `Запустить голос`; assert Stop button NOT visible OR disabled; assert status text `Готов к запуску`.
    2. (VOI-01-I) Click Start → assert `navigator.mediaDevices.getUserMedia` called with `{ audio: true }` BEFORE any `fetch` call (assert call order via `vi.fn().mockImplementationOnce` + a timestamp / sequence counter).
    3. (VOI-01-J) After mic granted → assert `fetch` called with `'/api/voice/signed-url'`, `{ method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ lessonId: 'lid-test' }) }`.
    4. (VOI-01-K) After fetch returns `{ signedUrl: 'wss://mock', topic: 'Дроби' }` → assert `mockStartSession` called with `{ signedUrl: 'wss://mock', connectionType: 'websocket', overrides: { agent: { firstMessage: 'Привет! Сегодня у нас тема: Дроби. Тебя как зовут?' } } }`. Notably: `connectionType` is EXACTLY the string `'websocket'` (RESEARCH Pitfall 3 — explicit even though default for signed URL).
    5. (VOI-01-K defense-in-depth) `firstMessage` uses the topic from the RESPONSE body, not from the `topic` prop. Test by passing `topic="WRONG_PROP"` as prop AND making fetch return `topic: 'CORRECT_TOPIC'`; assert `mockStartSession` receives `firstMessage` containing `'CORRECT_TOPIC'`, NOT `'WRONG_PROP'`. (Resolves RESEARCH Open Q4.)
    6. (VOI-01-L) Fire `capturedOptions.onModeChange({ mode: 'listening' })` → assert `mockEmit` called with `('voice:state', { state: 'listening' })`.
    7. (VOI-01-M) Fire `capturedOptions.onModeChange({ mode: 'speaking' })` → assert `mockEmit` called with `('voice:state', { state: 'speaking' })`.
    8. (VOI-01-N) Fire `capturedOptions.onDisconnect()` → assert `mockEmit` called with `('voice:state', { state: 'idle' })`.
    9. (Connect) Fire `capturedOptions.onConnect()` → assert `mockEmit` called with `('voice:state', { state: 'idle' })` (per RESEARCH Open Q3 — `'idle'` NOT `'connected'`).
    10. (VOI-01-O) Fire `capturedOptions.onError(new Error('boom'))` → assert error block visible with `Ошибка: Ошибка голосового сервиса. Попробуйте снова.` AND `mockEmit` called with `('voice:state', { state: 'idle' })` (fail-safe).
    11. (VOI-01-P) `getUserMedia` rejects with `DOMException('denied', 'NotAllowedError')` → assert error block visible with `Ошибка: Доступ к микрофону запрещён. Разрешите его в настройках браузера и попробуйте снова.` AND `mockStartSession` NEVER called.
    12. (VOI-01-Q) `getUserMedia` rejects with `DOMException('no device', 'NotFoundError')` → assert error block contains `Микрофон не найден.`
    13. (Mic busy) `getUserMedia` rejects with `DOMException('busy', 'NotReadableError')` → assert error block contains `Микрофон занят другим приложением`.
    14. (Fetch failure) fetch resolves with `{ ok: false, status: 500, text: async () => 'server error' }` → error block contains `Не удалось получить ссылку. Попробуйте снова.` AND `mockStartSession` NEVER called.
    15. (VOI-01-R) When mock returns `{ status: 'connected' }` (override capturedReturn for this test) → Stop button visible with label `Стоп`; click Stop → assert `mockEndSession` called.
    16. (Cleanup — Pitfall 7) Unmount the component while `status === 'connected'` → assert `mockEndSession` called (cleanup effect fires).
    17. (Stable callbacks — Pitfall 4) Re-render the component three times by manually calling `rerender(...)` with same props → assert `capturedOptions.onModeChange` reference is the SAME function across renders (proves `useCallback` deps are stable).
  </behavior>
  <action>
    SUB-STEP 1.1 — Edit `components/lesson-shell.tsx` (ONE LINE CHANGE).
    Locate the existing `<VoicePanel lessonId={lessonId} />` on line 117. Replace with:
    ```tsx
    <VoicePanel lessonId={lessonId} topic={topic} />
    ```
    The `topic` variable is already in scope (destructured from `LessonShellProps` on line 37 — confirmed in current file). No other changes to LessonShell. Run `npx tsc --noEmit` after — TypeScript will error if `VoicePanel` props don't yet include `topic`. That error gets fixed by sub-step 1.2.

    SUB-STEP 1.2 — Rewrite `components/panels/voice-panel.tsx`.

    PRESERVE (do NOT touch):
    - `'use client'` directive on line 1.
    - Existing file header comment block (extend it with Phase 6 note — see below).
    - Imports for `Card/CardHeader/CardTitle/CardContent`, `Button`, `useLessonBus`, `Avatar`, `useAvatarState`.
    - The `<Card>` shell with `<CardHeader>` containing the Mic icon + `Голос` title.
    - The TOP HALF div containing `<Avatar state={avatarState} />` — this stays as-is.

    REWRITE (the bottom half — lines 50-63 of current file):

    Change the props interface to REQUIRE both `lessonId` and `topic`:
    ```typescript
    interface VoicePanelProps {
      lessonId: string  // Required as of Phase 6 — lesson page passes it via LessonShell.
      topic: string     // Required as of Phase 6 — used as defense-in-depth fallback for firstMessage.
    }

    export function VoicePanel({ lessonId, topic }: VoicePanelProps) {
      // ...
    }
    ```
    Drop the optional underscore-prefixed destructure. The default `= {}` object also goes — props are now required.

    Drop `SHOW_TEST_BUTTON`, the `counter` state, and `handleTestBus`. Phase 6 puts real UI in the bottom half; the bus will be exercised in real flow via the SDK callbacks. (Per PATTERNS § voice-panel.tsx "Decision for plan: drop the test button".)

    Add NEW imports at top of file (alphabetical within their import group):
    ```typescript
    import { useCallback, useEffect, useRef, useState } from 'react'
    import { useConversation } from '@elevenlabs/react'
    import type { VoiceErrorKind } from '@/lib/elevenlabs/types'
    ```

    Inside the component, BEFORE the existing `useAvatarState()` call, add the voice machinery:

    a) `const bus = useLessonBus()` — same hook already used at line 25 of current file (move from where it was if needed).

    b) Local state:
    ```typescript
    const [error, setError] = useState<string | null>(null)
    const isStartingRef = useRef(false)  // prevent double-start while fetch is in flight
    ```

    c) Stable callbacks via `useCallback` (RESEARCH Pitfall 4 — prevents stale closures):
    ```typescript
    const handleConnect = useCallback(() => {
      bus.emit('voice:state', { state: 'idle' })  // RESEARCH Open Q3 — 'idle' not 'connected'
    }, [bus])

    const handleModeChange = useCallback(
      ({ mode }: { mode: 'speaking' | 'listening' }) => {
        bus.emit('voice:state', { state: mode })  // direct mapping
      },
      [bus],
    )

    const handleDisconnect = useCallback(() => {
      bus.emit('voice:state', { state: 'idle' })
    }, [bus])

    const handleError = useCallback((err: unknown) => {
      console.error('[voice-panel] SDK error:', err)
      bus.emit('voice:state', { state: 'idle' })  // fail-safe — avatar back to 🙂
      setError('Ошибка голосового сервиса. Попробуйте снова.')
    }, [bus])
    ```

    d) The SDK hook:
    ```typescript
    const conversation = useConversation({
      onConnect: handleConnect,
      onDisconnect: handleDisconnect,
      onModeChange: handleModeChange,
      onError: handleError,
    })
    ```

    e) `useAvatarState()` call — keep as-is, AFTER the conversation hook (Avatar consumes bus state, so it observes what we just wired).

    f) The mic-permission helper. Inline function (NOT exported) that returns a `VoiceErrorKind` union member, OR succeeds. Implementation:
    ```typescript
    async function requestMicPermission(): Promise<{ ok: true } | { ok: false; message: string }> {
      try {
        const stream = await navigator.mediaDevices.getUserMedia({ audio: true })
        // Release the stream — SDK will request its own.
        stream.getTracks().forEach((t) => t.stop())
        return { ok: true }
      } catch (err) {
        const e = err as DOMException
        if (e.name === 'NotAllowedError') {
          return { ok: false, message: 'Доступ к микрофону запрещён. Разрешите его в настройках браузера и попробуйте снова.' }
        }
        if (e.name === 'NotFoundError') {
          return { ok: false, message: 'Микрофон не найден. Подключите микрофон и попробуйте снова.' }
        }
        if (e.name === 'NotReadableError') {
          return { ok: false, message: 'Микрофон занят другим приложением (Zoom, Discord). Закройте его и попробуйте снова.' }
        }
        return { ok: false, message: 'Не удалось получить доступ к микрофону. Попробуйте перезагрузить страницу.' }
      }
    }
    ```
    Place it inside the component function (or as a top-level helper outside — either works; choose top-level for cleanliness and unit-testability via a non-React util). Actually — put it inside the component to keep the file's surface area minimal and per the `board-panel.tsx` style of co-locating helpers (line 105+).

    g) The Start click handler:
    ```typescript
    const handleStart = useCallback(async () => {
      if (isStartingRef.current) return
      isStartingRef.current = true
      setError(null)
      try {
        // STEP 1: mic permission BEFORE anything else (RESEARCH Pitfall 1).
        const mic = await requestMicPermission()
        if (!mic.ok) {
          setError(mic.message)
          return
        }
        // STEP 2: fresh signed URL — fetch inside click handler, NOT pre-fetched (Pitfall 2 — 15-min TTL).
        const res = await fetch('/api/voice/signed-url', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ lessonId }),
        })
        if (!res.ok) {
          setError('Не удалось получить ссылку. Попробуйте снова.')
          return
        }
        const data = (await res.json()) as { signedUrl: string; topic: string }
        // STEP 3: start the session. Use topic from RESPONSE (defense-in-depth — Open Q4),
        // not from prop (which may be stale due to navigation). Fall back to prop if response field empty.
        const effectiveTopic = data.topic || topic
        await conversation.startSession({
          signedUrl: data.signedUrl,
          connectionType: 'websocket',  // CRITICAL — RESEARCH Pitfall 3
          overrides: {
            agent: {
              firstMessage: `Привет! Сегодня у нас тема: ${effectiveTopic}. Тебя как зовут?`,
            },
          },
        })
      } catch (err) {
        console.error('[voice-panel] start failed:', err)
        setError('Ошибка голосового сервиса. Попробуйте снова.')
      } finally {
        isStartingRef.current = false
      }
    }, [conversation, lessonId, topic])
    ```

    h) The Stop click handler:
    ```typescript
    const handleStop = useCallback(async () => {
      try {
        await conversation.endSession()
      } catch (err) {
        console.error('[voice-panel] stop failed:', err)
      }
    }, [conversation])
    ```

    i) Cleanup effect (RESEARCH Pitfall 7 — mic stays red on unmount):
    ```typescript
    useEffect(() => {
      return () => {
        if (conversation.status === 'connected' || conversation.status === 'connecting') {
          conversation.endSession().catch((err) => console.error('[voice-panel] cleanup endSession:', err))
        }
      }
    }, [conversation])
    ```

    j) Derive button visibility and status text:
    ```typescript
    const isActive = conversation.status === 'connected' || conversation.status === 'connecting'
    const statusText =
      conversation.status === 'connecting' ? 'Подключаемся…' :
      conversation.status === 'connected' && conversation.mode === 'speaking' ? 'Говорю…' :
      conversation.status === 'connected' && conversation.mode === 'listening' ? 'Слушаю…' :
      'Готов к запуску'
    ```

    k) The bottom-half JSX (replacing lines 50-63 of current file):
    ```tsx
    <div className="flex flex-col items-center gap-2 pb-2">
      <span className="text-xs text-muted-foreground">{statusText}</span>

      {!isActive && (
        <Button size="sm" onClick={handleStart} disabled={isStartingRef.current}>
          <Mic className="h-4 w-4 mr-1" />
          Запустить голос
        </Button>
      )}

      {isActive && (
        <Button size="sm" variant="outline" onClick={handleStop}>
          Стоп
        </Button>
      )}

      {error && (
        <div className="rounded border border-destructive/50 bg-destructive/10 px-2.5 py-1.5 text-xs text-destructive max-w-full text-center">
          <span className="font-semibold">Ошибка: </span>
          {error}
        </div>
      )}
    </div>
    ```
    Error block CSS mirrors `components/panels/board-panel.tsx` lines 319-324 verbatim except for the `max-w-full text-center` addition (VoicePanel is narrower than BoardPanel so we need wrapping/centering).

    l) File header comment block — extend the existing one to mention Phase 6:
    ```typescript
    // VoicePanel — Phase 6 rewrite (D-05/D-06/D-07, VOI-01).
    // Layout: Avatar (top half — Phase 9 contract unchanged) + voice control area (bottom half).
    // Bottom half: real mic Start/Stop button + status text + error block.
    // Flow: click Start → getUserMedia → POST /api/voice/signed-url → useConversation.startSession.
    // SDK callbacks emit 'voice:state' events on the lesson bus (Avatar subscribes via Phase 9 contract).
    // window.__lessonBus is exposed by LessonBusProvider in non-prod (Phase 9, D-11) so Playwright
    // E2E can simulate SDK callbacks without hitting real 11labs (11labs daily call limit is 100 — D-08).
    //
    // Per RESEARCH § Pattern 1 + § Pitfall 1/3/4/7 + PHASE-6-SETUP § 12 plan scope.
    ```

    SUB-STEP 1.3 — Create `components/panels/__tests__/voice-panel.test.tsx` (NEW directory — `components/panels/__tests__/` does not exist yet, per PATTERNS § voice-panel.test.tsx warning).
    Top of file: import block + mocks block in this exact order (vitest hoisting requires it):
    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
    import { render, screen, fireEvent, cleanup } from '@testing-library/react'
    import React from 'react'

    // ─── Mock @elevenlabs/react ────────────────────────────────────────────────
    // Module-level mutables let tests fire SDK callbacks synchronously and override return state.
    let capturedOptions: any = null
    let capturedReturn: any = {
      status: 'disconnected',
      mode: 'listening',
      isSpeaking: false,
      isListening: false,
      isMuted: false,
      startSession: vi.fn().mockResolvedValue('conv_test_123'),
      endSession: vi.fn().mockResolvedValue(undefined),
    }

    vi.mock('@elevenlabs/react', () => ({
      useConversation: (options: any) => {
        capturedOptions = options
        return capturedReturn
      },
    }))

    // ─── Mock @/lib/lesson-bus ─────────────────────────────────────────────────
    const mockEmit = vi.fn()
    vi.mock('@/lib/lesson-bus', () => ({
      useLessonBus: () => ({ emit: mockEmit, on: vi.fn(), off: vi.fn() }),
      useLessonBusEvent: vi.fn(),  // unused by VoicePanel but exported
    }))

    // ─── Mock Avatar + useAvatarState — keep test focused on Voice behavior ────
    vi.mock('@/components/avatar/avatar', () => ({
      Avatar: ({ state }: { state: string }) =>
        React.createElement('div', { 'data-avatar-state': state }, 'AVATAR'),
    }))
    vi.mock('@/components/avatar/use-avatar-state', () => ({
      useAvatarState: () => 'idle',
    }))

    // ─── Mock fetch + getUserMedia ─────────────────────────────────────────────
    const mockFetch = vi.fn()
    global.fetch = mockFetch as any

    const mockGetUserMedia = vi.fn()
    Object.defineProperty(global.navigator, 'mediaDevices', {
      value: { getUserMedia: mockGetUserMedia },
      writable: true,
    })

    const { VoicePanel } = await import('../voice-panel')
    ```

    For each test (17 in `<behavior>`), inside `beforeEach` reset mocks + restore default `capturedReturn`. For test #15 (Stop visible), override `capturedReturn.status = 'connected'` BEFORE rendering, then assert.

    For the call-order test (#2 — VOI-01-I), implement order check using a counter:
    ```typescript
    let callOrder: string[] = []
    mockGetUserMedia.mockImplementation(async () => {
      callOrder.push('getUserMedia')
      return { getTracks: () => [{ stop: vi.fn() }] }
    })
    mockFetch.mockImplementation(async () => {
      callOrder.push('fetch')
      return { ok: true, json: async () => ({ signedUrl: 'wss://x', topic: 'T' }) }
    })
    // ... render + click ...
    await vi.waitFor(() => expect(callOrder).toEqual(['getUserMedia', 'fetch']))
    ```

    For the unmount cleanup test (#16):
    ```typescript
    capturedReturn = { ...capturedReturn, status: 'connected' }
    const { unmount } = render(React.createElement(VoicePanel, { lessonId: 'lid', topic: 'T' }))
    unmount()
    expect(capturedReturn.endSession).toHaveBeenCalled()
    ```

    For the stable-callback test (#17 — Pitfall 4):
    ```typescript
    const { rerender } = render(React.createElement(VoicePanel, { lessonId: 'lid', topic: 'T' }))
    const firstRef = capturedOptions.onModeChange
    rerender(React.createElement(VoicePanel, { lessonId: 'lid', topic: 'T' }))
    rerender(React.createElement(VoicePanel, { lessonId: 'lid', topic: 'T' }))
    expect(capturedOptions.onModeChange).toBe(firstRef)
    ```

    Run `npm test -- voice-panel` — all 17 tests MUST pass. If test #17 fails (callback ref not stable), it indicates a `useCallback` deps array problem in SUB-STEP 1.2 — fix the deps array (most likely `bus` should be the only dep).

    Decision IDs implemented: D-05 (env vars not touched on client — only signedUrl + topic crosses boundary), D-06 (firstMessage override path active; system prompt NOT overridden), D-07 (bus event wiring per Phase 9 contract — 4 states only, no 'connected'), D-08 (no custom tools wired — deferred to Phase 8), D-09 (UAT criteria 1-9 land via this component; #10 verified by Task 2 E2E).
  </action>
  <acceptance_criteria>
    - `test -f components/panels/__tests__/voice-panel.test.tsx` returns 0.
    - `grep -E "topic=\\{topic\\}" components/lesson-shell.tsx` matches exactly once (in the `<VoicePanel ... />` JSX).
    - `grep -E "import \\{ useConversation \\} from '@elevenlabs/react'" components/panels/voice-panel.tsx` matches.
    - `grep -E "connectionType: 'websocket'" components/panels/voice-panel.tsx` matches (CRITICAL — RESEARCH Pitfall 3).
    - `grep -E "fetch\\(['\"]/api/voice/signed-url" components/panels/voice-panel.tsx` matches.
    - `grep -E "navigator\\.mediaDevices\\.getUserMedia" components/panels/voice-panel.tsx` matches.
    - `grep -E "bus\\.emit\\(['\"]voice:state['\"]" components/panels/voice-panel.tsx` matches at least 4 times (onConnect/onModeChange/onDisconnect/onError → bus emit each).
    - `grep -E "Запустить голос" components/panels/voice-panel.tsx` matches.
    - `grep -E "Стоп" components/panels/voice-panel.tsx` matches.
    - `grep -E "Привет! Сегодня у нас тема:" components/panels/voice-panel.tsx` matches.
    - `grep -E "NotAllowedError" components/panels/voice-panel.tsx` matches (mic-denied branch).
    - `grep -E "NotFoundError" components/panels/voice-panel.tsx` matches.
    - `grep -E "NotReadableError" components/panels/voice-panel.tsx` matches.
    - `grep -E "Доступ к микрофону запрещён" components/panels/voice-panel.tsx` matches.
    - `grep -E "Микрофон не найден" components/panels/voice-panel.tsx` matches.
    - `grep -E "Микрофон занят другим приложением" components/panels/voice-panel.tsx` matches.
    - `grep -E "Не удалось получить ссылку" components/panels/voice-panel.tsx` matches.
    - `grep -E "endSession" components/panels/voice-panel.tsx` matches at least 2 times (handleStop + cleanup effect).
    - `grep -E "useCallback" components/panels/voice-panel.tsx` matches at least 5 times (4 SDK handlers + handleStart + handleStop — at minimum 6).
    - `grep -E "'connected'" components/panels/voice-panel.tsx` — `bus.emit\(.*'connected'\)` MUST NOT match (RESEARCH Open Q3 — we use 'idle' for the connect transition).
    - `grep -E "NEXT_PUBLIC_ELEVENLABS" components/panels/voice-panel.tsx` finds NOTHING (D-05 invariant on client side).
    - `grep -E "'sk_'" components/panels/voice-panel.tsx` finds NOTHING.
    - `grep -E "agent_7701" components/panels/voice-panel.tsx` finds NOTHING (Agent ID never on client).
    - `grep -c "Avatar" components/panels/voice-panel.tsx` is at least 2 (Avatar import + render — top half preserved).
    - `npm test -- components/panels/__tests__/voice-panel` exits 0 with at least 17 passing tests.
    - `npm test` exits 0 (no regressions in 301+ existing tests — Phase 9 avatar tests still green, no LessonShell test breakage).
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0 (bundle still builds).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- components/panels &amp;&amp; npm test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <done>
    VoicePanel rewritten with `useConversation` + real mic button + Russian error block + bus wiring. LessonShell passes `topic` prop. Component-level test file at `components/panels/__tests__/voice-panel.test.tsx` with 17 passing tests covering VOI-01-I through VOI-01-R plus order/cleanup/stability invariants. No client-side leak of ELEVENLABS_*/Agent ID. Single commit: `feat(voice): VoicePanel mic integration + topic prop (VOI-01-I..R)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright E2E voice-flow.spec.ts + bundle-leak smoke (VOI-01-S/T) + Open Q1 manual-smoke note</name>
  <files>e2e/voice-flow.spec.ts, .planning/phases/06-voice/06-02-SUMMARY.md, .planning/MANUAL-ACTIONS.md</files>
  <read_first>
    - `e2e/avatar.spec.ts` (analog — full file; especially lines 9-24 imports, 47-75 seedAvatarUser, 82-117 beforeAll login flow, 127-151 goToLesson helper, 161-176 bus-injection assertion)
    - `e2e/board-panel.spec.ts` (analog — second proof point for the login + cookie + seed pattern)
    - `e2e/fixtures/db-setup.ts` (helper functions: `makeTestEmail`, `resetTestDb`, `readMagicLinkFor`, `clearMagicLinkFile`)
    - `components/panels/voice-panel.tsx` (just rewritten in Task 1 — confirms button selectors `Запустить голос` and `Стоп`)
    - `lib/lesson-bus/provider.tsx` (look for `window.__lessonBus` exposure pattern — Phase 9 D-11)
    - `playwright.config.ts` (confirms chromium project, base URL)
    - `.planning/phases/06-voice/06-RESEARCH.md` § Code Examples (lines 502-543 — voice-flow.spec.ts pattern) + § Common Pitfalls #8 (lines 452-456 — DO NOT hit real 11labs, daily limit 100) + § Security Domain (lines 758-771 — T1 API key bundle leak mitigation)
    - `.planning/phases/06-voice/06-PATTERNS.md` § `e2e/voice-flow.spec.ts` (lines 655-770) + § E2E Login + Lesson Seed Pattern (lines 866-878)
    - `.planning/phases/06-voice/06-CONTEXT.md` § D-09 step 10 (UAT criterion — ELEVENLABS_API_KEY absent from client bundle)
    - `.planning/PHASE-6-SETUP-2026-05-10.md` § 7 Security (Allowlist = klassio-one.vercel.app + localhost:3000) — for Open Q1 manual smoke note context
  </read_first>
  <behavior>
    Test cases for `e2e/voice-flow.spec.ts`:

    1. (VOI-01-S — mic button visible) Lesson page loads via login + cookie inject + goToLesson → `page.getByRole('button', { name: /Запустить голос/i })` is visible within 10s.
    2. (VOI-01-S — fetch stub + click) Network stub `/api/voice/signed-url` returns `{ signedUrl: 'wss://mock.local/test', topic: 'E2E голос' }` → click Start → assert mock route was called at least once (via `page.waitForRequest` or counter).
    3. (VOI-01-S — bus drives avatar listening) `page.evaluate(() => window.__lessonBus.emit('voice:state', { state: 'listening' }))` → assert `[data-avatar-state="listening"]` within 2s.
    4. (VOI-01-S — bus drives avatar speaking) Emit `{ state: 'speaking' }` → assert `[data-avatar-state="speaking"]`.
    5. (VOI-01-S — bus drives avatar idle) Emit `{ state: 'idle' }` → assert `[data-avatar-state="idle"]`.
    6. (Russian error UI) Override `page.route` to return 500 with body `{ error: 'Voice service не настроен' }` → click Start (with `page.evaluate` first to stub `navigator.mediaDevices.getUserMedia` resolved) → assert error block visible with text matching `/Ошибка: Не удалось получить ссылку/`.
    7. (VOI-01-T — bundle leak: ELEVENLABS_API_KEY absent) `await page.goto(/lesson/${lessonId})`; `const html = await page.content()`; assert `html.includes('ELEVENLABS_API_KEY')` is FALSE.
    8. (VOI-01-T — bundle leak: ELEVENLABS_AGENT_ID absent) Same — assert `html.includes('ELEVENLABS_AGENT_ID')` is FALSE.
    9. (VOI-01-T — bundle leak: literal agent ID absent) Assert `html.includes('agent_7701kr9c2v7eev3tabzv4f2b0e8b')` is FALSE.
    10. (VOI-01-T — bundle leak: 'sk_' prefix absent) Assert `html.match(/['"]sk_[A-Za-z0-9_-]{20,}['"]/)` is null (no API-key-shaped substring). Use a strict regex to avoid false positives from unrelated strings.
    11. (VOI-01-T — JS chunk bundle leak) Fetch each `<script src="...">` URL via `page.context().request.get(url).text()` and assert NONE contain `ELEVENLABS_API_KEY` literal. This goes beyond page.content() to scan the actual JS chunks. Limit to chunks under `/_next/static/chunks/` to avoid third-party CDNs.
  </behavior>
  <action>
    SUB-STEP 2.1 — Create `e2e/voice-flow.spec.ts`.

    Copy the file scaffold from `e2e/avatar.spec.ts` (imports, BASE_URL, dotenv loading, pgQuery helper if present, seed helper, beforeAll/afterAll). Rename:
    - `seedAvatarUser` → `seedVoiceUser`
    - Note in DB seed: `notes: 'e2e voice test'`
    - Topic in seeded lesson: `topic: 'E2E голос'`
    - Other fields identical.

    Add `test.describe('Voice flow (VOI-01)', () => { ... })` wrapper.

    The `beforeAll` block is copy-paste from `e2e/avatar.spec.ts` lines 82-117 — change only the describe-name in the test wrapper. Same magic-link login dance, same `savedCookies = await context.cookies()` at the end. Reuse `clearMagicLinkFile`/`readMagicLinkFor` from `e2e/fixtures/db-setup.ts`.

    Reuse `goToLesson(page)` helper from `e2e/avatar.spec.ts` lines 127-151 verbatim (Neon warm-up + 3-retry navigation). Either copy the function INTO `voice-flow.spec.ts` or import it from a shared spot if `avatar.spec.ts` exports it — currently it's a local function in `avatar.spec.ts`, so copy verbatim.

    For each test, ALWAYS set up the network stub BEFORE `goToLesson`:
    ```typescript
    await page.route('**/api/voice/signed-url', (route) =>
      route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({ signedUrl: 'wss://mock.local/test', topic: 'E2E голос' }),
      }),
    )
    ```
    This satisfies RESEARCH Pitfall 8 — E2E NEVER hits real 11labs.

    For tests that need to verify the network stub fired:
    ```typescript
    let stubCalled = false
    await page.route('**/api/voice/signed-url', (route) => {
      stubCalled = true
      return route.fulfill({ /* ... */ })
    })
    // ... click Start ...
    await page.waitForTimeout(500)  // allow click + fetch
    expect(stubCalled).toBe(true)
    ```

    For tests that click Start, FIRST inject a `navigator.mediaDevices.getUserMedia` stub via `page.addInitScript`:
    ```typescript
    await page.addInitScript(() => {
      Object.defineProperty(navigator, 'mediaDevices', {
        configurable: true,
        value: {
          getUserMedia: async () => ({ getTracks: () => [{ stop: () => {} }] }),
        },
      })
    })
    ```
    Otherwise the click triggers a real browser permission prompt that hangs the test.

    For the bundle-leak tests (VOI-01-T — tests #7–#11), do this AFTER all other tests in the describe (they read the full HTML, which is slow):
    ```typescript
    test('VOI-01-T: ELEVENLABS_API_KEY absent from page HTML', async ({ page }) => {
      await page.context().addCookies(savedCookies)
      await goToLesson(page)
      const html = await page.content()
      expect(html).not.toContain('ELEVENLABS_API_KEY')
      expect(html).not.toContain('ELEVENLABS_AGENT_ID')
      expect(html).not.toContain('agent_7701kr9c2v7eev3tabzv4f2b0e8b')
      // Strict regex — match a quoted sk_ followed by 20+ alphanum/_/-
      expect(html).not.toMatch(/['"]sk_[A-Za-z0-9_-]{20,}['"]/)
    })

    test('VOI-01-T: ELEVENLABS_API_KEY absent from JS chunks', async ({ page }) => {
      await page.context().addCookies(savedCookies)
      await goToLesson(page)
      const scriptSrcs = await page.locator('script[src]').evaluateAll((els) =>
        (els as HTMLScriptElement[]).map((el) => el.src),
      )
      const chunkUrls = scriptSrcs.filter((u) => u.includes('/_next/static/chunks/'))
      expect(chunkUrls.length).toBeGreaterThan(0)
      for (const url of chunkUrls) {
        const resp = await page.context().request.get(url)
        const body = await resp.text()
        expect(body).not.toContain('ELEVENLABS_API_KEY')
        expect(body).not.toContain('agent_7701kr9c2v7eev3tabzv4f2b0e8b')
      }
    })
    ```

    Add a `test.afterAll(resetTestDb)` at the end of the describe block — copy verbatim from `avatar.spec.ts` line 121.

    SUB-STEP 2.2 — Run the spec.
    `npm run test:e2e -- e2e/voice-flow.spec.ts` must pass all 11 tests (or whatever count the executor lands — minimum 9). If any bundle-leak test FAILS (i.e., the literal IS found in the bundle), STOP immediately — this is a P0 D-05 violation. Inspect the source of the leak (most likely an accidental `console.log` of the env var, or a stale build cache); fix at the source, do NOT add an allowlist to the assertion.

    SUB-STEP 2.3 — Manual smoke note for Open Q1 (allowlist + signedURL conflict).
    Do NOT make this an autonomous test (it would consume 1+ 11labs daily-call-limit credits AND requires actually being logged in with VPN). Instead, write a clear note in `06-02-SUMMARY.md` under `### Manual smoke results` after running the procedure below ONCE:

    ```markdown
    ### Manual smoke (Open Q1 — allowlist + signedURL conflict)

    Procedure:
    1. Start dev server: `npm run dev`
    2. Login as dev user (kratov.gr@gmail.com magic link)
    3. Navigate to any lesson page (use admin CLI to create one if needed)
    4. Open DevTools Network tab + WS tab
    5. Click «Запустить голос»
    6. Observe: mic prompt appears → grant → status text "Подключаемся…" → either:
       - SUCCESS path: status → "Слушаю…" within 2-3s; Avatar transitions to 👂 (data-avatar-state="listening"); speak; bot responds in Russian via Nataly; assert key NOT in WS frames (Network → WS → frame inspector).
       - FAILURE path: WS frame "1011 abnormal closure" OR "403 Forbidden" appears.
         → This signals Allowlist+signedURL conflict (or expired key, or RU-block requiring VPN).
         → Mitigation: in 11labs Dashboard → Agent → Security tab, REMOVE entries from Allowlist; save.
         → Retry. If now succeeds, RECORD this in MANUAL-ACTIONS.md as Phase 6 follow-up: "Allowlist disabled on agent 7701; signedURL+auth is the only gate."

    Recorded outcome (fill in during execution):
    - Date:
    - Browser + VPN config:
    - Outcome (success / failure):
    - Action taken (if failure):
    ```

    This note ships in SUMMARY.md, not in code. It does NOT block the plan from being marked done — D-09 step 10 is verified by the automated bundle-leak tests; the WS handshake success is dev-environment-dependent.

    SUB-STEP 2.4 — Update MANUAL-ACTIONS.md (if needed).
    If the manual smoke in 2.3 surfaces an allowlist conflict, add a new entry under Phase 6 follow-ups:
    ```markdown
    ### Phase 6 — Allowlist conflict resolution
    **Discovered:** {date}
    **Action:** In 11labs Dashboard → Agent agent_7701kr9c2v7eev3tabzv4f2b0e8b → Security tab → Allowlist → remove all entries → Save.
    **Why:** Authentication=ON + Signed URL is now the only gate. Allowlist was redundant per 11labs docs ("Do not configure signed URLs and allowlists together").
    **Verification:** Re-run manual smoke from 06-02-PLAN § SUB-STEP 2.3 — bot greeting plays in dev browser.
    ```
    If the smoke succeeds without removing the allowlist, RECORD that fact: "Allowlist + signedURL coexist without conflict in our setup (verified {date})."

    Decision IDs implemented: D-09 (UAT criteria automation — steps 1-9 via E2E bus path, step 10 via bundle scan; manual UAT for steps requiring real voice still owned by dev), D-08 (E2E never hits real 11labs — daily call limit respected per Pitfall 8), T-06-02-01/02 (bundle-leak verification — see threat model).
  </action>
  <acceptance_criteria>
    - `test -f e2e/voice-flow.spec.ts` returns 0.
    - `grep -E "test\\.describe\\('Voice flow" e2e/voice-flow.spec.ts` matches.
    - `grep -E "page\\.route\\('\\*\\*/api/voice/signed-url'" e2e/voice-flow.spec.ts` matches (network stub present).
    - `grep -E "addInitScript" e2e/voice-flow.spec.ts` matches (getUserMedia stub).
    - `grep -E "window\\.__lessonBus\\.emit" e2e/voice-flow.spec.ts` matches at least once.
    - `grep -E "ELEVENLABS_API_KEY" e2e/voice-flow.spec.ts` matches in the negative-assertion context (`not.toContain('ELEVENLABS_API_KEY')`).
    - `grep -E "agent_7701kr9c2v7eev3tabzv4f2b0e8b" e2e/voice-flow.spec.ts` matches at least once (in negative-assertion context — verifies we test for THIS literal).
    - `grep -E "/_next/static/chunks/" e2e/voice-flow.spec.ts` matches (chunk-scanning test present).
    - `grep -E "seedVoiceUser|seedUser" e2e/voice-flow.spec.ts` matches (seed helper used).
    - `grep -E "savedCookies" e2e/voice-flow.spec.ts` matches (cookie persistence pattern reused).
    - `grep -E "resetTestDb" e2e/voice-flow.spec.ts` matches (afterAll cleanup present).
    - `grep -cE "test\\(" e2e/voice-flow.spec.ts` is at least 9 (9+ test cases).
    - `grep -E "Запустить голос" e2e/voice-flow.spec.ts` matches (button locator).
    - `grep -E "data-avatar-state" e2e/voice-flow.spec.ts` matches (avatar assertion uses data attribute, not emoji).
    - `npm run test:e2e -- e2e/voice-flow.spec.ts` exits 0 with all tests passing.
    - `npm run test:e2e` exits 0 (no regressions in 20+ existing E2E tests).
    - Full Vitest suite (`npm test`) still green.
    - `npm run build` exits 0.
    - SUMMARY.md (created by execute-plan) contains a `### Manual smoke (Open Q1 — allowlist + signedURL conflict)` section with the procedure copied verbatim AND outcome filled in OR explicit "deferred — not yet attempted on real 11labs in this session" if the dev did not run the manual smoke. If outcome is "FAILURE — allowlist conflict", MANUAL-ACTIONS.md MUST be updated per SUB-STEP 2.4.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/voice-flow.spec.ts &amp;&amp; npm test &amp;&amp; npm run test:e2e &amp;&amp; npm run build</automated>
  </verify>
  <done>
    `e2e/voice-flow.spec.ts` ships with 9+ tests: 5 covering VOI-01-S (button visible, fetch stub fires, bus drives avatar through listening/speaking/idle), 1 fetch-failure UI test, 5 bundle-leak tests covering VOI-01-T at HTML + chunk level. Manual smoke procedure for Open Q1 recorded in SUMMARY.md with outcome (success / failure / deferred). If failure, MANUAL-ACTIONS.md updated. Single commit: `test(voice): E2E voice flow + bundle-leak smoke (VOI-01-S/T)`. Phase 6 implementation COMPLETE — pending only the developer's manual end-to-end UAT (real voice via VPN) per D-09 criteria #1-9.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM ↔ window.__lessonBus | Non-prod-only global exposed by Phase 9 LessonBusProvider for E2E inspection. Removed in production builds via `process.env.NODE_ENV !== 'production'` guard. Not a security boundary in dev/test; in prod the global is absent. |
| VoicePanel ↔ /api/voice/signed-url | Authenticated browser fetch — same boundary as 06-01 Task 2 covered, but now with a live consumer. |
| VoicePanel ↔ 11labs WebSocket (via signedUrl) | Direct browser-to-vendor WebSocket. Signed URL carries authentication; the browser never holds the raw API key. TTL 15 min enforced by 11labs at handshake time. |
| LessonShell prop boundary | `topic` crosses from RSC (server) to LessonShell (client) to VoicePanel (client). Topic is admin-controlled (ACC-04 admin CLI writes it); no user-input path. Acceptable as trusted string for `firstMessage` interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information disclosure | `ELEVENLABS_API_KEY` in client bundle | mitigate | Server-only env var (no NEXT_PUBLIC_); component reads NOTHING from env directly; key reaches client only as the signature INSIDE the signed URL (HMAC-protected, opaque from key recovery). E2E asserts via 4 distinct scans: HTML body, JS chunks, agent-ID literal, and `sk_*` prefix regex. Build-time grep also rejects any new `NEXT_PUBLIC_ELEVENLABS*` declaration. |
| T-06-02-02 | Information disclosure | `ELEVENLABS_AGENT_ID` in client bundle | mitigate | Same as T-06-02-01. Agent ID is semi-secret (it identifies our agent); E2E scans HTML + chunks for the literal `agent_7701kr9c2v7eev3tabzv4f2b0e8b`. |
| T-06-02-03 | Tampering | `lesson_topic` injection into `firstMessage` override | mitigate | Topic is admin-CLI-written (Phase 2 ACC-04), no user-input path. Defense-in-depth: route returns topic in response JSON, panel prefers that over the prop (Open Q4 resolution). Length is bounded by DB column type (`text` — unbounded in Postgres, but admin CLI does not generate huge strings). Worst case (compromised admin): TTS reads weird text — NOT prompt injection (firstMessage is sent to TTS pipeline AFTER system prompt is locked). System prompt override blocked at 11labs Security tab (D-06). |
| T-06-02-04 | Spoofing | Cross-tab/origin reuse of signed URL | accept | Signed URL TTL is 15 min from generation. Reuse across the same session is OK (intended). Cross-origin reuse is gated by 11labs's signature validation. Risk: LOW — accept for Phase 6, document for Phase 6.5 if abused. |
| T-06-02-05 | Denial of service | Browser tab holds open WS forever after navigation | mitigate | Cleanup `useEffect` in VoicePanel calls `endSession` on unmount (RESEARCH Pitfall 7). Component unit test #16 verifies this. Mic indicator returns to off after navigation. |
| T-06-02-06 | Tampering | Stale closures in SDK callbacks | mitigate | All 4 callbacks (`onConnect`, `onModeChange`, `onDisconnect`, `onError`) wrapped in `useCallback` with `[bus]` dep. Stability invariant tested by component test #17 (re-render then assert callback reference is `===` to first render). |
| T-06-02-07 | Denial of service ($) | E2E suite burning real 11labs daily limit | mitigate | E2E uses `page.route` to stub `/api/voice/signed-url` at network layer (RESEARCH Pitfall 8). Real 11labs is NEVER hit by automated tests. Manual UAT (one developer, one session per day) stays under the 100/day limit by an order of magnitude. |
| T-06-02-08 | Spoofing | Replay of `firstMessage` greeting carries leaking PII | accept | `firstMessage` template is `"Привет! Сегодня у нас тема: ${topic}. Тебя как зовут?"` — `topic` is the lesson topic (math subject), not PII. The `child_name` field is explicitly deferred to Phase 6.5 (D-08), so no PII enters the override in Phase 6. Risk: LOW. |
</threat_model>

<verification>
After all tasks complete, run:

```bash
npm test                                           # 17+ component tests + all prior — green
npm run test:e2e -- e2e/voice-flow.spec.ts        # 9+ E2E tests — green
npm run test:e2e                                   # Full E2E suite — green (no avatar regression etc.)
npx tsc --noEmit                                   # Zero TypeScript errors
npm run build                                      # Bundle builds; /lesson/[id] route still listed
```

D-05 invariant grep checks (run from repo root):
```bash
# Ensure no NEXT_PUBLIC_ELEVENLABS* env vars added to repo
grep -rE "NEXT_PUBLIC_ELEVENLABS" --include="*.{ts,tsx,js,jsx,json}" .
# Should print NOTHING.

# Ensure ELEVENLABS_API_KEY is never read on client-shipped paths
grep -nE "ELEVENLABS_API_KEY" components/ lib/elevenlabs/get-signed-url.ts app/lesson app/login
# Should match ONLY in `lib/elevenlabs/get-signed-url.ts` (server util — that's intended).
# Should NOT match in components/ or app/lesson/.
```

Manual UAT (per D-09 criteria #1-9 — developer responsibility, not automated):
1. `npm run dev` with VPN ON
2. Log in via magic link to localhost:3000
3. Open a lesson with a real topic
4. Click «Запустить голос» → grant mic → speak Russian → bot responds
5. Avatar visibly transitions through 🙂 → 👂 → 🗣️
6. Click «Стоп» → conversation ends, avatar returns to 🙂
7. Reload page → can start again
8. Manual smoke for Open Q1 (per SUB-STEP 2.3 procedure) — record outcome in SUMMARY.md

Latency observation (informational, not blocking): Note the time between click Start and first audio out of bot. Target <3.5s end-to-end (3s baseline in 11labs Test UI + 200-500ms fetch overhead per CONTEXT.md `<deferred>`). If significantly higher, log as Phase 6.5 follow-up (Eagerness=High or Turbo v2.5 optimization).
</verification>

<success_criteria>
- VOI-01-I through VOI-01-R covered by 17 passing component tests in `components/panels/__tests__/voice-panel.test.tsx`.
- VOI-01-S covered by 5 passing E2E tests in `e2e/voice-flow.spec.ts` (button visible, fetch stub fires, bus drives avatar through 3 states).
- VOI-01-T covered by 5 passing E2E bundle-leak scans (page HTML × 4 patterns + JS chunks).
- D-05 invariant (no client-side ELEVENLABS_* exposure) verified by grep tests + E2E.
- D-07 invariant (use existing 4 voice:state variants, NO 'connected') verified by source grep + component test #9.
- D-09 UAT step 10 (no key in bundle) verified automatically by E2E.
- D-09 UAT steps 1-9 (real voice flow) recorded in SUMMARY.md after manual run.
- Open Q1 (allowlist + signedURL) resolved: either confirmed compatible (recorded in SUMMARY.md) OR remediated via MANUAL-ACTIONS.md update.
- All previous tests still green: avatar.spec.ts (Phase 9), board-panel.spec.ts (Phase 4), trainer tests (Phase 7), draw route tests (Phase 4), schedule tests (Phase 2).
- Two atomic commits, both following project commit-message style: `feat(voice): VoicePanel ...` + `test(voice): E2E ...`.
- Phase 6 implementation COMPLETE — only manual end-to-end UAT remains as a developer action.
</success_criteria>

<output>
After completion, create `.planning/phases/06-voice/06-02-SUMMARY.md` documenting:
- Files created/modified with final line counts.
- Test run output snippets (1 per test file).
- D-09 step 10 verification: paste the bundle-leak test output line showing 0 matches.
- Manual smoke results for Open Q1 (per SUB-STEP 2.3 — fill in date/outcome/action).
- Manual UAT log (per developer's local run — date, VPN status, latency observation, whether all 9 D-09 criteria passed, any open issues for Phase 6.5).
- Confirmation that MANUAL-ACTIONS.md is updated (or doesn't need to be) per the allowlist smoke outcome.
- Phase 6 final status: "COMPLETE pending Phase 6.5 (Hetzner WS proxy for РФ-без-VPN)" — confirms scope boundary held.
</output>
