---
phase: 02-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema.ts
  - drizzle/0001_lesson_phase2_columns.sql
  - scripts/db-push.ts
  - scripts/admin/create-user.ts
  - scripts/admin/create-lesson.ts
  - scripts/admin/list-users.ts
  - scripts/admin/list-lessons.ts
  - scripts/admin/__tests__/parse-args.test.ts
  - package.json
autonomous: true
requirements:
  - ACC-04
must_haves:
  truths:
    - "lib/db/schema.ts lesson table has 4 new nullable columns: recording_url, transcript_url, html_trainer_path, and topic is already present (verify it's TEXT nullable from Phase 1)"
    - "Running 'npm run db:push' against live Neon applies the new columns without dropping existing data"
    - "Running 'npm run admin:create-user -- --email x --child-name y --child-age z' inserts a row in user and allowed_email (idempotent)"
    - "Running 'npm run admin:create-lesson -- --email x --topic t --date 2026-06-01 --time 16:00' inserts a lesson row bound to that user (idempotent)"
    - "Running 'npm run admin:list-users' prints a table of all users"
    - "Running 'npm run admin:list-lessons' prints all lessons with user email + topic + scheduled_at"
  artifacts:
    - path: "scripts/admin/create-user.ts"
      provides: "Admin CLI: create user + whitelist email"
      exports: []
    - path: "scripts/admin/create-lesson.ts"
      provides: "Admin CLI: create lesson bound to a user"
      exports: []
    - path: "scripts/admin/list-users.ts"
      provides: "Admin CLI: list all users"
      exports: []
    - path: "scripts/admin/list-lessons.ts"
      provides: "Admin CLI: list all lessons with user info"
      exports: []
    - path: "scripts/admin/__tests__/parse-args.test.ts"
      provides: "Vitest unit tests for shared arg parser"
      exports: []
  key_links:
    - from: "scripts/admin/create-user.ts"
      to: "allowed_email + user tables"
      via: "pg.Client INSERT ... ON CONFLICT DO NOTHING"
      pattern: "ON CONFLICT.*DO NOTHING"
    - from: "scripts/admin/create-lesson.ts"
      to: "lesson table"
      via: "pg.Client INSERT with user_id resolved from email"
      pattern: "SELECT id FROM.*user.*email"
    - from: "scripts/db-push.ts"
      to: "live Neon DB"
      via: "drizzle-kit generate + per-statement SQL apply"
      pattern: "drizzle-kit generate"
---

<objective>
Extend the Drizzle schema with 3 nullable lesson columns required by later phases (D-11, D-13), push the migration to Neon, and build 4 admin CLI scripts (create-user, create-lesson, list-users, list-lessons) that a developer can run locally to seed test data without a UI.

Purpose: Unblocks Phase 2 E2E testing (admin can create the test child + test lesson) and pre-wires schema for Phase 7 (html_trainer_path) and Phase 10 (recording_url, transcript_url) without breaking anything.

Output:
- 3 new nullable TEXT columns on the `lesson` table in Neon
- 4 admin CLI scripts wired as npm scripts
- Vitest unit tests for the shared argument parser
</objective>

<execution_context>
@C:/Users/krato/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/krato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-admin/02-CONTEXT.md
@.planning/phases/01-account-shell/01-03-SUMMARY.md

<!-- Key interfaces the executor needs — no codebase exploration required -->
<interfaces>
<!-- From lib/db/schema.ts (Phase 1 deliverable — DO NOT REBUILD): -->
The lesson table currently has these columns:
  id uuid PK, user_id text FK→user.id, scheduled_at timestamp, topic text NOT NULL,
  duration_min integer DEFAULT 45, html_template_url text (nullable),
  status lessonStatusEnum DEFAULT 'scheduled', created_at timestamp

Phase 2 must ADD these 3 nullable text columns (D-11, D-13):
  recording_url text (nullable)       -- Phase 10 will populate
  transcript_url text (nullable)      -- Phase 10 will populate
  html_trainer_path text (nullable)   -- Phase 7 will populate (replaces html_template_url conceptually but coexists)

IMPORTANT: `topic` already exists as TEXT NOT NULL from Phase 1. Do NOT add it again.
Check lib/db/schema.ts before touching topic column — it is already there.

<!-- From scripts/seed.ts (idempotency pattern to follow exactly): -->
withClient pattern:
  async function withClient<T>(fn: (client: Client) => Promise<T>): Promise<T> {
    const client = new Client({ connectionString: process.env.DATABASE_URL_DIRECT })
    await client.connect()
    try { return await fn(client) } finally { await client.end() }
  }

DML idempotency pattern:
  INSERT INTO allowed_email (email, notes) VALUES ($1, $2) ON CONFLICT (email) DO NOTHING
  INSERT INTO "user" (id, email, ...) VALUES (...) ON CONFLICT (email) DO UPDATE SET ...

<!-- From scripts/db-push.ts (migration pattern — reuse as-is): -->
The existing db-push.ts already handles the full drizzle-kit generate + per-statement apply flow.
It looks for ALL .sql files in drizzle/ and applies them in sort order.
A new migration file 0001_*.sql will be auto-generated by drizzle-kit generate.
No changes to db-push.ts are needed for schema extension — drizzle-kit generate produces the diff.

<!-- DB env vars: -->
DATABASE_URL_DIRECT — used by admin CLI scripts (pg.Client, direct non-pooler connection)
DATABASE_URL — used by app runtime (neon-http, pooler)
Both are loaded from .env.local via: config({ path: '.env.local' }); config()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Drizzle schema + push migration to Neon</name>
  <files>lib/db/schema.ts, drizzle/0001_lesson_phase2_columns.sql, lib/db/__tests__/schema.test.ts</files>
  <behavior>
    - Test: lessons table export has recordingUrl column of type text (nullable)
    - Test: lessons table export has transcriptUrl column of type text (nullable)
    - Test: lessons table export has htmlTrainerPath column of type text (nullable)
    - Test: topic column still exists (regression guard — was already in Phase 1 schema)
    - Test: lessonStatusEnum still has all 4 values: scheduled, in_progress, completed, cancelled
  </behavior>
  <action>
1. Read lib/db/schema.ts to see the current lesson table shape. The topic column is already TEXT NOT NULL — do NOT add it again.

2. Add 3 nullable text columns to the lessons pgTable definition AFTER the existing htmlTemplateUrl line:
   ```typescript
   recordingUrl: text('recording_url'),          // Phase 10 will populate (D-11)
   transcriptUrl: text('transcript_url'),         // Phase 10 will populate (D-11)
   htmlTrainerPath: text('html_trainer_path'),    // Phase 7 will populate (D-13)
   ```

3. Update lib/db/__tests__/schema.test.ts — add 3 test cases asserting the new columns exist on the lessons table export (check that the drizzle ColumnBuilder is present). Follow the existing test pattern in that file.

4. Run tests to confirm RED (new column tests fail before adding columns) then GREEN after adding columns:
   ```
   npm run test -- --reporter=verbose 2>&1 | head -40
   ```

5. Generate the SQL migration (schema-only, no DB connection needed):
   ```
   npm run db:generate
   ```
   Drizzle-kit will produce drizzle/0001_*.sql with 3 ALTER TABLE ADD COLUMN statements.

6. Apply the migration to Neon via the existing custom push script:
   ```
   npm run db:push
   ```
   Expected output: each ADD COLUMN either "OK" or "SKIP (column already exists)". Verify tables output still shows all 6 tables.

7. Run typecheck:
   ```
   npm run typecheck
   ```
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|schema)" | head -20</automated>
  </verify>
  <done>
    - lib/db/schema.ts has recordingUrl, transcriptUrl, htmlTrainerPath on lessons table
    - 0001_*.sql file exists in drizzle/ with the 3 ADD COLUMN statements
    - npm run db:push exits 0 (columns applied or already present on live Neon)
    - npm run typecheck exits 0
    - All schema tests pass (no regressions on existing 6-test suite)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin CLI scripts + npm scripts wiring</name>
  <files>
    scripts/admin/create-user.ts,
    scripts/admin/create-lesson.ts,
    scripts/admin/list-users.ts,
    scripts/admin/list-lessons.ts,
    scripts/admin/__tests__/parse-args.test.ts,
    package.json
  </files>
  <behavior>
    - Test (parse-args): parseArgs(['--email', 'x@y.com', '--child-name', 'Маша', '--child-age', '10']) returns { email: 'x@y.com', 'child-name': 'Маша', 'child-age': '10' }
    - Test (parse-args): parseArgs(['--topic', 'Дроби']) returns { topic: 'Дроби' }
    - Test (parse-args): parseArgs([]) returns {}
    - Test (parse-args): parseArgs(['--flag']) returns { flag: true } (boolean flag without value)
    - Test (parse-args): parseArgs(['--a', '1', '--b', '2']) returns { a: '1', b: '2' }
  </behavior>
  <action>
1. Create scripts/admin/__tests__/parse-args.test.ts with 5 test cases (see behavior block). Run RED.

2. Create a minimal shared parser inline in each script — do NOT create a separate parse-args.ts module (D-10: native process.argv, single-file scripts). The parser is small enough to inline. The test file tests the function directly imported from create-user.ts using a named export `export function parseArgs(argv: string[])`.

   parseArgs implementation (simple, no yargs/commander per D-10):
   ```typescript
   export function parseArgs(argv: string[]): Record<string, string | true> {
     const out: Record<string, string | true> = {}
     for (let i = 0; i < argv.length; i++) {
       if (argv[i].startsWith('--')) {
         const key = argv[i].slice(2)
         if (i + 1 < argv.length && !argv[i + 1].startsWith('--')) {
           out[key] = argv[i + 1]
           i++
         } else {
           out[key] = true
         }
       }
     }
     return out
   }
   ```

3. Run tests GREEN after adding parseArgs to scripts/admin/create-user.ts.

4. Create scripts/admin/create-user.ts:
   - Imports: config from dotenv, Client from pg
   - Loads .env.local: `config({ path: '.env.local' }); config()`
   - Exported parseArgs function (from step 2)
   - Parses: --email (required), --child-name (optional, default 'Ребёнок'), --child-age (optional, default 10)
   - Validates --email present, else prints usage and exits 1
   - withClient helper (same pattern as scripts/seed.ts — new Client per call, DATABASE_URL_DIRECT)
   - Step 1: INSERT INTO allowed_email ON CONFLICT (email) DO NOTHING
   - Step 2: INSERT INTO "user" ON CONFLICT (email) DO UPDATE SET child_name, child_age, returning id
   - Step 3: Print success (D-discretion: use Unicode ✓/✗ glyphs, works on Windows PowerShell ≥7):
     ```
     ✓ Whitelisted email kid@example.ru
     ✓ Created/updated user (email: kid@example.ru, child: Маша, age: 10)
     ✓ Magic link: visit /login and enter this email to receive a login link
     ```
   - CLI entrypoint guard: `if (require.main === module) { ... }` or ESM equivalent with `argv[1]?.endsWith(...)` pattern matching scripts/seed.ts

5. Create scripts/admin/create-lesson.ts:
   - Parses: --email (required, identifies user), --topic (optional, default 'Тестовый урок'), --date (required, format YYYY-MM-DD), --time (optional, default '10:00', format HH:mm), --duration (optional, default 45), --trainer (optional, html_trainer_path), --status (optional, default 'scheduled')
   - Validates --email and --date present
   - Step 1: SELECT id FROM "user" WHERE email = $1
   - If user not found: print error and exit 1 (hint: run admin:create-user first)
   - Step 2: Parse scheduledAt = new Date(`${date}T${time}:00`) (local time; document in output)
   - Step 3: INSERT INTO lesson (user_id, scheduled_at, topic, duration_min, html_trainer_path, status) VALUES ... RETURNING id
   - No ON CONFLICT for lesson (lessons are not unique on any natural key; idempotency = re-running creates a new lesson, which is acceptable — documented in output)
   - Output:
     ```
     ✓ Lesson created (id: <uuid>)
       Topic: Дроби — введение
       Scheduled: Пт 15 мая 2026 в 16:00 (local time)
       User: kid@example.ru
     ```

6. Create scripts/admin/list-users.ts:
   - SELECT u.id, u.email, u.child_name, u.child_age, u.created_at, u.last_login_at FROM "user" u ORDER BY u.created_at DESC
   - Print as aligned table with headers: ID | Email | Child | Age | Created | Last Login
   - Truncate ID to first 8 chars for readability

7. Create scripts/admin/list-lessons.ts:
   - SELECT l.id, u.email, l.topic, l.scheduled_at, l.duration_min, l.status FROM lesson l JOIN "user" u ON u.id = l.user_id ORDER BY l.scheduled_at DESC
   - Print as aligned table: ID | Email | Topic | Scheduled | Duration | Status

8. Add npm scripts to package.json (per D-06, naming convention: admin:verb-noun):
   ```json
   "admin:create-user":   "tsx scripts/admin/create-user.ts",
   "admin:create-lesson": "tsx scripts/admin/create-lesson.ts",
   "admin:list-users":    "tsx scripts/admin/list-users.ts",
   "admin:list-lessons":  "tsx scripts/admin/list-lessons.ts"
   ```

9. Run tests to confirm all pass (unit tests for parseArgs; no integration test needed — create-user/create-lesson are validated by the E2E in Plan 03):
   ```
   npm run test -- --reporter=verbose
   ```

10. Run typecheck:
    ```
    npm run typecheck
    ```
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|parse-args|admin)" | head -20</automated>
  </verify>
  <done>
    - scripts/admin/__tests__/parse-args.test.ts: 5 tests all pass
    - scripts/admin/create-user.ts: runs without error (dry typecheck — no live DB call in tests)
    - scripts/admin/create-lesson.ts: runs without error
    - scripts/admin/list-users.ts: runs without error
    - scripts/admin/list-lessons.ts: runs without error
    - package.json has 4 new admin:* scripts
    - npm run typecheck exits 0
    - npm run test exits 0 (all existing + new tests pass)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → Neon DB | Admin CLI runs locally with .env.local credentials; DATABASE_URL_DIRECT only in local env |
| lesson table schema | New nullable columns have no NOT NULL constraint — Phase 10/7 will populate; no data integrity risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | DATABASE_URL_DIRECT in admin scripts | accept | Scripts run locally only; .env.local is gitignored; no web surface |
| T-02-02 | Tampering | admin:create-user inserts into allowed_email | accept | CLI requires local env with direct DB access — attacker who has DATABASE_URL_DIRECT already has full DB access |
| T-02-03 | Elevation of Privilege | admin CLI adds arbitrary email to whitelist | accept | Dev-only path per D-07; no web UI; physical access to .env.local required |
| T-02-04 | Denial of Service | create-lesson without --date validation causes invalid timestamp | mitigate | Validate --date format (YYYY-MM-DD) and reject with helpful error before INSERT |
</threat_model>

<verification>
After both tasks complete, the following must hold:

1. `npm run db:push` exits 0 — shows 3 ADD COLUMN statements as OK or SKIP
2. `npm run test` exits 0 — all tests pass (unit + parse-args)
3. `npm run typecheck` exits 0 — no type errors
4. Manual smoke (using live Neon):
   ```
   npm run admin:create-user -- --email smoke-test@example.ru --child-name "Тест" --child-age 11
   npm run admin:list-users
   npm run admin:create-lesson -- --email smoke-test@example.ru --topic "Дроби" --date 2026-06-15 --time 16:00
   npm run admin:list-lessons
   ```
   Expected: ✓ lines for each, list commands show the created rows.
5. Verify schema in live DB has the 3 new columns (db-push verification output confirms this).
</verification>

<success_criteria>
- lib/db/schema.ts: lessons table has recording_url, transcript_url, html_trainer_path (all nullable text)
- Live Neon DB: 3 new columns present in lesson table (confirmed by db:push verification step)
- 4 admin CLI scripts executable via npm run admin:*
- parseArgs unit tests: 5/5 pass
- npm run test: all tests green (no regressions)
- npm run typecheck: exits 0
- ACC-04 acceptance criterion "mechanism for creating a user + lesson" is met
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin/02-01-SUMMARY.md` using the template at `@C:/Users/krato/.claude/get-shit-done/templates/summary.md`.

Key items to capture in SUMMARY.md:
- Schema columns added (recording_url, transcript_url, html_trainer_path)
- Migration file name (drizzle/0001_*.sql)
- Admin CLI command signatures (for docs/admin-guide.md in Plan 03)
- Any Neon quirks encountered during db:push
- Test count delta (expected: +5 parse-args tests)
</output>
