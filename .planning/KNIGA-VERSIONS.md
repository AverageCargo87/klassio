# Урок-учебник `/kniga` — журнал версий

Зачем: по скриншоту должно быть однозначно видно, какую сборку смотрят. Номер стоит **в
заголовке вкладки** и **плашкой в нижней панели** урока (рядом с ценой прогона). Прошлые
версии не выбрасываются — они лежат рядом и открываются по своему адресу, чтобы «было и
стало» можно было показать бок о бок, а не по памяти.

| версия | дата | адрес | состояние |
|---|---|---|---|
| **v1.0** | 03–04.08.2026 | `http://localhost:8781/kniga/v1` | заморожена, правкам не подлежит |
| **v2.0** | 05.08.2026 | — | не архивировалась (см. ниже) |
| **v2.1** | 05.08.2026, вечер | — | не архивировалась |
| **v2.2** | 05.08.2026, ночь | — | не архивировалась |
| **v2.3** | 06.08.2026 | `http://localhost:8781/kniga/v23` | заморожена |
| **v2.4** | 06–07.08.2026 | — | не архивировалась (v2.5 легла сверху в тот же круг) |
| **v2.5** | 08–10.08.2026 | — | адресом не заморожена, но **лежит релизом `0002` на сервере**: `node scripts/lab-rollback.mjs --to 0002` возвращает её целиком |
| **v2.6** | 11.08.2026 | `http://localhost:8781/kniga/v26` | заморожена целиком (свои данные в book/v26) |
| **v2.7** | 13.08.2026 | — | не архивировалась (v2.8 легла сверху в тот же день) |
| **v2.8** | 13.08.2026, вечер | — | не архивировалась: наружу не выкладывалась, v2.9 легла сверху |
| **v2.9** | 17.08.2026 | `http://localhost:8781/kniga` | текущая |

Правило нумерации: **старшая цифра** — смена формы или круг фидбэка руководителя, **младшая** —
доводка внутри круга. Урок всегда открывается по `/kniga`; `/kniga/vN` — архив.

⚠️ v2.0 отдельным адресом не заморожена: правки того же дня легли поверх — карта пересобрана
своим скриптом, привязки перестроены. Сравнивать есть с чем и так: `/kniga/v1` — ровно то, что
видел руководитель, а всё, что после, идёт от его четырёх пометок. На будущее: если сборку
показали кому-то за пределами команды — заморозить сразу, до первой правки.

---

## v2.10 — учителя выбирают лицом, а не списком (20.08.2026)

**Выбор учителя переехал из шестерёнки на само окно Ани — кнопкой с лицом.** Раньше
учителя меняли списком имён в настройках в другом конце экрана: «Liv», «Chloe» — по этим
словам лицо не выбрать, а смотришь при этом совсем в другую сторону.

- **Кнопка стоит на окне учителя**, и на ней — лицо того, кто ведёт, и его имя. Видно,
  кто в кадре, ещё до того, как что-то открыл.
- **Панель выбора — карточки с фотографиями**, пропорция та же (3:4), в которой лицо
  потом и ведёт урок: выбранное на карточке и увиденное в уроке — одно и то же.
- **Наведи на карточку — лицо оживёт.** Играет шестисекундная запись того, как оно
  выглядит, когда МОЛЧИТ. Это и есть главное при выборе: ровно на неподвижном лице между
  репликами провалился прошлый движок.
- **Десять карточек:** пять лиц Anam — те самые, что уходили вам пробами 19 августа;
  лицо bitHuman; две бесплатные 3D-учительницы; записанный урок; «без аватара». На каждой
  написана цена за минуту, чтобы выбор был не только по красоте.
- **Голос выбирается в той же панели** — пять голосов, у каждого «послушать». Лицо и
  голос звучат вместе, значит и выбираться должны вместе.
- **Видео Ани само выключается через 2 минуты** и когда вы уходите со страницы —
  видео-аватар берёт деньги за всё время, включая молчание. Урок при этом не
  останавливается: он идёт дальше голосом, а на окне появляется кнопка «включить снова».
- **bitHuman остался одной карточкой, а не двумя.** Живьём у них работает только одно
  лицо; второе (Essence 2) на нашем сервере даёт чёрное окно — их движок так не умеет,
  проверено 19 августа. Показывать в списке неработающее нечестно.

Вместе с этим на витрину наконец выложено всё, что делалось с 11 августа: **v2.7** — Аня
слушает сама и помнит, где ученик; **v2.8** — урок доходит до конца, а тренажёр мерит
знание, а не угадывание; **v2.9** — лицо двигается вместе со звуком.

### Что накопилось с v2.6 — той сборки, что стояла на витрине

Четыре круга правок делались, но на сервер не выкладывались.

- **v2.7 (13.08)** — круг по пятнадцатиминутной записи разбора: Аня слушает сама (кнопка
  «Говорить» больше не условие), чтение вслух засчитывается, урок помнит, где ученик, и
  не спрашивает о непройденном и не ссылается на неоткрытые страницы; карта больше не
  мечется, добавлена обзорная карта Европы.
- **v2.8 (13.08, вечер)** — урок доходит до конца: сроки на всех четырнадцати сетевых
  вызовах и сторож «застрял на шаге N», отказ голоса не убивает занятие, прогресс
  переживает F5. Тренажёр стал мерить знание: зелёное только за ответ без подсказки,
  сначала попытка — потом разбор.
- **v2.9 (17.08)** — «звук идёт, а она не двигается» вылечено линейкой, а не на глаз:
  раннер прогревается заранее, повторные кадры выбрасываются, звук не начинается, пока
  не пришёл первый живой кадр.
- **19.08** — bitHuman заменён на **Anam**: лицо считается из звука целиком, и верх лица
  (брови, глаза) двигается **вчетверо сильнее**, замерено `scripts/face-liveness.py`.
  Подробности — `.planning/SESSION-2026-08-19-ANAM.md`.

### Как это сделано

| что | где |
|---|---|
| каталог лиц и отрисовка карточек | `ЛИЦА[]`, `faceРисуй`, `faceПоставь` в `kniga.html` |
| картинки карточек: портрет 3:4 и петля простоя | `scripts/make-avatar-cards.mjs` → `lica/<ключ>.jpg` + `.mp4` |
| отдача карточек сервером (узкий шаблон имени) | ветка `/lica/…` в `scripts/yandex-test-server.mjs` |
| три сторожа минут | `anamСторож`, `anamВыключи`, `ANAM_ПРЕДЕЛ` |
| ключ Anam на сервере — в `shared/`, вшивается ссылкой | `link_shared` в `scripts/lab-common.mjs` |

### Сторожа минут — подробно

Anam берёт деньги за **всю сессию, включая молчание**, а в уроке из сорока минут чистой
речи только пятнадцать. Открытая вкладка на паузе жжёт пакет так же, как урок. Три
независимых сторожа:

1. **потолок сессии — 2 минуты.** Он уходит В САМ ТОКЕН, поэтому Anam закроет сессию,
   даже если вкладку убили вместе с ноутбуком. Свой сторож в такой ситуации уже не поможет;
2. **простой — 45 секунд** без речи. Тишину считаем от конца поданного PCM: события
   «договорила» у Anam нет вовсе;
3. **ушёл — 20 секунд**, если вкладка не на виду.

Потолок поднимается адресом `?anamsec=600` — это для наших прогонов, не для показа.

### Приёмка

Новый файл `scripts/check-kniga-vybor.mjs` — **27 проверок**: кнопка внутри рамки окна
(а не в стороне), на кнопке лицо и имя, все картинки карточек реально отдаются, выбор
доезжает до урока (сменили карточку — сменился режим и лицо, а не только рамка), голос
переживает перезагрузку, и потолок минут вшит в запрос токена. Остальные файлы приёмки
прогнаны — расхождений с тем, что было до правки, нет.

---

## v1.0 — «учебник читает» (03–04.08.2026)

Первая целая сборка формы «учебник-листалка + ИИ-учитель» — та, которую вы смотрели первой.

- Аня читает страницу вслух и подчёркивает ровно те слова, о которых говорит.
- Справа — картинка к сказанному и карточки «запомни».
- В конце страницы проверка: прочитай сам → перескажи своими словами → тест по памяти.
- Учитель на выбор: 3D, записанные ролики, видео-аватар или без аватара.

### Подробно

**Что в ней есть**
- Одна страница учебника в центре (не разворот) + рабочая панель справа: верх — показ того,
  о чём говорят, низ — карточки «запомни» по страницам.
- Сценарий не пишется руками: 70 тактов собираются из текстового слоя PDF §20 (стр. 120–125).
- Подсветка идёт за голосом: подчёркивание, маркер, прожектор на картинку.
- Проверка каждой страницы: прочитай сам → перескажи (судит DeepSeek по карточкам) → тест при
  закрытой странице → адресный повтор того куска, где ошибся.
- Учитель слева в четырёх режимах: 3D (0 ₽) · записанные ролики (0 ₽) · bitHuman (~1.7 ₽/мин) ·
  без аватара. Голос — 9 женских голосов Яндекса на выбор.
- Картинки учебника кликаются и открываются во весь экран (апскейл 2560 px).

**Что по ней сказал руководитель** (4 пометки на скриншотах, 04–05.08):
1. Показ не совпадает с тем, что звучит: Аня читает про части Греции и Аттику, а в панели —
   море с кораблём. «Визуализация должна быть связана с воспроизводимым текстом».
2. Шаг «прочитай сам» с кнопкой «прочитал» — не чтение. Ребёнок должен читать **вслух**, а
   система — идти за голосом и писать транскрипцию на экране.
3. Перед тестом нужно **разобрать вопросы вместе с учителем** в почти тестовом виде, и только
   потом закрывать страницу и спрашивать по памяти.
4. Одной читки мало для запоминания: показывать карту, разжёвывать несколько раз («вот Аттика,
   вот тут, рядом с Афинами»), давать интерактив — пазл «расставь названия на карте».

---

## v2.0 — «показ по фразе, чтение вслух, разбор перед тестом» (05.08.2026)

Закрывает пункты **1, 2, 3**. Пункт 4 сознательно отложен: он единственный требует добавлять к
тексту учебника свой материал, а на этом держится главное свойство формы — 90 % времени звучит
учебник, то есть стопроцентный источник истины, и это то, что масштабируется на другие
параграфы и учебники. Обсуждается отдельно.

### 1. Показ идёт за фразой, а не за тактом

Причина претензии оказалась механической. Такт — это до 380 знаков, три-четыре предложения;
показ вешался на весь такт целиком и вставал в его начале. Пока Аня доходила до Аттики, на
экране висела картинка, подобранная к первой фразе такта.

Теперь у каждой привязки считается её **место в реплике** (доля знаков до неё), и показ
переключается, когда голос до этого места дошёл. Доля прочитанного уже была — та самая, что ведёт
линию под строкой; второго источника правды не появилось. Карточки «запомни» считаются по КОНЦУ
фразы: факт должен сначала прозвучать.

Привязок стало **120 вместо 44** (6 агентов разложили по странице каждый, ещё 6 придирчиво
сверили с текстом учебника и вычистили добавки сверх него). Замер до и после на тех же правилах:

| | было (v1.0) | стало (v2.0) |
|---|---|---|
| тактов с топонимом и без карты | 27 | 0 |
| длинных тактов на одной картинке | 23 | 0 |
| тактов, начинающихся с чужой картинки | 12 | 0 |

### 2. Карта Греции

🔑 **В учебнике карты нет** — проверен весь PDF (302 страницы): ни растровой, ни векторной.
Поэтому карта своя, и чтобы она не стала «своим материалом»:
- подписи — **только те названия, что звучат в §20** (в том числе «Лаконика», как в учебнике,
  а не «Лакония», как на большинстве карт);
- **никаких границ государств**: рисуется физическая карта, потому что современная Греция ≠
  Древняя Греция и рисовать её границы значило бы сказать ребёнку то, чего в учебнике нет;
- три части Греции подсвечиваются зонами, обрезанными по берегу, а не полигонами «как было на
  самом деле»: учебник даёт их приблизительно, и карта не должна врать точностью.

Берега — Natural Earth 10m, public domain. Собирает `scripts/make-map-greece.mjs`, живёт в
`book/map-greece.svg`. Это живой SVG: у него загорается названное место и он наезжает на него,
поэтому под фразу «Аттика с городом Афины» видно и Аттику, и Афины рядом, и соседние области.

### 3. Чтение вслух с транскрибацией

Было — кнопка «прочитал», то есть честное слово ребёнка. Стало: он читает вслух, микрофон пишется
непрерывно, а на распознавание уходит кусками **по паузам** — конец фразы естественная граница,
поэтому строка появляется на экране почти сразу, а не в конце абзаца. Услышанное сверяется с тем
самым куском учебника, который подсвечен, и **линия под строкой едет за голосом ребёнка** так же,
как ехала за голосом Ани. Сверка цепляется только за слова от четырёх букв: на коротких («и»,
«мы», «во») приёмка поймала, как посторонняя болтовня закрывала восьмую часть куска.

Обход остался: «прочитал про себя» — для ребёнка без микрофона или который читать вслух стесняется.

### 4. Разбор вопросов перед тестом

Между пересказом и тестом появился этап **«РАЗБЕРЁМ ВОПРОСЫ»**: те же вопросы, тот же вид, что в
тесте, но страница ОТКРЫТА и ответ можно найти в тексте. На каждый ответ Аня показывает, где это
написано: подсвечивает кусок на странице и зачитывает ровно то предложение, в котором ответ.
После этого страница закрывается и те же вопросы идут по памяти.

Тест перестал быть замером знаний и стал инструментом запоминания — это и просили: «ребёнок
должен элементарно запомнить текст».

### Приёмка

```
node scripts/check-kniga.mjs           # 44 (было 40): + показ меняется ВНУТРИ такта, карта
node scripts/check-kniga-panel.mjs     # 215 (было 133): + плотность, место привязки, «звучит место — на экране карта»
node scripts/check-kniga-control.mjs   # 40 (было 27): + чтение вслух и разбор перед тестом
node scripts/check-kniga-figures.mjs   # 20
node scripts/make-map-greece.mjs       # пересобрать карту
node scripts/build-kniga-panel.mjs <журнал> # пересобрать привязки из раскладки агентов
```

Три правила приёмки написаны так, чтобы претензия не вернулась: показ обязан меняться внутри
длинного такта, первый показ такта не может стоять во второй его половине, и там, где
**рассказывают про место**, на экране обязана быть карта. Последнее срабатывает не от всякого
упоминания: «ворота, увенчанные фигурами львов» — про вещь, и там правильная картинка сама вещь.

### Найдено и починено по дороге
- Карточка, привязанная к последним словам такта, могла не появиться совсем: последний кадр до
  конца звука приходит на доле 0.98, а её место 1.0. Теперь после каждой реплики план досыпается.
- Приёмка картинок (`check-kniga-figures`) падала ещё до этой сессии: её писали под разворот, а
  страница с 03.08 одна — локатор ждал картинку, которой на экране нет.

---

## v2.1 — доводка по прогону (05.08.2026, вечер)

Кратов прошёл урок целиком и дал семь правок. Все закрыты.

**Карта не влезала в коробку.** «Справа почему-то обрезано всё»: окно карты было почти
квадратным (1.13:1), а коробка показа широкая (1.6:1) — карта вписывалась по высоте, занимала
две трети ширины, остальное было пустой водой. Окно подогнано под коробку (18.6–30.8° в. д.,
34.4–40.6° с. ш.): север обрезан — Болгария к Древней Греции отношения не имеет, — зато влез
берег Малой Азии. Поля по краям: было 88 px, стало 9.

**«Хочется чуть более дорого».** Море глубже, суша теплее, берег темнее — вода и земля больше не
сливаются в одну пастель; под сушей мягкая тень (подводный склон) даёт глубину, которой не бывает
у плоской заливки. Подписи крупнее, с разрядкой, активная — тёмно-красная. Наезд стал 2.4 с с
мягким разгоном, зоны загораются за 1.1 с. Подпись при наезде растёт по корню масштаба: в лоб
она застывала, и наезд переставал читаться как «вот сюда смотри».

**Картинки повторялись.** Иллюстраций на параграф 28, показов больше сотни — какие-то неизбежно
идут не по разу. Но глаз замечает не «использована дважды», а ВОЗВРАТ: А → Б → А. Чинилось с двух
сторон:
- у той же картинки теперь меняется только ПОДПИСЬ, показ не пересобирается — половина «повторов»
  была именно этим (картинка моргала и заново начинала наезд, хотя разговор шёл про то же самое);
- возвраты вычищены из данных (`scripts/thin-kniga-panel.mjs`) — 13 штук. Считаются они по
  СЕМЬЯМ, а не по именам файлов: «лодки два раза» — это три разных файла (корабли Миноса, Греция
  у моря, картинка учебника «Природа Греции», где тоже лодка у берега).

**Мегарон, фреска, Минос.** У каждого нового слова теперь своя иллюстрация, и она успевает
уложиться: показ держится не меньше 1.8 с, а короткая реплика с картинками держит паузу по их
числу. Под «фреску» встала НАСТОЯЩАЯ фреска из учебника — дельфины со стр. 122, поднятая
апскейлом (2 кредита), она же теперь кликается на самой странице. Под «Минос» — нарисованный
портрет царя в тронном зале Кносса (2 кредита), он же заменил «царя-жреца вообще» на 122-й.
Определений сверх учебника не появилось: на 120-й эти слова стоят на полях как новые, а разбор
у них там, где его даёт сам параграф (мегарон — на 124-й, фрески — на 122-й).

**Транскрипция при чтении вслух** выкладывается по одному-два слова с проявлением, а не падает
абзацем после каждой паузы.

**Разбор перед тестом больше не спрашивает дважды.** Было: ребёнок выбирает ответ на разборе, а
через минуту те же четыре вопроса идут ещё раз при закрытой странице — «это как-то перебор». Стало:
на разборе Аня сама показывает вопрос в тестовом виде, называет верный вариант и место в тексте, а
отвечает ребёнок один раз — в тесте, по памяти. Подводки и реакции больше не повторяются слово в
слово (по четыре варианта на «верно», «не так» и на разбор).

**Чужая картинка после переворота.** «Перевернул страницу — и там зачем-то картинка с лабиринтом»:
показ оставался от прошлой страницы, пока на новой не прозвучит первая привязанная фраза. Теперь
при смене страницы он гаснет: пустое место честно говорит «сейчас появится», а чужая картинка врёт
про то, что читают.

### Что поймала приёмка по дороге
- Троттлинг показа (полторы секунды) сначала работал по правилу «побеждает последний» — и приёмка
  тут же поймала, что из пяти показов реплики на экран доезжают два. Сделана очередь, которая
  держит края и выбрасывает середину.
- Приёмка мерила показ шагами по 200 мс — это в сто раз быстрее речи, и она ловила не поведение
  урока, а троттлинг. Теперь шаги в темпе речи, а ожидание — по длине очереди показов.

## v2.2 — второй прогон (05.08.2026, ночь)

Шесть правок, все закрыты.

**Наезд карты уводил подпись за край.** «Говорит про Коринфский перешеек и зумит карту, а
подписи не видно»: наезжали на ТОЧКУ места, а у половины меток подпись отодвинута выноской —
её и выносило за кадр. Теперь на вход идёт рамка «точка вместе со своей подписью», и масштаб
подбирается так, чтобы рамка легла в коробку с запасом (не больше 78 % по стороне, потолок 2.4×).

**Подписи дрожали при смене наезда.** Кегль подписи задаётся переменной, урок пересчитывает её
при каждом наезде — и менял СКАЧКОМ, поверх карты, которая едет две с лишним секунды. Теперь
переменная зарегистрирована (`CSS.registerProperty`) и едет тем же переходом, что и наезд; свой
переход по кеглю у подписи убран — два перехода на одном свойстве и давали рывки.
⚠️ Регистрировать её внутри SVG нельзя: `syntax:'<length>'` в `<style>` ломает файл как XML —
угловые скобки разбираются как тег, и вместо карты остаётся чёрное место. Поймано глазами на
скриншоте, стандартной приёмкой — нет.

**Картинка держится 2.5 с** вместо 1.8; короткая реплика с несколькими картинками держит паузу
по их числу.

**Транскрипция склеивалась в одно длинное слово.** Слова выкладываются по одному, и пробел жил
хвостом ВНУТРИ слова — а у `inline-block` концевой пробел схлопывается. Пробел стал отдельным
текстовым узлом. Приёмка меряет не текст, а расстояние между двумя словами на экране.

**Вопрос прыгал при ответе.** Содержимое теста центрировано, и строка итога, появляясь снизу,
сдвигала вопрос вверх. Место под неё держится с самого начала.

**Пауза — две поломки сразу.** Пауза звала `cutVoice()`, то есть тот же слом, что «остановить»:
он ДОигрывал обещание, на котором ждёт реплика, и прогон считал такт дочитанным — после
«продолжить» урок начинал со следующего. Плюс картинки продолжали меняться по своим таймерам
«не в попад». Теперь пауза только приостанавливает звук (и очередь показов), а продолжение идёт
с того же места. ⚠️ Сторож конца реплики пришлось научить молчать на паузе: он ловит
`audio.paused && currentTime>0` — ровно то состояние, в которое ставит пауза.


## v2.3 — прогон 06.08

- **Вопросы самого учебника задаются первыми**, а ответа ждут в закреплении — а не посреди чтения.
- **Кнопка «следующий вопрос»** после верного ответа: можно не дослушивать.
- **Подсветка совпадает с тем, что звучит** — до предложения, а не целым абзацем.
- Метки пересказа «назвал / забыл» перестали пропадать.
- Подрезаны тёмные поля у трёх иллюстраций.

### Подробно

**Вопросы учебника идут первыми.** «Сначала пусть задаёт те вопросы, которые есть в самом
учебнике, а потом всё остальное». Раньше вопрос учебника звучал ПОСРЕДИ чтения и микрофон
включался прямо там — со стороны это читалось как «спросила и не дала ответить». Теперь вопрос
по-прежнему зачитывается на своём месте (это текст учебника), но ответа ждут в закреплении:
там он первым шагом, до нашей проверки, с кнопкой «ответить», видимым «слушаю» и «пропустить».
⚠️ Приёмка поймала настоящую поломку: у живых тактов нет поля n (номер вычисляется только
при выгрузке сценария), и сравнение по нему давало undefined===undefined — второй вопрос
страницы молча считался дублем первого. Копим сами такты, а не номера.

**Кнопка «следующий вопрос» в тесте.** Ответил верно — можно не дослушивать: кнопка глушит
реплику на полуслове и ведёт дальше. Ответил неверно — кнопки нет: объяснение и есть смысл шага.
Тем же приёмом стала прерываться и реплика на разборе.

**Подсветка совпадает с тем, что звучит.** На разборе Аня читает ОДНО предложение, а подсвечивался
весь такт. Теперь такт умеет обрезаться до предложения: у него сохранена разметка предложений и
строк, и подсветка считается той же функцией, что при нарезке.

**Метки пересказа перестали пропадать.** Итог «назвал / забыл» жил только в DOM, и первая же новая
карточка стирала его перерисовкой списка. Теперь он хранится отдельно и переживает перерисовку.
Приёмка ловила это как «0 назвал / 0 забыл» через раз — то есть дефект был плавающий.

**Тёмные поля у иллюстраций.** «Картинка с Минотавром кривая»: у неё сверху 177 пикселей почти
чёрного потолка, и в широкой коробке это читалось как полоса. Подрезано у трёх картинок
(минотавр, флот, таблички), оригиналы в .tmp/art-orig/.

## v2.4 — цепочка руководителя (06–07.08)

Дословная постановка: «прочитали материал → сделал короткое **самари** → попросил
**прочитать самари** → выделили тизеры → **объяснил детально тизеры** → попросил своими
словами рассказать, что запомнил → объяснил тесты». Плюс три пометки со скриншотов:
карточки слева должны быть **с картинками**, разбор топиков — на **отдельном экране**, а на
шаге «прочитай вслух» (он обвёл там пустое место) — **интерактив с открывающимися
картинками**. Закрыто всё четыре.

**Короткое самари.** Новый шаг перед чтением вслух: 3–5 предложений, каждое подсвечивается
и на самой странице, а в коробке показа идёт картинка того топика, о котором фраза.
🔑 Самари **не пишется нами** — это те самые предложения учебника, в которых факты и
сказаны. Иначе рушится главное свойство формы: «90 % времени звучит учебник, а учебник —
стопроцентный вариант истины». Приёмка проверяет это отдельной строкой: каждая фраза самари
обязана дословно находиться в тактах параграфа.

**Читают теперь самари, а не случайный кусок.** Эталон сверки — ровно текст самари.

**Мозаика.** Справа, где было пустое место, выкладываются закрытые плитки; каждая
открывается на своей доле прочитанного и показывает картинку того топика, о котором в этом
месте текста и говорится. Ничего нового не добавляется — под плитками те же показы урока,
так что награда честная: ребёнок открывает визуальный ряд страницы, который через минуту
разбирается поимённо.

**Экран топиков.** Книга уезжает, сцену занимают три колонки: слева оглавление из тех же
карточек, в центре крупный визуальный ряд, справа — термин, короткая мысль и дословная
строка учебника. Проходится кнопкой по одному топику.

**Миниатюры на карточках «запомни».** Картинка не случайная: это тот же показ, что стоял на
экране, когда факт прозвучал. У «мест на карте» картинки быть не может (карта — живой SVG с
наездом), там стоит значок.

**Приёмка выросла с 327 до 363 проверок** — прибавились самари, мозаика, экран топиков и
миниатюры.

```
node scripts/check-kniga.mjs           # 53
node scripts/check-kniga-control.mjs   # 82
node scripts/check-kniga-panel.mjs     # 207
node scripts/check-kniga-figures.mjs   # 21
```

### Грабли, купленные в этот круг

- **Пустой топик виден именно на новом экране.** У «Камня и мрамора» (стр. 121) автомат не
  нашёл ни одного показа: со словами «камень» и «мрамор» ни один не пересекается ни единым
  словом. На карточке это была мелочь, а на экране топиков — коробка с надписью «БЕЗ
  КАРТИНКИ» посреди экрана, который весь про визуальный ряд. Взята **собственная
  иллюстрация учебника** со стр. 121 («Природа Греции»). ⚠️ Соседний показ «В ГРЕЦИИ ВСЁ
  ЕСТЬ» стоит на той же картинке, но он про фразу Чехова, а не про строительный камень:
  подставить его — это ровно «говорит про бухты, а показывает жаркое лето».
- **Коробка самари прокручивается, и звучащая фраза уезжала за нижний край.** Голос идёт, а
  подсветки не видно — тот же дефект, что чинили на странице в v2.3, только внутри коробки.
  Теперь фраза подтягивается в вид.
- **На 1280×720 (ноутбук руководителя) самари обрезалось на полуслове**: из 204 px было
  видно 121. Место уступает мозаика (`#demo.mos`), потому что текст — задание, а мозаика —
  награда. Стало скрыто 4 px вместо 83. Экран топиков на этом окне садится без переполнения.
- **Шестерёнка настроек висит `fixed` в углу ОКНА** и не знает про новый экран — села ровно
  на подпись «стр. N» правой колонки.
- **Приёмка молча отстала от урока.** Она не знала про новые шаги, вставала на самари в
  ожидании кнопки, которую не жмёт, и сыпалась дальше каскадом — со стороны это выглядело
  как поломка урока, вплоть до «страница упала». ⚠️ Из этого же: у проверки «0 из 0» всегда
  зелёная — читать состояние нового экрана надо ПОСЛЕ того, как он появился, а не в момент
  смены этапа (экран включается позже: сначала Аня говорит «убираю страницу»).
- **Промах обязан печатать то, по чему падает.** «Картинка открывается переходом» показывала
  список переходов, а падала по фону подложки (0.47) — диагноз читался как загадка. Ждать
  надо ОБА события: картинка догрузилась и подложка дотемнела.
- **Считать надо номера, а не тексты.** Вопросы теста слипались в один, потому что ключом
  был текст вопроса, а он в момент замера бывает ещё пустым. То же с вопросами учебника: без
  дедупликации один вопрос засчитывался дважды и второй оставался без ответа — урок вставал.
- **Список карточек нельзя читать на пересказе**: их там прячут, и гонка давала судье
  несуществующий номер, а на экране — «0 назвал / 0 забыл» при исправном разборе.

## v2.5 — методичка проведения урока (08–10.08)

Источник: документ «Промпт3» от руководителя (08.08) — 35 требований к методике урока, плюс
переписка того же вечера: «материал учебника не всегда понятен, видимо предполагается, что
его комментирует учитель вживую».

🔑 **Как разрешено главное противоречие.** Методичка требует объяснений — «причина ошибки»,
«как решать», «наводящие», «рекомендации». Форма урока держится на обратном: 90 % звучит
учебник, своего не добавляем. Выход: **пояснения не сочиняются, а собираются из того, что уже
привязано к тексту** — чужая короткая мысль служит разбором неверного выбора, а разъяснение
это дословное предложение учебника, где ответ и сказан. Своего в тренажёре ровно два сорта
строк: формулировки заданий и порядок действий, то есть механика, а не материал.

**Что сделано**

- **Тренажёр раздела вместо теста.** 6 заданий на раздел: 3 на активный словарь и 3 на
  события. Три типа: слово → значение, значение → слово, соединить пары мышью. Перед
  заданиями — короткий показ, как каждый тип решается.
- **Три попытки с растущим разбором.** Первая ошибка — короткая причина; вторая — как
  решать; третья — правильный ответ вместе со строкой учебника. Верный вариант до третьей
  попытки не подсвечивается, иначе вторая теряет смысл. Разбор ПЕРЕЖИВАЕТ перерисовку:
  ребёнок читает его, пока делает следующую попытку.
- **Замена проваленного.** Задание, не взятое с трёх попыток, дописывается в конец очереди
  похожим из запаса (22 запасных на параграф).
- **Тупиков нет.** Ни одна ветка не запирает урок: после третьей попытки идём дальше сами,
  время на задание ограничено тремя минутами, по его истечении задание закрывается разбором.
- **Счётчик и часы всё время видны** в нижней панели: верные, неверные, время урока, а на
  задании — обратный отсчёт.
- **Звук и значок** на верный (короткое восходящее трезвучие) и неверный (мягкий низкий)
  ответ. Файлов нет намеренно — стенд обязан работать без сети.
- **Кнопка «запросить подсказку»** — голосом и текстом, ответа не раскрывает.
- **Каждые 15 минут** — колокольчик и вежливая реплика «прошло столько-то, давай чуть
  бодрее», четыре разные формулировки по кругу.
- **Чтение вслух стало условием.** Меньше половины — просят дочитать и к заданиям не
  пускают; ошибки при чтении не учитываются.
- **Итоги урока**: таблица по разделам (задания, верно, неверно, с 1-й, со 2-й, замен,
  время), точные дата и время начала и окончания, среднее на раздел и на задание, блок «на
  чём сосредоточиться» из фактов прогона. Влезает в один экран 1280×720.

**Приёмка выросла с 363 до 422 проверок**, добавился пятый скрипт:

```
node scripts/make-drill-data.mjs        # пересобрать задания (36 + 22 запасных)
node scripts/check-kniga.mjs            # 53
node scripts/check-kniga-control.mjs    # 80
node scripts/check-kniga-drill.mjs      # 61   ← новый, гоняет тренажёр целиком
node scripts/check-kniga-panel.mjs      # 207
node scripts/check-kniga-figures.mjs    # 21
```

### Где мы отступили от буквы методички — и почему

- **Разделов шесть, а не пять.** Раздел = страница параграфа; §20 занимает шесть страниц.
  Резать текст учебника на ровно пять кусков значило бы кроить его под цифру.
- **«Прочитал про себя» остался.** Методичка запрещает пускать дальше без чтения вслух, но
  она же запрещает тупики: без микрофона ребёнок оказался бы заперт. Порог в половину
  требуется на голосовом пути, обход сохранён.
- **Реплика «прошло 15 минут» звучит МЕЖДУ заданиями**, а не ровно по таймеру: врезаться в
  середину вопроса нельзя, реплики наложатся друг на друга.
- **Письмо родителю не отправляется само** — готовится черновик и текст в буфере. Рассылка
  от имени урока делается с ведома человека.
- **v2.4 отдельным адресом не заморожена**: v2.5 легла сверху в тот же круг. Последний
  архив — `/kniga/v23`.

### Вычитка боевого сайта (10.08, после выкладки)

Полный прогон урока на боевом адресе с ловлей битых запросов и ошибок консоли:
`node .tmp/probes/walk-prod.mjs`. Итог — 6 разделов, 36 заданий, **0 ошибок страницы,
0 битых запросов**. Что нашлось и поправлено:

- 🔴 **«Мегарон и фреска» попала в задание.** У этой карточки пояснение — «Два новых
  слова, встретим их дальше»: это анонс, а не определение. В сопоставлении ребёнку
  предлагалось соединить тему с анонсом. Такие карточки в задания больше не берутся.
- 🔴 **Почти все темы — не «слова»** («Три части Греции», «Оливы и виноград»), а
  формулировка была «в уроке встретилось слово „…“». Стало «„…“ — что это значит?».
- 🔴 **Тавтологичный вопрос**: «Самая ранняя цивилизация Европы жила на Крите» → ответ
  «Первая в Европе». Ответ содержался в самом вопросе. Термин для такого вопроса теперь
  выбирается по непересечению с собственным описанием.
- **Отвлекающие варианты брались с ЧУЖИХ страниц** и отбрасывались не думая («Что значит
  „Горы Греции“?» → «Так греки звали родину»). Теперь сначала соседние темы той же
  страницы — вариант приходится читать. Методичка прямо запрещает примитивные задания.
- **Пунктуация и обрывы**: «…ориентирами.».» и «…Высшая —…».
- 🔴 **Три темы показывали КАРТУ вместо сути** («Упадок Микен», «Переняли у минойцев»,
  «Дорийцы»), и четыре картинки работали на две темы каждая — на экране топиков подряд шли
  разные темы с одинаковым изображением. Дорисовано 5 иллюстраций (Higgsfield, 2 кредита
  за штуку), «Дорийцам» привязан уже существовавший показ. Теперь карту показывают только
  те три темы, которые и правда про места.
- 🔴 **На разборе вопросов варианты выглядели нажимаемыми**, хотя отвечать там нельзя по
  замыслу: ребёнок жмёт, ничего не происходит, читается как поломка. Стали пунктирными и
  приглушёнными, подпись прямо говорит, что отвечать будем в тренажёре.
- **Нижняя панель разъезжалась на две строки** — строка состояния и цена растут по ходу
  урока и выдавливали плашку версии. Обрезка многоточием, счётчик символов ушёл в
  подсказку, кнопка «замечание» переехала в левую группу. Проверено прогоном: за весь урок
  панель ни разу не переносится.
- **Запрос за роликами давал 404** на каждом открытии. В пакет кладётся пустой индекс:
  урок понимает, что роликов нет, и убирает из настроек пункт «записанный урок» — иначе
  руководитель выбрал бы режим, в котором учитель молча пропадает.

### Грабли, купленные в этот круг

- 🔴 **Занятый идентификатор.** Новый счётчик получил `id=hud` — а так уже звалась панель
  настроек. Счётчик уехал в правый верхний угол, стал открываться шестерёнкой и накрыл
  итоговый процент. ⚠️ И продолжение: массовая замена по тексту файла переименовала заодно
  правило, которым прячется сама панель настроек, — она распахнулась поверх урока. Правка
  «переименовать везде» обязана заканчиваться проверкой того, что рядом.
- 🔴 **Занятый КЛАСС — та же болезнь, что занятый id.** Значок ответа получил класс `.mk`,
  а так уже звались метки подсветки страницы, и у них `position:absolute`. Галочка легла
  ПОВЕРХ текста варианта: «Эллада» читалась как «✓лада». ⚠️ Правило на будущее: перед новым
  коротким именем (`mk`, `hud`, `tick`) — поиск по файлу, он один на весь урок.
- **Кнопка уезжала за нижний край.** На третьей попытке в панели одновременно жили задание,
  варианты, прошлый разбор, правильный ответ и кнопка — на 720 px кнопка оказывалась за
  краем, и урок выглядел запертым. Панель тренажёра прокручивается, а разбор второй попытки
  убирается, когда появляется сам ответ.
- 🔴 **Нажатия терялись.** Урок ждал ровно одно нажатие; всё, что ребёнок нажимал, пока Аня
  говорит или пока шаг заново встаёт в ожидание, проваливалось в никуда. На тесте это было
  почти незаметно (одно нажатие на вопрос), а на сопоставлении убийственно: пары кладутся
  подряд, половина кликов исчезала — задание «не работает». Теперь лишние нажатия ждут
  очереди, а при смене экрана очередь чистится.
- **Разбор ошибки жил ровно столько, сколько его читают.** Следующая попытка перерисовывала
  задание и стирала объяснение — ребёнок пробовал снова, уже не видя, что ему сказали.
- **Сопоставление сбрасывалось целиком.** После ошибки все пары раскладывались заново, в том
  числе верные, — ровно то, что методичка запрещает («не терять время на перепроверку
  правильно отвеченной части»). Верные пары теперь остаются с галочкой и заперты.
- **Карточки «запомни» оставались на экране тренажёра** — а это и есть активный словарь:
  половина заданий решалась списыванием.
- **Новый файл данных не отдавался стендом**: список разрешённых имён в роуте `/book/*` —
  белый, и `drill.json` в него не входил. Пустой тренажёр выглядел как «данные не собрались».

## v2.9 — аватар оживает вместе со звуком (17.08)

Жалоба была одна и давняя: «звук идёт, а она не двигается», а на длинных репликах — «губы
обгоняют». Дефект нашли не глазами, а линейкой по живому раннеру. Оказалось, обе жалобы —
одна и та же поломка, и **модель тут ни при чём**: артикуляция Essence 1 в порядке (размах
рта 16.7 единиц яркости при контроле 2.0, рот открывается до тёмной полости), движок держит
23.8 кадр/с, и до браузера доезжает 22.5 разных кадра в секунду. Полный разбор с числами —
`.planning/BITHUMAN-SELFHOST-2026-08-11.md`, раздел «ЗАМЕР 17.08».

Ломалось так: генератор кадров на раннере поднимается ПРИХОДОМ ЗВУКА и гаснет через 1.5 с
тишины — это предохранитель от утечки денег, поставленный 14.08. Значит каждая реплика
платила холодный старт заново: **3.86 с до первого движения рта против 0.90 с на тёплом
раннере**. Фраза урока короче этой паузы — и договаривалась в неподвижное лицо. А на длинной
реплике поверх ложилось второе: часы показа заводил первый пришедший кадр, но пока лицо
молчит, раннер 25 раз в секунду шлёт ОДИН И ТОТ ЖЕ снимок — часы стартовали по повтору,
за две секунды расписание убегало на полсотни кадров, и настоящая анимация выхлёстывалась
залпом. Это и читалось как «обгоняют».

| что сделано | где |
|---|---|
| прогрев: 100 мс тишины уходят раннеру в момент запроса синтеза и повторяются каждые 0.8 с, пока Яндекс думает | `bhПрогрев`/`bhПрогревСтоп` рядом с `bhPush` |
| повтор кадра узнаётся по подписи и выбрасывается НЕ ДЕКОДИРУЯ — в очередь идут только живые кадры | `bhПодпись` + ветка `живой` в `bhКачай` |
| звук не начинается, пока не пришёл первый живой кадр (ждём не дольше 1.5 с) | `bhЖдиЖивой` + ветка `bhLive` в `speak` |
| часы показа не заводятся, пока ждём пуска звука | `bhЖдёмЗвука` в `bhРисуй` |
| прогрев не может отменить саму реплику: ждём хвост тишины перед подачей речи | `bhПодайРечь` |
| подача речи проверяет ответ раннера и повторяет один раз, а отказ говорит вслух | `bhПодайРечь` |

🔴 Гонка, найденная сетевой панелью уже после первой правки: у раннера **любой** новый
`/push` отменяет недоговорённую подачу (`подача.cancel()` — так задумано, чтобы новая
реплика перебивала старую). Значит прогревочная тишина, застрявшая в сети, могла прийти
ПОСЛЕ речи и отменить её — лицо молчало бы всю реплику. Плюс в живом прогоне 2 из 9 `/push`
вернули 502, а код ответ вообще не смотрел (`.catch(()=>{})`). Оба места закрыты.

Замер после правки, живой прогон урока: прогрев уходит вместе с запросом синтеза → подача
речи (`ответ 200`) → **звук ждёт первый живой кадр 0.81 с и только тогда идёт**. Было:
звук стартовал сразу, а лицо оживало через 1.98 с. По построению остаточное расхождение —
не больше `ЗАПАС`×40 мс (0.16 с), это фора очереди против голодания.

⚠️ Поднимать `IDLE_TAIL` на раннере вместо прогрева НЕЛЬЗЯ: тёплый раннер считает кадры
круглые сутки, а кадры — деньги. Ровно эта утечка стоила 59 кредитов 14.08.

🔴 Найдено попутно, НЕ чинено: в **скрытой вкладке** браузер режет `setInterval` до одного
раза в секунду — и выкладка кадров, которую специально сделали таймером (rAF в скрытой
вкладке не зовётся вовсе), тоже падает до 1 кадр/с. Замер: 18.6 смены картинки в секунду на
активной вкладке против 1.2 на скрытой. Для урока это не страшно (на него смотрят), но
деньги в это время идут: раннер считает свои 25 кадров. Решать вместе с вопросом, гасить ли
поток по `visibilitychange`.

Приёмка после правки: `check-kniga.mjs` 61 · `check-kniga-control.mjs` 83 ·
`check-kniga-video.mjs` 7 · `check-kniga-stuck.mjs` 10 — всё зелёное (гонять по одной).

Линейки замера положены в репозиторий, чтобы вывод можно было перепроверить, а не поверить
на слово: `scripts/bh-lipsync-measure.py` (движок против доставки, размах рта, сдвиг) и
`scripts/bh-warm-check.py` (холодный раннер против тёплого).

### 🔴 Вторая поломка, найденная тем же вечером: аватар вообще не включался

После починки холодного старта Кратов прогнал урок в трёх браузерах — «картинка статичная,
звук идёт». Решающий факт нашёлся не в браузере, а в счётчике раннера: **`кадров_за_сеанс`
не сдвинулся ни на один кадр** за все его прогоны. Значит смотрел он не на аватар вовсе.

Причина: выбор учителя **сохранялся, но при загрузке не восстанавливался**. `kn_teacher`
пишется в трёх местах и читается ровно один раз — ради предзагрузки `.glb`; сам список
`#tchWho` оставался на значении из разметки, то есть «3D · реалистичная». Выбрал
«ВИДЕО · bitHuman», перезагрузил страницу — молча снова 3D-учитель, и bitHuman даже не
поднимается. Со стороны неотличимо от поломки аватара.

⚠️ Урок на будущее: **жалоба «статичная картинка» не говорит, КТО в кадре.** Прежде чем
чинить липсинк — убедиться по счётчику раннера, что аватар вообще работал. Дважды подряд
диагноз ставили не тому.

### Третий заход: одного прогрева на синтезе не хватило

Аватар ожил, и стало видно то, что раньше пряталось: первая фраза читается в неподвижное
лицо, вторая идёт с перекосом, на третьей «голова двигается, а губы нет». Замеры:

| состояние раннера | пауза до движения рта |
|---|---|
| тёплый (фразы подряд через 1 с) | **0.05–0.35 с** — четыре круга подряд |
| остыл (пауза 4 с, как между фразами урока) | **1.7–4.8 с**, а один круг из четырёх не отыграл ВООБЩЕ |
| остыл, но грет тишиной в паузе | **0.12–0.35 с** в трёх кругах из четырёх |

То есть задержки движка нет — есть остывание. Хвост у раннера 1.5 с, а пауза между фразами
урока длиннее, поэтому **каждая фраза платила холодный старт заново**. Прогрев только на
синтезе давал фору 0.5–1 с — вдвое меньше нужного. «Голова двигается, а губы нет» — это
кадры холостого дыхания: рот ещё не завёлся.

Прогрев теперь стоит в трёх местах: на подъёме потока (до первой фразы, потолок 6 с), в
паузе после каждой фразы (потолок 8 с) и на синтезе. Потолок обязателен — кадры это деньги,
и думающий над ответом ребёнок не должен греть движок бесконечно. Цена: ~50–100 кадров на
фразу ≈ 0.05–0.1 кредита, на урок в 70 фраз — 3.5–7 кредитов (≈ 5 ₽).

Ещё две правки того же круга: потолок ожидания живого кадра поднят 1.5 → 2.5 с (был меньше
холодного старта, и звук уходил вперёд), а часы показа получили поправку на дрейф — если
очередь кадров распухла, часы подтягиваются, иначе к концу длинной фразы губы уезжают от
текста.

⚠️ Честно про остаток: один круг из четырёх даже с прогревом стартовал через 3.4 с. Движок
иногда думает дольше, и до конца детерминированным он не стал.

### 🔴🔴 ПРОГРЕВ ТИШИНОЙ ОТМЕНЁН — он и был причиной «губы не открываются»

Кратов прогнал урок: «дёргает головой, но губы не открывает». Замер подтвердил, и вина
моя — прогрев тишиной через `/push` ломает саму речь. Три круга против трёх:

| | размах рта на фразе |
|---|---|
| без прогрева | **12.85 · 13.53 · 7.94** |
| с прогревом тишиной | **1.01 · 12.85 · 0.00** ← дважды рот не открылся вовсе |

Голова при этом «дышала» одинаково (1.0–1.8) — то есть шёл холостой цикл, а речь до
артикуляции не доходила. Причина: у раннера **любой** `/push` отменяет недоговорённую
подачу (`подача.cancel()`) и закрывает реплику (`last_chunk=True` + `flush()`). Тишина
перед речью рвёт её на входе. Прогрев тишиной удалён, в коде оставлено предупреждение.

### ✅ Правильный прогрев: ручка `/keepalive` на раннере

Греть можно только БЕЗ звука — трогая генератор кадров и не трогая аудио-тракт. Раннеру
добавлена ручка `GET /keepalive`: обновляет метку речи (чтобы хвост не оборвал генерацию)
и поднимает насос кадров. Выложена на боевой `87.120.93.151`, служба перезапущена.

🔑 **Перезапуск сам по себе снял большую часть беды.** Раннер висел с 14.08 через убитых
клиентов и фантомных зрителей. На свежем: пауза до движения рта **0.23–0.45 с во всех
кругах**, размах рта **15.19 стабильно** — и с прогревом, и без. Прежние 2–4 с были
состоянием процесса, а не законом.

Урок теперь: пять секунд разогрева после кнопки «начать урок» (`bhРазогрев`) и **грелка на
весь урок** (`bhГрейкаОн`, стук раз в 0.7 с — чаще хвоста в 1.5 с). Разовый разогрев не
работает: между фразами проходит 2–4 с (пауза + синтез), и движок успевает остыть —
поймано замером, где разогрев отработал за 0.34 с, а фразы всё равно ждали по 2.2 с.
Ожидание фраз после грелки: **1.18 · 1.35 · 2.07 с** против 2.22 · 2.05 с без неё.

💸 Цена честного реалтайма: пока грелка стучит, кадры считаются — ~1.7 ₽/мин, ровно то,
что показывает счётчик урока. Гаснет на паузе, на остановке и по уходу со страницы.

⚠️ Мерить браузером в скрытой вкладке НЕЛЬЗЯ: там режутся и таймеры, и декодирование
кадров, цифры получаются вдвое хуже реальных. Сверять — `scripts/bh-keepalive-check.py`
(прямо к раннеру) и `scripts/bh-doctor.mjs` (через прокси стенда, путём браузера).

Починено: восстановление `kn_teacher` перед разбором параметров ссылки (ссылка по-прежнему
главнее памяти). Заодно появился `scripts/bh-doctor.mjs` — проходит цепочку ЧЕРЕЗ ПРОКСИ
СТЕНДА, тем же путём, что браузер, и называет сломанное звено: стенд · учитель по умолчанию ·
раннер · поток · РАЗНЫЕ кадры · пауза до движения.

## v2.8 — урок доходит до конца и меряет знание (13.08, вечер)

Круг по второму документу руководителя — «Что сделано хорошо». Утренний круг чинил
**разговор**; этот чинит то, **работает ли урок как урок**.

- **Урок больше не замирает молча.** Ни на одном из четырнадцати сетевых вызовов не стояло
  срока: зависший запрос не отвечает и не падает, и урок вставал навсегда. Теперь у каждого
  вызова свой срок, а поверх — сторож: шаг не двигается 45 секунд → красная плашка «урок
  застрял на шаге N» с кнопками «пропустить шаг» и «остановить».
- **Отказ голоса больше не убивает занятие.** Раньше один не ответивший запрос синтеза
  останавливал урок целиком — двадцать пять минут работы сгорали. Теперь урок едет дальше по
  тексту, а после второго отказа честно выключает озвучку и говорит об этом.
- **Прогресс переживает закрытие вкладки.** Ушёл на обзор и вернулся — урок предлагает
  «продолжить с шага N» или «начать сначала». Раньше любая перезагрузка обнуляла всё.
- **Тренажёр больше не выучивается стратегией «жми первое».** Верный ответ стоял первым в
  21 задании из 30 (70 %). Теперь позиция разложена: 18 · 19 · 14. Приёмка это проверяет —
  раньше она смотрела, ЕСТЬ ли верный вариант, и не смотрела, где он стоит.
- **Зелёная галочка достаётся ответом, а не кликом.** На повторе параграфа пункт зеленел от
  того, что карточку открыли (а первый — ещё до того, как ребёнок что-то сделал). Теперь
  счётчик называется «знаю N из 28», и попасть в него можно только верным ответом.
- **Сначала попытка, потом разбор.** В разборе вопроса ответ показывали до того, как ребёнок
  успевал подумать. Теперь первый шаг — чистый выбор без подсказок, и разбирается именно его
  ошибка, а его вариант подсвечен.
- **Подсказка стоит попытки.** Ответ после подсказки шёл в графу «с первой попытки», и
  завышенная цифра уезжала в итоги и в письмо родителю. Теперь в отчёте своя колонка
  «Подсказок», а «с 1-й» стало «Сам с 1-й».
- **Карта: что названо — то и в кадре.** Утреннее правило «на страницу один наезд» ставило
  кадр по первому месту страницы, и Крит, Кносс, Микены и Коринфский перешеек оказывались за
  экраном — двенадцать показов из двадцати девяти. Кадр теперь считается по всем местам
  страницы сразу: карта так же не двигается, но всё, что прозвучит, видно.
- **На карте наконец видно, что подсвечено.** Механизм приглушения был в карте с самого
  начала и не включался ни разу: названное место шло на полной яркости, остальные — на
  половинной. Разница вдвое читалась как «ничего не выделено».
- **Вернулся путь «прочитал про себя».** Утренний круг незаметно его разменял: кнопку рисовали
  на экране-подводке, а через пять секунд экран перерисовывался и нажатие терялось. Это
  единственное место, которое руководитель отметил как сделанное хорошо.
- **Убран тупик на повторе.** Кнопка «Перейти к тесту» переключала режим, обещала голосом
  «ответь по памяти» — и теста не существовало ни строкой, а вернуться было нельзя. Кнопка и
  чип «ТЕСТ» убраны. Собирать ли тест из уже загруженных 24 вопросов — открытое решение.

### Приёмка

Прибавилось 13 проверок, из них новый файл `scripts/check-kniga-stuck.mjs` — он подвешивает
синтез навсегда и смотрит, что урок это переживает.

⚠️ **Две проверки перенацелены.** «Обход про себя» искали на экране-подводке, где кнопка и
стояла, — но нажатие там терялось; теперь проверяется экран чтения. «Карта наезжает на
названное место» требовала ровно того поведения, которое давало регрессию, — теперь
проверяется, что кадр один на страницу и все места в нём.

### Что нашла сама приёмка, чего в разборе не было

- После обзорной карты Европы возврат к Греции перерисовывал коробку и обнулял кадр, а память
  «кадр уже поставлен» оставалась — кадр страницы больше никто не ставил. Ровно на той
  странице, где карт больше всего.
- На стр. 125 два разных задания несли один идентификатор. Пока безобидно, но по нему будет
  храниться, что ребёнок уже знает.
- **«Перечитаю то, что не запомнилось» — и не перечитывал.** Повтор слабого места искал такт
  в учебнике ПО НОМЕРУ, а утренний круг вставил в сценарий три новых такта: номера съехали, и
  у «Трёх частей Греции» номер стал показывать на реплику Ани, где текста учебника нет вовсе.
  Ребёнку обещали перечитать — и не подсвечивали ничего. Теперь место ищется по тексту, а
  номер остался последним запасным путём (и только если по нему действительно есть текст).

---

## v2.7 — разбор с записи: Аня слушает сама и знает, где ученик (13.08)

- **Кнопку «Говорить» жать больше не надо.** Задала вопрос — и слушает; ответ доходит, даже если про кнопку забыть. Кнопка осталась запасным путём.
- **Прочитал вслух — засчитано.** Раньше Аня просила прочитать, а прочитанное принимала за посторонний разговор и уводила в сторону.
- **Не спрашивает о том, что ещё не читали.** У неё появилась память урока: какие страницы пройдены, какая открыта. Ответ, который ссылается вперёд («об этом прочитаем дальше», «на странице 121»), в урок не попадает.
- **Не показывает пальцем в никуда.** Пропали «этот абзац» и «здесь написано» — там, где ребёнок не видит, на что показывают.
- **Подсветка больше не спорит сама с собой.** На выжимке страница слева уходит в полутень, и всё внимание — справа: раньше приходилось смотреть в две стороны одновременно.
- **Выжимка не теряет подлежащее.** Было «Она делилась на три части» — стало «Древняя Греция делилась на три части». Правлено в восьми местах, словами учебника.
- **Новые слова объясняются.** «Мегарон» и «фреска» больше не просто зачитываются списком; заодно сказано, почему царь Минос — легендарный.
- **Урок честно говорит, где начало параграфа.** Первые строки страницы 120 — вступление ко всей главе, а не §20; раньше это читалось молча и путало.
- **Главный вопрос параграфа не ждёт ответа.** Предупреждение «отвечать не надо» звучит ДО вопроса, а не после долгой паузы.
- **Карта перестала метаться.** На страницу — один наезд, дальше места просто загораются на неподвижной карте; переезд стал вдвое быстрее.
- **Появилась карта Европы.** Перед разговором о Балканском полуострове видно, где он относительно всей Европы, и рамкой обведён кусок, который будем рассматривать.
- **Пропала пауза посреди чтения.** Следующий кусок уходит в синтез, пока звучит текущий; сбой синтеза больше не глушит урок молча.

### Подробно

Круг правок по 15-минутной записи разбора от 13.08. Расшифровка и разбор по пунктам —
`.planning/KNIGA-REVIEW-2026-08-13.md`, транскрипт — `.tmp/video-in/razbor.transcript.md`.

Пять замечаний из четырнадцати оказались одним дефектом: у сценария не было понятия
«что уже пройдено», и реплики с ним не сверялись. Отсюда и вопрос про микенских царей
«о которых мы читаем дальше», и ссылка на неоткрытую страницу 121, и незачтённое чтение
вслух. Чинилось это в одном месте — состояние урока теперь собирается из того, что уже
было (индекс такта и открытый разворот) и уезжает в промпт, а ответ дополнительно
процеживается от ссылок вперёд.

Точка отката: **v2.6 заморожена целиком** — своя копия страницы и все семь файлов данных
(`/kniga/v26`), она не зависит от сегодняшних правок.

---

## v2.6 — вычитка глазами и удобство проверки (11.08)

- **Пять новых иллюстраций** там, где на экране было не про то: стада овец, верфь, засуха, дворец ахейцев, изрезанный бухтами берег.
- **Четыре задания переписаны** — в них ответ повторял сам вопрос, и его можно было угадать.
- **«Что изменилось» открывается отдельным окном**, а не в узкой колонке карточки.
- **Сборки названы греческими буквами**: АЛЬФА · БЕТА · ГАММА.
- **К замечанию можно приложить фото** — файлом или обведя место прямо на экране урока.
- **Экран загрузки** вместо полупустой страницы в первые секунды.
- **Всё содержимое урока — одним экраном** (`/kniga/obzor`): показы, карточки, задания, вопросы. Смотреть, не проходя урок.
- **Время везде московское** — было на три часа назад.
- **Губы учителя вдвое спокойнее.**
- **Замечания хранятся с историей** и не теряются.
- **Экран загрузки ждёт учителя** — модель 13 МБ приезжала позже страницы, и аватар появлялся не сразу.
- **Темп речи виден и слышен сразу**, добавлен быстрый прогон ×2 — просмотреть урок целиком.
- **На чтении вслух видно, ЧТО читать**, а после честного захода открываются все картинки мозаики.
- **Экран топиков переключается сам** — даёт посмотреть и идёт дальше.
- **«Мегарон и фреска» теперь объясняется**, а не «встретим дальше».
- **Ещё одна иллюстрация — «Камень и мрамор»**: у этой темы своей картинки не было вовсе, показывали общий пейзаж учебника.
- **После урока открывается повтор всего параграфа** — экран закрепления с картинками и строками учебника, чтобы не слушать урок заново.
- **В «что изменилось» видно дату и время выкладки** — не подпись руками, а время релиза с сервера.
- **Витрина стала витриной ВАРИАНТОВ**: добавлен четвёртый — макет «Экран закрепления», другая подача по вашему референсу.

### Подробно

Круг правок по замечаниям с прогона 11.08. Приёмка при этом была зелёной вся —
422 проверки и полный прогон урока без единой ошибки, поэтому искали то, куда она
не смотрит: **совпадает ли картинка с тем, что звучит, и можно ли решить задание,
не зная материала**.

**Пять новых иллюстраций — там, где на экране было не про то.** Картинок на параграф
было 36 на 112 показов, поэтому часть работала на два-три разных места сразу — и в
половине случаев это читалось как ошибка:

- «Стада овец и коз паслись на холмах» — показывали **глину и посуду**;
- «лес использовался для строительства кораблей» — показывали **готовый флот** в море,
  хотя речь про лес, из которого его только предстоит построить;
- «длительную засуху, вызвавшую голод» — показывали **сухую землю жарким летом**, то
  есть погоду вместо бедствия;
- «дворцы ахейских царей были похожи на критские» — показывали **сам критский дворец**:
  говорим про одно, показываем другое;
- «берега изрезаны множеством заливов и бухт» и тема «Бухты и острова» — показывали
  **карту**, на которой при таком масштабе никаких бухт не разглядеть; вдобавок карта
  на экране топиков стояла сразу у двух тем.

Теперь у каждого из этих мест своя картинка: пастух со стадом · верфь на берегу ·
растрескавшееся поле · дворец ахейского царя в разрезе · изрезанный бухтами берег.

**Четыре задания решались без знания материала.** Ответ повторял слова самого вопроса:
«„Бухты и острова“ — что это значит?» → «Бухты укрывали корабли, острова были
ориентирами». Ребёнок выбирает по совпадению слов, а не по смыслу — методичка такие
задания прямо запрещает. Формулировки переписаны по строке учебника, которая рядом:
теперь ответ говорит, ЧТО это, не называя того же самого. Затронуты «Бухты и острова»,
«Глина и посуда», «Первая в Европе» и «Царь — верховный жрец» (последняя тема названа
по учебнику — «Во главе государства»).

**«Что изменилось» открывается отдельным окном.** Раньше список правок разворачивался
внутри карточки шириной в треть экрана, и читать его приходилось в узком окошке с
прокруткой. Теперь он открывается поверх страницы на всю ширину, закрывается по Escape
или щелчком мимо.

**Сборки названы буквами греческого алфавита** — АЛЬФА · БЕТА · ГАММА вместо
армейских АЛЬФА · БРАВО · ЧАРЛИ. Урок про Грецию, и порядок букв сам говорит, что за чем шло.

**К замечанию можно приложить фото** — и на витрине, и прямо из урока (кнопка «✎ замечание»
в нижней панели). Скриншот объясняет за секунду то, на что уходит абзац текста. Снимок
видно тут же в списке замечаний, по нажатию он открывается во весь экран, закрывается по
Escape. Картинки грузятся **лениво**: пока список замечаний не попал на экран, за ними никто
не ходит, и открытие витрины не тяжелеет.

**Время везде московское.** Сервер витрины живёт по UTC, и в замечаниях стояло время на три
часа раньше, чем на часах: замечание, оставленное в 11:37, подписывалось «08:37». Пояс
теперь задан явно — и в записи замечания, и в итогах урока, и в списке релизов.

**Экран загрузки.** Урок тянет страницы учебника, привязки показов, задания и модель
учителя — первые секунды он выглядел полупустым, будто сломался. Теперь видно, что идёт:
«открываем учебник» → «иллюстрации» → «задания тренажёра» → «страница учебника». Экран
уходит не по таймеру, а когда картинка страницы действительно загрузилась.

**Обзор материалов — `/kniga/obzor`.** Урок ведёт ребёнка по одному шагу и идёт полчаса;
тому, кто пришёл ПРОВЕРИТЬ материал, слушать его целиком незачем. На отдельной странице
собрано всё сразу: 112 показов с подписями и фразами, к которым они привязаны, 28 карточек
«запомни» со строками учебника, 57 заданий тренажёра вместе с запасными и верными ответами,
24 вопроса учебника — по страницам, с переходами. Страница читает те же файлы, что и урок,
поэтому разойтись с ним не может. Вход — с витрины.

**Размах губ уменьшен вдвое.** И в коэффициентах, и по смыслу: единица на ползунке «сила
губ» теперь означает спокойную артикуляцию; кому нужно как было — ползунок до 2.

**Витрина показывает варианты, а не только историю одной линии.** Постановка Кратова:
«в этом и смысл версий: тут новые варианты визуальных представлений, уроки уже немного
отличаются функционально, идут параллельные ветки — и мы смотрим, какая интереснее».
Поэтому четвёртой карточкой встал **макет «Экран закрепления»** (`/zakrep`, 06.08) — тот
самый другой визуальный язык по референсу Anatomy Atelier: учебника на экране нет, во весь
экран карта или иллюстрация, слева список «что надо запомнить», справа разбор одного пункта.
Возить его на сервер стоило один файл: данные (`book/zakrep.json`, карта) и так едут.
⚠️ Закрытые 3D-ветки (`/krit3`, `/hub`, `/lesson3d`) на витрину не попали намеренно: они
весят 1.2 ГБ и закрыты решением 02.08. Понадобится посмотреть — это отдельная задача.

**Дата и время выкладки в «что изменилось».** Раньше в окне был только список правок, и
было непонятно, когда это вышло. Теперь в шапке стоит «вышло 11.08.2026 в 12:49 МСК» —
время берётся из `RELEASE.json` последней выкладки, где была эта версия, и той же датой
подписывается карточка. Подпись руками через неделю начала бы врать, а эта — не может.

**Две правки по картинкам на экране повтора.** Тем 28, и две из них показывали не своё:
«Камень и мрамор» — общий пейзаж учебника «Природа Греции» (своего показа у этой фразы не
было ВООБЩЕ, Аня читала про строительный камень, а на экране висела предыдущая картинка),
и «Мегарон и фреска» — ту же картинку, что настоящая тема «Мегарон» на стр. 124.
Дорисована каменоломня (`stonework`, 2 кредита) — и она же встала показом в самом уроке;
анонсу «Мегарон и фреска» отдана НАСТОЯЩАЯ фреска из учебника (дельфины, стр. 122):
мегарон уже занят своей темой, значит анонсу достаётся вторая половина. Одинаковых
картинок на разных темах больше нет.

**Правки по живому прогону 11.08 (вторая половина дня).**

- **Аватар появлялся не сразу.** Заставка ждала картинку страницы, а модель учителя — это
  13 МБ, и она приезжала позже. Теперь загрузка ждёт и её (замер: модель готова на 1.7 с,
  заставка уходит на 2.8 с); не приехала за 12 с — урок всё равно открывается.
- **Темп речи «вроде не работает».** Работал, но синтез применяет скорость к СЛЕДУЮЩЕЙ
  реплике — со стороны это и читается как «ничего не изменилось». Теперь рядом с ползунком
  стоит значение (×1.4), текущая фраза ускоряется сразу, а для просмотра урока есть кнопка
  **⏩ прогон ×2**; потолок поднят с 1.15 до 2.5.
- **«Непонятно, что читать».** На экране после чтения оставался только УСЛЫШАННЫЙ текст, а
  сам кусок исчезал — особенно сбивало на «читать ещё раз». Теперь читаемое остаётся и
  подписано, услышанное идёт отдельным блоком ниже.
- **«Вроде прочитал, а картинки не открылись».** Мозаика раскрывалась целиком только при
  85 % прочитанного. Теперь после честного захода открывается весь ряд: это обещанная
  награда, и следующий шаг всё равно разбирает эти картинки по одной.
- **Экран топиков переключается сам.** Досказала — держит картинку пару секунд и идёт
  дальше; кнопка осталась для тех, кто не хочет ждать. ⚠️ Первая реализация гонкой промисов
  оставляла висеть ожидание, и следующее нажатие проваливалось — урок вставал через шаг.
  Правильно — будить тот же ждущий таймером.
- **«Мегарон и фреска» — она их не объясняет.** Карточка говорила «два новых слова,
  встретим их дальше». Теперь объяснение дословно из учебника: «главный зал с очагом,
  окружённым четырьмя колоннами, — мегарон» (стр. 124) и «стены дворцов украшали фрески»
  (стр. 123). ⚠️ В тренажёр эта карточка не идёт: она объясняет СРАЗУ ДВА слова, и любое
  её определение повторяет оба — задание решалось бы угадыванием. У каждого слова есть
  своё задание по отдельности.

**Повтор всего параграфа после урока.** Постановка Кратова: «возможный вариант
закрепления основного материала со всего параграфа; откроется после прохождения урока,
можно будет потом из ЛК быстро глянуть, чтобы весь урок заново не слушать». На экране
итогов появилась кнопка **«повторить весь параграф»** — она открывает экран закрепления
(он же БЕТА на витрине): 28 тем всего параграфа, у 26 своя картинка, у двух «мест» —
карта с наездом, у каждой дословная строка учебника. 🔑 Данные те же самые, что у урока
(`book/zakrep.json`), поэтому картинки и тексты совпадают с тем, что ребёнок только что
видел: правки в уроке доезжают сюда сами, расходиться нечему.

**Замечания больше не теряются.** Записи приёмки помечаются служебными и в список
руководителя не попадают, а сам файл замечаний не чистится никогда: раньше их выпалывали
руками — и однажды вместе с ними стёрли живое замечание. Восстановить его удалось из
журнала службы, но правило теперь железное: лишнее прячут, а не удаляют.

### Грабли, купленные в этот круг

- 🔴 **Приёмка тренажёра позеленела не сразу — и была права не она.** После правки
  формулировок 8 проверок упали при исправном уроке: они ждали разбор ошибки
  «через 900 мс», а разбор появляется ПОСЛЕ реакции Ани, и та тем длиннее, чем длиннее
  вариант ответа. Удлинили тексты на два слова — проверка поехала. Теперь ждём событие
  (появился новый разбор), а не срок. ⚠️ Это ровно та грабля, что записана в прошлом
  круге: «ждать условие, а не фиксированную паузу» — и она вернулась в другом месте.
- **Правило непересечения дошло не до всех заданий.** В прошлый круг тавтологию
  вычистили из основных заданий, а запасные (их выдают взамен проваленных) остались со
  старой сборкой. Проверка теперь гоняет и запасные тоже.
- **Перед генерацией — проверять, нет ли готового.** Три картинки из «неиспользуемых»
  (`icarus`, `dorians2`) под эти места не подошли, но смотреть надо было до заказа.
- **Фото ужимает СТРАНИЦА, а не сервер.** С телефона прилетает 4–8 МБ, для «вот тут криво»
  хватает 1600 px: браузер жмёт в JPEG и шлёт data-URL'ом. Сервер тогда остаётся без разбора
  multipart и без обработки картинок — а он живёт с нулём зависимостей, и так и должно быть.
- 🔴 **Фото обязано лежать ВНЕ релиза.** Первым же движением хотелось положить его рядом с
  уроком — тогда выкладка новой версии унесла бы все снимки руководителя. Снимки живут в
  `shared/photos`, релиз видит их ссылкой — как и сам журнал замечаний.

## Как нарезать новую версию

```bash
cd .tmp/sketches/tutor
cp kniga.html kniga-v2.html                 # заморозить то, что было
mkdir -p book/v2 && cp book/panel.json book/test.json book/v2/
```
Дальше в замороженном файле: `fetch('/book/panel.json')` → `/book/v2/panel.json` (то же для
`test.json`), плашка `#ver` и `<title>` — на номер архива. В рабочем `kniga.html` поднять номер
в двух местах (`<title>` и `<span id=ver>`), в этот журнал добавить строку и раздел.

Свои данные каждой версии обязательны: привязки показов и вопросов правятся почти каждый круг,
и без заморозки прошлая сборка через день показывает не то, что на скриншотах.

Роуты живут в `scripts/yandex-test-server.mjs`: `/kniga/vN` → `kniga-vN.html`,
`/book/vN/*.json` → замороженные данные.
