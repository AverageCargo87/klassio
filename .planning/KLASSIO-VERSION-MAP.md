# Карта версий и веток Klassio — что где живёт (на 07.08.2026)

Зачем файл: за июль–август набралось около двадцати прототипов, и по названиям уже не
видно, что из этого продукт, что закрытая ветка, а что инструмент. Здесь всё разложено по
трём полкам. **Вширь мы не идём** — в работе одна линия, остальное либо отрезано решением,
либо служебное.

---

## 1. Главная линия — урок-учебник `/kniga`

Единственное, что сейчас развивается. Форма выбрана руководством 02.08 взамен 3D.

| версия | дата | адрес | что нового | статус |
|---|---|---|---|---|
| **v1.0** | 03–04.08 | `/kniga/v1` | первая целая сборка: страницы PDF, Аня читает и подчёркивает, панель показа + карточки «запомни», проверка страницы | заморожена — **это то, что видел руководитель** |
| v2.0 | 05.08 | — | показ идёт за фразой (120 привязок вместо 44) · своя карта Греции · чтение вслух вместо кнопки «прочитал» · разбор вопросов перед тестом | не архивировалась |
| v2.1 | 05.08 вечер | — | 7 правок прогона: карта под коробку, повторы картинок по семьям, иллюстрация на каждое новое слово, разбор больше не спрашивает дважды | не архивировалась |
| v2.2 | 05.08 ночь | — | наезд карты по рамке с подписью, подписи не дрожат, пауза не перескакивает такт, транскрипция не склеивается | не архивировалась |
| v2.3 | 06.08 | `/kniga/v23` | вопросы САМОГО учебника идут первыми · «следующий вопрос» в тесте · подсветка ровно по звучащему предложению | заморожена |
| v2.4 | 06–07.08 | — | **цепочка руководителя**: самари из предложений учебника → чтение самари вслух с мозаикой → отдельный экран топиков (книга уезжает) → миниатюры на карточках | не архивировалась |
| v2.5 | 08–10.08 | — | **методичка проведения урока** (документ «Промпт3»): тренажёр раздела на 6 заданий с тремя попытками и заменой проваленного, счётчик и таймеры, звуки, подсказка, чтение вслух как условие, экран итогов с письмом родителю | отдельным адресом не заморожена — лежит релизом `0002` на сервере |
| **v2.6** | 11.08 | `/kniga/v26` | вычитка глазами: 5 новых иллюстраций там, где показывали не про то · 4 задания, где ответ повторял вопрос · «что изменилось» отдельным окном · сборки названы греческими буквами | заморожена ЦЕЛИКОМ (свои данные в book/v26) — точка отката перед разбором 13.08 |
| **v2.7** | 13.08 | — | круг по 15-минутной записи разбора: Аня слушает сама (кнопка «Говорить» больше не условие) · чтение вслух засчитывается · память урока (не спрашивает о непройденном, не ссылается на неоткрытые страницы) · на выжимке слева гаснет · подлежащее в выжимке · объяснение новых слов · карта не мечется + обзорная карта Европы · пауза посреди чтения убрана | прожила полдня, не заморожена — сверху легла v2.8 |
| **v2.8** | 13.08, вечер | — | круг по второму документу («Что сделано хорошо»): урок доходит до конца — сроки на всех 14 сетевых вызовах + сторож «застрял на шаге N» · отказ голоса не убивает занятие · прогресс переживает F5 · тренажёр меряет знание (верный ответ был первым в 70 % заданий) · зелёное только за ответ без подсказки · сначала попытка, потом разбор · кадр карты по всем местам страницы + приглушение остальных · убран тупик «Перейти к тесту» | не архивировалась |
| **v2.9** | 17.08 | — | аватар оживает вместе со звуком: прогрев раннера, повтор кадра выбрасывается не декодируя, звук ждёт первого живого кадра. 19.08 сверху лёг **Anam вместо bitHuman** | не архивировалась |
| **v2.10** | 20.08 | `/kniga` | **выбор учителя кнопкой на самом окне Ани, карточками с лицами**: 5 лиц Anam (те, что уходили Владимиру пробами) · 1 живое лицо bitHuman · 2 бесплатные 3D · запись · без аватара. Наведение крутит петлю простоя — видно, живёт ли лицо молча. В той же панели выбор голоса с «послушать». **Три сторожа минут Anam**: потолок сессии 2 мин (вшит в токен), простой 45 с, «ушёл» 20 с; видео гаснет — урок едет голосом, на окне кнопка «включить снова» | **текущая** |

Приёмка (20.08): kniga 54 · control 83 · panel 209 · drill 62 · video 7 · figures 21 ·
stuck 9 · **vybor 27** (новый файл: кнопка внутри рамки окна, картинки карточек реально
отдаются, выбор доезжает до урока, потолок минут вшит в токен). Гонять по одной: в прогоне
пачкой проверки моргают.
⚠️ **Долг приёмки:** 7 проверок в `check-kniga.mjs` ждут 3D-учительницу (позу, морфы, ход
модели), а с 19.08 по умолчанию стоит Anam — мешей у неё нет по существу. Падают одинаково
до и после правок; чинить = переписать их под видео-путь либо ставить в них 3D явно.

**Витрина `/lab`** (10.08, перестроена 11.08) — вход для руководителя. Буква = ВЕТКА подачи:
АЛЬФА — учебник-листалка (**v2.10**, архивы v1.0/v2.3/v2.6 под её карточкой), БЕТА — повтор параграфа
(`/zakrep`, открывается после урока). Плюс обзор материалов `/kniga/obzor` — всё содержимое
урока одним экраном. У каждой ветки описание, кнопка входа и «что изменилось» с лентой
«что и во сколько выходило» (режется из журнала версий и времён релизов, руками не пишется),
внизу — приём замечаний с фото. Приёмка `scripts/check-lab.mjs`, 54 проверки по боевому адресу. Живёт на российском VPS —
`.planning/LAB-DEPLOY-RUNBOOK.md`.

🔑 **20.08 разрыв «выложено ≠ сделано» закрыт.** С 11.08 на витрине девять дней стояла
v2.6, пока делались v2.7…v2.10: руководитель смотрел позапрошлую сборку и замечания давал
по ней. Релиз `0018` везёт всё сразу. ⚠️ Лента выкладок ищет релизы **по букве ветки**, а не
по номеру версии: искала по номеру — и на первой же смене номера история обнулялась.
🔑 **Ключ Anam живёт на сервере в `shared/.env.local`** и вшивается в релиз ссылкой
(`link_shared`), как секрет Яндекса. В пакет выкладки ключам хода нет, и откат их не уносит.

🔑 **Откат (11.08).** Выкладка `node scripts/lab-deploy.mjs -m "зачем"` кладёт КАЖДУЮ
версию отдельной папкой-релизом и переключает ссылку; не встало — сервер сам возвращает
прежнюю. Сказали «фигня» — `node scripts/lab-rollback.mjs --back`, секунды. Исходники
сборок зеркалятся под git в `lab/` с меткой `lab-000N`, журнал выкладок —
`.planning/LAB-RELEASES.md`.
⚠️ **v2.4 отдельной сборкой не существует** — v2.5 легла поверх неё в тот же круг. Журнал версий с подробностями и граблями —
`.planning/KNIGA-VERSIONS.md`, хендофф — `.planning/SESSION-2026-08-05-KNIGA-V2.md`.

🔑 **Закон формы, на котором всё держится:** 90 % времени звучит текст учебника, учебник —
стопроцентный вариант истины. Отсюда и масштабирование на другие параграфы, и контроль.
Каждый раз, когда просят «добавить своего», это решение принимается отдельно.

**`/zakrep` (06.08) — вторая ветка подачи.** Экран закрепления по референсу Anatomy Atelier:
из него вырос экран топиков в v2.4, а 11.08 он включён в урок как **повтор всего параграфа**
после итогов — «чтобы весь урок заново не слушать». Данные общие с уроком.

---

## 2. Закрытые ветки — решения уже приняты, возвращаться не нужно

| ветка | адрес | даты | чем закончилась |
|---|---|---|---|
| **3D-урок истории «Крит»** | `/krit3` | 28–31.07 | 16 шагов, 4 сцены, 28 сгенерённых иллюстраций, живой диалог голосом. 🔴 **Забракован на созвоне 02.08** вместе со всем 3D-иммерсивом |
| ХАБ + античные сцены | `/hub`, `/animus` | 20–22.07 | белое пространство, сборка мира под тему, 3 локации. Идея АНИМУСа жива как концепция, реализация закрыта вместе с 3D |
| Иммерсив-класс v2 | `/lesson3d`, `/lesson360`, `/class3d` | 16–17.07 | панорамы, 3D-класс, Anam-экран — перебор вариантов «где сидит ребёнок» |
| Урок как сайт-страница | `/site`, `/klass` | 17–19.07 | плоский вид, забракован руководителем — с него и начался поиск 2.5D |
| Учебник в 3D-классе | спайк `uchebnik-3d` | 03.07 | V1 (ходьба → сел за парту → 2D-урок) выбрал Кратов; поглощён формой `/kniga` |
| Miro-режим, космос-урок | спайк `miro-lesson`, `space-lesson` | 23.06 | режимы отображения; ось выбора описана, ветки закрыты |

---

## 3. Стенды и инструменты — не продукт, а оснастка

| что | адрес | дата | зачем |
|---|---|---|---|
| витрина 3D-учителей | `/showcase` | 24–27.07 | 3 модели рядом + Anam + bitHuman, липсинк и цена каждого; Кратов выбирает лицо |
| стенд аватаров | `/avatars` | 18–19.07 | сравнение сервисов после ресёрча 26 агентов |
| липсинк | `/teacher` | 23.07 | wawa-lipsync, висемы, A/B |
| тетрадь (рукопись) | `/tetrad` | 29.07 | Vision OCR + разбор жестов + судья DeepSeek |
| стенд Яндекс/DeepSeek | сервер `:8781` | 16.07 | бенчмарк голоса и мозга; **сегодня это хост всех прототипов** |
| **аватар bitHuman** | боевой VPS `87.120.93.151:8090`, служба `klassio-avatar` | 11–19.08 | ❌ **Забракован 19.08** («полная фигня»): причина структурная — `base frame + mouth patch`, мимика со звуком не связана. Раннер оставлен ЖИВЫМ ради сравнения: одно лицо `anya` (essence-1), карточка в выборе учителя есть. Essence 2 живьём у них не работает на чужом сервере — чёрное окно. Отчёт `.planning/BITHUMAN-SELFHOST-2026-08-11.md` |
| **аватар Anam** | облако, ключ в `shared/.env.local` на витрине | 19–20.08 | ✅ **Выбран.** Верх лица двигается вчетверо сильнее bitHuman (`scripts/face-liveness.py`). Пять лиц отобраны и стоят в выборе учителя. Карточки нарезает `scripts/make-avatar-cards.mjs`. ⚠️ Тариф с 1 параллельной сессией (Free/Starter) — сорокаминутный урок целиком на нём не пройдёт, нужен предрендер или Growth |

Всё это поднимается одним процессом:

```bash
node scripts/yandex-test-server.mjs
```

---

## 4. Боевой продукт — отдельная жизнь

Сайт на Next.js: уроки `/tutor/*`, ЛК, авторизация. Задеплоен **сам на себя** с 10.07 —
`https://87.120.93.151.nip.io` (systemd `klassio-web` + nginx), Vercel остался бэкапом.
Живые уроки: астроном (окр. мир), детская карта ВТБ, инвестиции. Голос там — прежний контур.

⚠️ **Прототипы `/kniga` и прочие в бой не идут** и в git не лежат: они в `.tmp/` (gitignored),
плюс страницы учебника под копирайтом. Перенос в продукт — отдельная задача, она не начата.

---

## Как читать эту карту

- **Развиваем одно** — `/kniga`. Номер версии виден в заголовке вкладки и плашкой в нижней
  панели урока, прошлые сборки открываются по `/kniga/vN`.
- **Ветки 2 и 3 не «недоделаны»** — по ним есть решения. Возврат к любой = смена формы, а
  не продолжение работы.
- Если добавляется новая ветка — сначала строка в этой таблице, потом код.
