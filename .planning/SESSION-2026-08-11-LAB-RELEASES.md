# Релизы с откатом, витрина по веткам, круг правок v2.6 — хендофф 11.08.2026

**Читать первым.** Форма и история версий — `.planning/KNIGA-VERSIONS.md`.
Команды выкладки и отката — `.planning/LAB-DEPLOY-RUNBOOK.md` (разделы 3 и 6).
Карта веток проекта — `.planning/KLASSIO-VERSION-MAP.md`.
Предыдущий хендофф — `.planning/SESSION-2026-08-10-LAB-DEPLOY.md`.

---

## ✅ Что живое прямо сейчас

**https://5.35.90.219.nip.io** — витрина для руководителя. Логин `klassio`, пароль `6hv98SBo4sQ`.
На боевом релиз **0016** (11.08, 14:28 МСК). Сервер: Beget СПб, вход по ключу
`ssh -i .tmp/keys/klassio-lab root@5.35.90.219`, служба `klassio-lab`, часовой пояс МСК.

**Витрина — по ВЕТКАМ подачи.** Буква = направление, номер = состояние внутри него:

| | что это | адрес |
|---|---|---|
| **АЛЬФА · v2.6** | учебник-листалка, единственная развиваемая ветка | `/kniga` |
| ↳ архивы ветки | прошлые состояния, ссылками под её карточкой | `/kniga/v1` · `/kniga/v23` |
| **БЕТА · повтор** | другая подача: карта во весь экран, список «что запомнить», разбор пункта | `/zakrep` |
| обзор материалов | всё содержимое урока одним экраном, не проходя урок | `/kniga/obzor` |

БЕТА **открывается после урока**: на экране итогов кнопка «▶ повторить весь параграф».
Данные у неё те же (`book/zakrep.json`), поэтому картинки и тексты совпадают с уроком.

---

## 🔑 Выкладка и откат — главное, что появилось сегодня

Раньше выкладка распаковывала архив ПОВЕРХ живой папки: откатывать было нечего.

```bash
node scripts/lab-deploy.mjs --dry                 # что изменится на боевом, боевого не касается
node scripts/lab-deploy.mjs -m "зачем"            # выложить (сам вернёт прежний релиз, если не встало)
node scripts/lab-rollback.mjs                     # список релизов: когда, зачем, версии, метка git
node scripts/lab-rollback.mjs --back              # вернуть предыдущий, ~5 секунд
node scripts/lab-rollback.mjs --to 0009 --local   # и вернуть исходники на рабочей машине
```

- На сервере: `releases/000N-…/` (храним 10) + `shared/` + ссылка `current`, на неё смотрит служба.
- 🔴 **`shared/` вне релизов**: `.yandex-secret.json`, `feedback.jsonl`, `photos/` — откат их не трогает.
- Исходники (текст: 6 страниц + 19 файлов данных) зеркалятся в `lab/` под git, метка `lab-000N`.
- Журнал выкладок — `.planning/LAB-RELEASES.md`, пишется скриптом.
- Проверено по-настоящему: откат туда-обратно и нарочно сломанный релиз (сервер вернул прежний сам).

---

## Что сделано за день (v2.6, релизы 0001–0016)

**Содержание урока**
- 6 новых иллюстраций: `goats` · `shipyard` · `drought` · `mycpalace` · `bays` · `stonework` —
  там, где на экране было не про то. У «Камня и мрамора» показа не было ВООБЩЕ.
- 4 задания переписаны: ответ повторял вопрос («Бухты и острова» → «Бухты укрывали…»).
  Правило заведено в приёмку и гоняет **запасные задания тоже**.
- «Мегарон и фреска» объясняется словами учебника; парные карточки в тренажёр не идут.

**Урок**
- Экран загрузки; данные грузятся ПАРАЛЛЕЛЬНО (боевой: 17.2 с → 10.1 с), заставка ждёт учителя.
- Темп речи: значение видно, текущая фраза ускоряется сразу, потолок 2.5, кнопка «⏩ прогон ×2».
- Чтение вслух: на экране остаётся ТЕКСТ ДЛЯ ЧТЕНИЯ, услышанное — отдельным блоком;
  после честного захода открывается вся мозаика.
- Экран топиков переключается сам (кнопка осталась как «не ждать»).
- Размах губ уполовинен.

**Витрина**
- «Что изменилось» — отдельным окном, короткая сводка (подробности в журнале под `### Подробно`
  на витрину не идут), **лента «что и во сколько выходило»** из времён релизов.
- Замечания: фото (файлом или **обведя область** прямо в уроке), просмотр во весь экран, ленивая
  загрузка, московское время.
- Имена веток — греческие буквы.

---

## 🔴 Правила, которые нельзя нарушать

- **Файл замечаний не чистить НИКОГДА.** Сегодня чистка «приёмочного мусора» стёрла живое
  замечание Кратова (восстановлено из `journalctl -u klassio-lab`). Приёмка ставит
  `window.__приёмка=true`, страница шлёт `служебное:true`, сервер прячет такие записи из
  `/api/feedback` (`?all=1` — для приёмки). Лишнее ПРЯЧУТ, а не удаляют.
- **Нарезал версию — допиши раздел в журнал**: витрина режет «что изменилось» оттуда.
- **Общие данные** (`blocks/figures/marks/pages/map-greece`) читают все ветки — выкладка про это
  печатает громкую строку.
- **push не делаем** без явного разрешения.

## Грабли дня (дорогие)

- **Ждать условие, а не срок.** Дважды: приёмка тренажёра ждала разбор «через 900 мс»
  (а нажатие до перерисовки урок кладёт в ОЧЕРЕДЬ — состояние ехало на шаг), и приёмка витрины
  читала окно через 700 мс, когда оно ждёт два запроса.
- **Гонка промисов оставила висеть ожидание** — авто-переход топиков съедал следующее нажатие,
  урок вставал через шаг. Будить надо тот же ждущий таймером.
- **Последовательные `await fetch`** семи независимых файлов = 5 секунд на латентности.
- `sed 's#^\./##'` жадной точкой съедал первый символ у `.tmp/…` — сверка показывала «изменилось ВСЁ».
- Имена переменных в bash только латиницей; Windows OpenSSH не берёт ключ из папки проекта
  («bad permissions») — зовём `ssh.exe` из комплекта Git.

## Приёмка

```bash
node scripts/check-kniga.mjs           # 53
node scripts/check-kniga-control.mjs   # 81
node scripts/check-kniga-drill.mjs     # 63
node scripts/check-kniga-panel.mjs     # 208
node scripts/check-kniga-figures.mjs   # 21
LAB_URL=https://5.35.90.219.nip.io LAB_USER=klassio LAB_PASS=6hv98SBo4sQ node scripts/check-lab.mjs   # 53
```

Локальный стенд: `node scripts/yandex-test-server.mjs` → `http://localhost:8781/lab`.

---

## 🔴 Открыто

- **Вторая полноценная подача — решение за Кратовым.** Он сказал: «у меня должно быть 2–3
  версии урока с разным визуалом», но на вопрос, какую собирать, не ответил. Варианты:
  доразвить БЕТУ до проходимого урока (короткий путь — материал уже её) · собрать подачу
  «как ИнтернетУрок» по его ссылке от 07.08 · не плодить, а доводить АЛЬФУ.
- **Пункт 4 Владимира** (разжёвывание + пазл «расставь названия на карте») — решения нет,
  это единственная правка, добавляющая к учебнику своё.
- **48 пояснений «почему неверный вариант неверный»** — единственное, что придётся писать руками.
- **Баланс Beget** и **root-пароль сервера** (засветился в переписке) — с 10.08 не закрыты.
- Закрытые 3D-ветки (`/krit3`, `/hub`, `/lesson3d`) на витрину не возим: 1.2 ГБ, закрыты 02.08.
  Захочет посмотреть — отдельная задача (ассеты или галерея снимков).
