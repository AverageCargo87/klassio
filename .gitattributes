# Зеркало исходников витрины хранится БАЙТ В БАЙТ.
# Иначе git подменяет переводы строк, и «вернуть ровно то, что видел руководитель»
# перестаёт быть правдой: файл отличается от выложенного, хоть и работает.
lab/** -text
